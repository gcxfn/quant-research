# 仓库工作规范

本文件只保存当前项目长期有效的开发、研究和策略契约。实验参数、单次运行结果和交接记录写入对应的计划、预登记或研究报告。`docs/legacy/` 是旧项目的只读档案，其中的 AGENTS.md、任务书、收益数字和环境配置不代表当前项目已实现或已验证。

## 1. 目标与边界

- 目标：日频短线交易助手。Day0 收盘后根据市场、资金、费用和真实持仓，输出下一交易日最多三个新候选，以及已有持仓的调整计划；Day1 在早盘结束和午盘结束后各更新一次；用户手动执行并逐笔回填成交。
- 股票排除科创板、ST/*ST、北交所；按历史当时的状态过滤。创业板等权限由账户配置决定。股票与 ETF 分别建模、统一管理账户风险。
- 持仓最多四只股票，本轮不设 ETF 或其他防御资产席位；Day0 最多新增三只。总仓位不超过100%，单只标的不超过25%；数量是上限，不得为了凑数强行推荐。
- 输出必须包含动作（新建、补仓、减仓、止盈、止损、持有或不动）、可执行买卖区间、建议数量、目标仓位、触发条件、有效期、失效条件、风险说明和与上一版建议的变化。股票和 ETF 的进攻、防御仓位分别核算，再汇总到账户级风险。
- 每个进攻标的必须有止损区间；止损位置以策略失效位置和资产波动率为基础，以实际成交成本计算风险 R。止盈以 R 倍数和趋势状态为基础，采用分批止盈和动态移动保护线。防御资产也必须有趋势、回撤或状态切换形式的风险退出条件。
- 账户级止损独立于单个标的：根据单日损失、阶段回撤、权益高水位回撤和市场风险状态，统一暂停新增进攻仓位或降低进攻仓位。首次研究先固定三档账户级风险方案，连同阈值、恢复条件和冷却期一并预登记和比较，不能事后挑选有利参数。
- 分批补仓必须受总仓位、单标的仓位和补仓次数限制；止盈后的标的进入冷却期，只有重新满足买入条件才允许买回。
- Day0 计划用于下一交易日；Day1 11:30 和15:00 决策点分别生成下一半日使用的计划。新建议覆盖未成交旧建议，已成交部分只按真实持仓处理；半日结束未成交订单失效，若信号仍有效只能由下一版计划重新生成，不得沿用旧订单暗中延续。
- 订单是半日限价单：15:00 计划只在下一交易日早盘生效，11:30 计划只在当日午盘生效；同一半日内不得修改订单。买入或卖出必须严格穿透价格区间才成交，不能因触价就假设成交，也不能取更有利的成交价。跳空、无行情、停牌和非法涨跌停价格必须按保守规则处理并留痕。
- 风险减仓类订单与止盈类订单必须区分：风险减仓连续三个半日未成交时，按预登记规则进入下一半日开盘兜底，跌停或停牌继续顺延；止盈类订单到期失效，不得假设必然成交。
- 持仓按取得批次记录。部分卖出按当前剩余持仓的加权平均成本重新计算止盈和风险，补仓形成新的批次；股票当日新买份额当日不可卖，T+0 ETF 按冻结品种清单处理。
- 行情、持仓或成交数据过期、缺失或身份不一致时，停止新增建议，只输出风险提示和需要补录的数据；不得沿用过期建议继续产生新订单。
- 第一阶段仅研究和历史验证。不连接券商、不自动下单、不实现实时分钟级调仓，不把未验证候选称为可实盘策略。
- 阶段 C0–C9 的进入条件、方法与验收条件见 `docs/plans/stock-first-trust-rebuild.md`（挂接执行包 `docs/quant_stage_plan_20260922/01_master_plan.md` 与 `02_acceptance_matrix.csv`）；`docs/plans/daily-short-term-rebuild.md` 与阶段 A/B 的结论保留为历史记录，不再作为新阶段的验收来源。用户要求讨论或评估时只给评估，不自行实施代码。

### 1.2 主线策略与执行取舍

- 当前主线是：**个股为主要收益来源，现金为防守资金**；账户级结果包含全部闲置现金，不以股票投入部分的年化替代总账户成绩。账户级风险状态统一调整个股进攻暴露。ETF 与其他防御资产的旧结论只作历史档案，不承担个股收益任务，也不得用 ETF 达标替代个股成功。股票与现金共用一个账户账本，按取得批次核算目标仓位、风险与变化原因。
- 主线输出采用动态价格区间和半日更新，不强制使用固定阶梯止盈或盈利达到某金额即清仓。分区阶梯止盈、席位回收和固定网格只作为独立对照实验，不能混入主线结果或作为已验证策略。
- 主线首先验证账户级降仓能否降低组合回撤；无账户级降仓、市场趋势降仓和账户回撤降仓必须作为可区分的预登记配置。单标的止损和止盈参数不能与账户级阈值一起事后调优。
- 当前任何股票因子、ETF 组合或混合配置在通过开发期、验证期和研究目标门槛前，都只能称为研究候选，不能称为可实盘策略。

### 1.1 账户、资金与费用口径

- 正式运行的账户资金由用户账户输入提供，生产代码不得硬编码账户金额。**正式目标评估固定 500,000 元初始权益，任何实验配置不得覆盖该值**；账户级 10%/20% 门只在 500,000 元口径下评价。确需其他金额的诊断实验（例如合成手算样例的小额数字）必须在预登记中显式说明用途，且不得用于目标门判定，也不得与 50 万口径的结论并列比较。
- 总仓位上限为100%，必须预留实际交易费用；单只标的目标仓位上限为25%。账户规模变化时，可以按预登记规则调整持仓数量，但不能突破策略契约中的风险约束。
- 佣金按名义金额的万分之一计算，最低5元；股票卖出印花税在2023-08-28以前按0.1%、之后按0.05%分段计算；ETF不收印花税；过户费沿现有研究口径不计。所有研究和计划必须记录实际采用的费率版本。
- 每次实际成交必须回填证券、方向、数量、价格、时间和费用。未成交意图按半日订单规则失效；系统不得把建议价格或估算成交冒充实际成交。

### 1.3 阶段、权限与角色

- 阶段推进采用"用户逐段授权"：每个 C 阶段开始前由用户在该阶段指令中写明授权范围；未获授权的阶段不得开工，也不得把上一阶段的完成当成下一阶段的许可。
- 三种状态不得混用：运行状态（`not_started/running/completed/failed/aborted`）、工程验收（`pending/accepted/rejected/blocked`）、研究判定（`not_evaluated/no_edge/insufficient_evidence/promising_not_validated/target_met_in_dev/validation_failed/validated_historical`）。`completed` 只表示进程结束并留下结果。
- **实施者不得给自己签验收**：实施者只写 `completed`/`pending`，不能代写用户或独立审阅者的 `accepted`。独立审阅必须由另一个会话或另一个账号完成，并独立读取合同、输入与原始证据，不能只读实施摘要。实施者与审阅者不得共用同一签名。
- 每阶段证据写入 `docs/evidence/trust-rebuild/<run_id>/`，并保留生成命令、退出码与文件哈希。新提交、新数据或新配置后，旧验收只对旧身份有效。
- **权限必须如实申报**：若分支保护、独立执行环境、数据隔离或身份认证尚未部署，阶段报告必须显式写 `enforcement_level=procedural_only`，不得声称技术上无法越权。任何阶段不得修改 `data/_meta/` 下的校验基线或以新版覆盖旧证据。

## 2. 文件归属（唯一约定）

以下为目标结构，按实际任务创建，不预先生成空框架。

| 路径 | 唯一用途 |
|---|---|
| `README.md` | 项目入口、当前状态、已验证运行命令及文档链接 |
| `AGENTS.md` | 本工作规范；不堆积实验日志和交接记录 |
| `pyproject.toml`、对应锁文件 | 后续建立的依赖、工具配置和可复现环境；禁止多套并行依赖清单 |
| `src/quant/` | 可复用生产代码；建议分 `data/`、`factors/`、`research/`、`backtest/`、`portfolio/`、`cli/`，按需建立 |
| `configs/` | 可版本管理的市场规则、策略和实验配置；账户配置只放脱敏示例 |
| `tests/` | 单元、集成、契约测试；`tests/fixtures/` 只放小型合成或明确许可的测试样本 |
| `tools/` | 数据台账、校验、维护和薄命令入口；不得成为第二套策略或因子实现 |
| `tools/migration/` | 已存在的一次性历史迁移脚本，只读留痕，不作为日常命令运行 |
| `data/raw/` | 已有原始数据，追加批次、不覆盖、不移动 |
| `data/_meta/` | 已有原始数据台账与校验基线；不得作为普通缓存目录 |
| `data/processed/<dataset_id>/` | 可复现的标准化行情、历史股票池及质量报告，大文件不入 Git |
| `data/features/<feature_set_id>/` | 因子结果和有效性掩码，附输入与计算版本，大文件不入 Git |
| `data/cache/` | 可重建的临时计算缓存；不得存唯一证据或真实账户 |
| `data/local/` | 真实账户快照、成交回填及其他本机私有输入，不入 Git、不上传 |
| `artifacts/runs/<run_id>/` | 每次实验的配置快照、清单、日志、指标、订单、成交、权益和报告 |
| `artifacts/plans/<date>/<run_id>/` | 未来获准启用后的账户交易计划；含隐私，不入 Git |
| `docs/plans/` | 任务范围、阶段、验收和未决事项；每个主题维护一份主计划 |
| `docs/research/<experiment_id>.md` | 精简的假设、试验范围、证据路径、结论及淘汰原因 |
| `docs/decisions/` | 真正改变架构、研究窗口或规则的决策记录，一事一文 |
| `docs/evidence/` | 已有迁移证据及需要长期版本管理的小型脱敏证据；不放全量回测输出 |
| `docs/legacy/` | 旧项目档案，只读参考，不修改、不复制旧架构到新代码 |

- 不在根目录散放脚本、CSV、截图、日志、临时报告；不另建 `output/`、`results/`、`data/artifacts/` 等平行产物目录。
- 现存文件保留原位。本规范不授权搬迁、删除或重新生成历史文件。
- Python 文件和模块用 `snake_case`；文档用有意义的英文短横线名称。禁止 `final_v2_new` 等命名。
- `experiment_id` 使用 `exp-YYYYMMDD-主题`；`run_id` 使用 `YYYYMMDDTHHMMSS-主题-短唯一串`，每次运行独立目录，不覆盖失败或成功记录。
- 临时文件放运行目录的 `tmp/` 或系统临时目录；清理须限定到已确认可重建的目标，不做宽泛递归删除。
- 实施代码前更新 `.gitignore`：忽略原始/派生大数据、缓存、产物、私有账户、本地环境和凭据；保留小型合成测试样本与配置示例。当前尚未初始化 Git，不擅自提交、推送或上传。

## 3. 数据纪律与样本隔离

- 使用数据前读 `docs/DATA_SOURCES.md`、`docs/DATA_COVERAGE.md`；迁移身份问题查 `docs/MIGRATION_REPORT.md`。历史资料的结论只作线索，必须说明是否重新验证。
- `data/raw/` 只追加新批次；批次名、manifest、校验文件、补丁清单均是身份记录，不重命名、不手改。旧绝对路径通过读取层映射，不修改原件。
- 不覆盖 `data/_meta/sha256.tsv` 等现有校验基线来让校验通过。新增数据用新增清单记录；更新基线须保留旧身份和变更证据。
- 原始数据有已知缺陷；明确单位、复权、时间戳、缺失处理和来源优先级。相同上游的不同封装不能声称是独立验证。
- 开发期研究范围固定 2015–2020（预热只用作特征历史，不计绩效年份）。2021–2024 只在用户单独授权的一次性验证中使用，每次授权写明日期、数据、用途、配置与执行次数，且授权后该样本即计入"已看样本"，不得再用作调参或筛选。2025-01-01 及以后冻结，不用于研究、筛选、调参或绩效查看，解锁需要用户明确决定。读取混合年份文件时在交付研究数据前过滤；标签、成交和持有期不得跨越边界。
- 历史样本已经被旧项目反复查看，不能因换目录或重写代码就称为全新样本外。冻结区也不能在未审计历史访问前宣称从未查看。
- 股票池、ST、上市退市、板块、价格限制均使用历史时点信息；财务数据按实际可得时间处理，缺少披露时间不得假装精确 PIT。任何保守滞后替代都须记录并限制结论。
- 15:00 收盘信号最早下一交易日早盘执行；11:30 决策只能用于当日午盘。每个决策点只使用此前已经完整结束的行情，明确预测标签、下单与成交时间，禁止未来信息泄漏。
- 凭据仅放 `~/.quant-credentials/`；真实账户与日志不得包含 token，不将私有输入发送外部服务。

## 4. 技术与研究边界

- 技术候选：Polars + Parquet 负责标准化与因子；vectorbt 负责简化筛选；Backtrader 负责少量候选的账户级验证。它们尚未安装验收，不得写成已完成能力。
- 先用一个固定小实验核验依赖兼容、性能及成交语义；不因选型失败直接转向自建通用引擎，先记录具体阻塞与最小替代方案。
- 不开发通用因子 DSL、分布式任务平台、可视化平台或大规模自动挖因子系统。没有实验需要不扩展架构。
- 因子结果独立于回测工具。两个回测层复用信号定义与时间口径，不各自实现一套策略。vectorbt 初筛结果不是完整成交证据。
- 市场适配限于必要规则：T+1、可卖数量、历史费用、整手与零股、停牌涨跌停、分红送转、订单生命周期及现金分配；不能假设 Backtrader 默认已正确处理 A 股。
- 策略决策使用 Day0 收盘、Day1 11:30 和15:00 三类决策点；成交验证使用对应的半日行情。一个半日内不得根据不可观测的分钟路径修改订单或假设有利成交。
- 风险层分为单标的、进攻/防御袖套和账户级三层；风险 R、目标仓位、止盈止损、移动保护线和账户级降仓必须使用同一账户权益与实际成交成本口径。
- 不用前复权价格直接充当历史实际成交价；信号、真实价格、公司行动与账户估值必须有明确契约。
- 日线或半日触价不等于必然成交，不默认有利的日内路径，不允许 A 股当日新买仓位当日卖出。半日验证与未来实时功能分开。

## 5. 每次实验的最低要求

1. 先记录可证伪假设、基准、股票池、时期、少量固定参数、费用及淘汰标准，再运行。
2. 先小样本检查正确性和计时，再扩大；小样本仅作冒烟或探索，不能代表完整盈利验证。
3. 运行清单至少记录：实验/运行 ID、开始结束时间、状态、实际命令、输入数据身份、配置与代码摘要、环境版本、随机种子、时间划分、耗时和峰值内存。无 Git 时也记录实际源码摘要。
4. 因子缓存键必须包含输入数据版本、股票池/日期范围、调整和缺失口径、因子代码与参数；不只用文件名或修改时间命中缓存。
5. 运行状态区分 `running/completed/failed/aborted`。失败留下异常与部分进度，不把部分指标冒充完整结论。
6. 初筛实验先以小规模可恢复运行确认数据、成交和风险语义，不把冒烟结果当成策略结论。预计超过 30 分钟的任务，先有阶段计时、规模外推、资源上限、预算和可恢复进度；没有预算不直接全量运行。
7. 禁止默认全市场分钟展开、重复读取全部原始数据、无限参数网格或无限多进程。测出瓶颈后才优化。
8. 结论至少覆盖费用后表现、基准、回撤、换手、成交情况、分年份稳定性、收益集中度、账户级风险触发次数、进攻仓位路径和试验次数。保存失败方向，禁止仅展示最优参数。
9. 账户级风险控制至少比较三档预登记方案，并与无账户级止损的控制组对照；单标的止损、R倍数止盈和移动保护线不得与账户级阈值一起事后调参。
10. 严格时间顺序验证；训练/验证边界考虑标签重叠和持有期。测试结果用于调参后必须降级为探索样本；2025年以后冻结数据不得参与筛选、调参或绩效查看。

## 6. 开发与交付

- 所有项目路径从配置或项目根目录解析，不硬编码旧环境路径；兼容 Windows 中文路径和 UTF-8。新环境放仓库 `.venv/`，不修改旧 Qlib 环境。
- 共享逻辑在 `src/quant/`；实验差异放配置。维护工具保持轻量，避免导入完整研究栈。
- 输出合同必须能表达：新建、补仓、减仓、止盈、止损、持有、不动、账户级降仓、风险状态、建议版本、行情时间、有效期和失效原因；机器可读字段与展示文字分开。
- 必测：因子手算样例、时间隔离、历史池过滤、单位和复权、费用与现金守恒、T+1、整手、半日挂单失效、多候选现金竞争、分批成交、公司行动、风险 R、移动保护线、账户级降仓、数据过期拒绝新增建议。
- 不为通过测试降低交易真实性；不把发现的 bug 解释成策略优势。
- 真实成交回填必须能重建账户状态；未成交建议不得修改现金、持仓或可卖数量。账户数据、行情数据或策略身份不一致时必须失败关闭。
- 每项工作完成后必须同步更新相关计划与日志文件：主计划（`docs/plans/`）追加完成记录（实际改动、结果、运行目录、遗留事项），试验台账与受影响运行清单按需更新；阶段收尾另写对应短报告。计划文件未更新视为交付未完成，不得只留运行产物。
- 完成说明必须写实际改动、实际测试及结果、未完成内容；没有运行就写未运行，不承诺盈利，不虚报速度。
