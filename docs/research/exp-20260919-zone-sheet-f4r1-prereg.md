# exp-20260919-zone-sheet-f4r1 · 预登记（冻结于运行前）

- 冻结时间：2026-09-19T21:30:00+08:00（实施与运行在此之后；冻结后不改配置与判据）。
- 引擎：band engine **v1.4**（任务书 `docs/plans/p3-band-contract.md` §10，双签 pin 后记录 sha256_16 于运行 manifest；pin 前不运行）。
- 试验计账：策略线 270 → 273（三臂）；因子线 FT01 = 120 不变。

## 1. 可证伪假设

F3 复合因子月度名单（ICW、K=10、行业≤2、50 万）在**分区执行**下——a1/a2/吃反弹三档限价建仓、成本锚定分档止盈、高水棘轮止损、月中席位回收——dev(2015–2020) 至少一臂过冻结八门且净 CAGR ≥ 10%。机制假设：(i) 每票止损替代 T200-40 路径承担回撤控制，不在强年份月末低点减仓（攻击 F3R3 门 3 死因）；(ii) 止盈落袋降低回吐；(iii) 席位回收提高资金利用。任一机制反向（止损鞭打、止盈截断集中收益）则整臂淘汰——这正是测量目标。

## 2. 判据（冻结，零搜索）

门 1–7 沿 R16 原文；门 8 = 混合族 v3 交付口径的 v1.4 重述（契约 §10.1-5）：armed = 止损触发未成交留滞，分子 = k3 兜底 + armed 后自行成交，≥99%，stuck==0 硬断言。锚点照抄 F3R3：B1(m) = R16 C05 T200-40 rate-only（净 +0.60%、maxDD −39.82%）；B3′ = −0.0199。目标列 = dev 净 CAGR ≥ 10%；goal_met = dev_pass ∧ 目标列。

## 3. 范围与底盘

- dev 2015-01-05 – 2020-12-31；2021–2024 val 零接触。
- 底盘 = 混合族 v3 R3-06 逐字（legs 511010×0.15 + 518880×0.25、ETF meta band 0.10/t0/pm-only）；F3R3-A0 锚（200k）随行字节复现。
- 袖珍只保留 **ICW**（EW 三连败：F3R1/R2/R3 全面弱于 ICW，登记淘汰理由，不再跑）。初始现金 500,000。
- 成员资格：月度候选表 = ICW 复合当月排名前 2K（20 名）；K=10、行业≤2（申万 L1、2026 单快照、前视披露沿 F3R3）；缓冲续持 rank ≤ 2K；月内席位回收、冷却、行业计数按引擎 v1.4 §10.1-4。

## 4. 分区规则（用户 2026-09-19 确认"分区参数用你的"）

σ0 = 信号日前 20 个官方交易日 close-to-close 对数收益标准差 × P0（P0 = 信号日官方收盘；次新不足 20 日不入名单）。P = 决策点价（11:30 am.close / 15:00 官方收盘），每决策点刷新。

| 区 | 定义 | 动作 |
|---|---|---|
| a1 建仓区 | P−0.5σ0 ~ P−1σ0 | 限价挂近沿 P−0.5σ0，买入目标量 40%（严格穿透成交） |
| a2 补仓区 | P−1.5σ0 ~ P−2σ0 | 限价挂近沿 P−1.5σ0，买 40% |
| 吃反弹区 | P−2.5σ0 ~ P−3σ0 | 限价挂近沿 P−2.5σ0，买 20% |
| 失效 | session low < P0−3.5σ0 | 下一决策点取消全部未成交档 |
| b1 止盈 | WAC + 2σ0 | 卖当前持股 50%（成本锚定不重锚，无兜底） |
| b2 止盈 | WAC + 3σ0 | 清余仓（含零股） |
| c 止损 | max(HWM−3σ0)，只升不降 | 全清；跳空按开盘；T+1 残部次 session k3 |

目标量：w_T×权益(信号日 15:00)/P0 折股、逐档 floor 整手；无路径臂 w_T ≡ 0.06；expiry = 下一信号日 +1 历日。目标权重、有效期、失效条件均随月度表下发——对应产品日表字段（触发条件/分批区间/数量/有效期/仓位上限/失效条件）。

## 5. 三臂（零网格，全部冻结）

| 臂 | 止盈 | 止损 | T200-40 路径 |
|---|---|---|---|
| F4R1-A（推荐形态） | b1+b2 阶梯 | 有 | **无**（e_T≡0.90，w_T≡0.06） |
| F4R1-B（对照：保留路径） | b1+b2 阶梯 | 有 | 有（w_T=e_T×0.06/0.90，月末减至目标沿 F3R3） |
| F4R1-C（用户原版止盈） | b_full：WAC+2σ0 一次全卖 | 有 | 无 |

用户想法"盈利达额后换标的"的映射：b 区全清 → 席位回收（rank 顺位下一位按其自身 a1 等待触发，不追高）；臂 C 检验"一步全卖"对照"分批止盈"（臂 A/B）。

## 6. 数据身份

| 项 | 身份 |
|---|---|
| 复合因子 | F3R1 冻结输出逐字：`F3-ICW_composite.parquet`（sha256_16 `b09242ee542cf675`；EW `9b93696b14fdd31f` 仅存档不用） |
| 日线 | `data/processed/daily-stocks-…`（沿 F3R3 runner 同一 DAILY_PARQUET，manifest 运行时登记） |
| 半日 bar | `data/processed/halfday-bars-20260918/`（目录 sha256_16 `c5ab29a09d2533eb`） |
| 行业 | `data/raw/tushare/index_member_all/20260909-r1/chunk_all.csv`（`4edffc3994fb527a`） |
| ETF 腿 | `data/processed/etf-daily-20260919/`（沿 F3R3） |
| 引擎 | v1.4 pin sha256（运行 manifest 登记；三锚零回归后有效） |

## 7. 淘汰与停止规则

- 任一臂 dev_pass ∧ ≥10% → 停，向用户报告（val 消费须明确批准）。
- 三臂全灭 → 报告并停，不自行开 F4R2；死因按门归因（重点：门 3 分年、门 4 回撤分解【止损 vs 路径】、门 5 换手【止盈×ladder 并存的鞭打计数】、门 8 armed 台账）。
- A0 锚不字节复现 → 停 runner 修复（不消耗试验）。
- 小样本冒烟：先 2015–2016 两月窗口核对意图帧/成交/现金守恒，再全期；冒烟不产生结论。

## 8. 预算

单臂全期预计分钟级（F3R3 实测同规模 <3 分钟/臂）；总额 900 s、峰值 RSS 8 GB、无网格、单次运行；超预算中止留档。

## 9. 披露（结论必须携带）

行业 2026 单快照前视（沿 F3R3）；σ0 月内冻结不重估；止损同 session 先于止盈判定（保守）；止盈成交不撤在途买入档（鞭打风险，门 5 裁定）；HWM 用决策价非日内 high（更晚触发，保守）；买入近沿成交价不取区内更优价（保守）；门 8 分母只含止损留滞（止盈/买入到期单独计数）。

## 10. 修正记录（运行前登记，2026-09-19 22:31 主对话核验）

§6 半日 bar 身份哈希 `c5ab29a09d2533eb` **无法由当前目录复现**：manifest.json sha256_16 = `2a414174b5df2eee`（= F3R3 runner 已验收 pin，行数 pin 18,486,939）；12 文件聚合（相对路径+digest）= `0e8cad4f83470c7b`；引擎 `batch_aggregate_sha256` 约定 = `c52f3cdc3d184f48`。该哈希在本仓库无任何其他出处（全部 manifest/report grep 无命中），判定为冻结文本转写笔误。**有效身份更正为：`data/processed/halfday-bars-20260918/manifest.json` sha256_16 `2a414174b5df2eee` + 行数 18,486,939**（沿 F3R3 已验收 pin 链）。判据、配置、本体均不变——本节为身份澄清，非判据修改；F4R1 运行 manifest 按更正后身份登记。同表其余身份已亲验：ICW 组合 `b09242ee542cf675` ✅、行业 chunk `4edffc3994fb527a` ✅。

## 11. 运行层接缝裁定（2026-09-20 冒烟后、全期前，主对话裁定）

冒烟（run `20260920T011431-f4r1-smoke-4d9e`，六窗口全 PASS、A0 锚 5/5 逐字节、引擎 pin 三重登记）暴露两个运行层适配，裁定如下，判据不变：

- **F4R1-S1（ETF 腿意图优先级 10^6→999）**：引擎 BR-1 护栏要求意图帧 priority 全部 < zone_priority_base(1000)。999 仍为账本最底层。语义影响 = 同决策点现金竞争中 ETF 底盘腿先于个股阶梯拿资金（F3R3 中相反，但 F3R3 无 zones 层，不构成同口径对照差异）；产品语义上固定权重底盘优先于弹性袖珍合理。接受，全期沿冒烟口径。
- **F4R1-S2（臂 B"月末减至目标"结构性缺失）**：§5 写"月末减至目标沿 F3R3"，但 zones 模式无月末减仓原语（契约 BR-7 非目标）且 S-v14-15 禁止个股入意图帧。臂 B 实际保留 = w_T=e_T×0.06/0.90 缩放 + 月边界续持/退出再平衡。裁定：**接受现状，不为对照臂改引擎**；臂 B 按"部分路径对照"解读（路径保留缩放半边）；全期若 A vs B 差异成为结论关键点，先复核本接缝再下结论。
- σ0 口径钉死（冒烟实施）：21 个官方收盘（含信号日）产生 20 个 close-to-close 对数收益、样本标准差 ddof=1、原始未复权收盘——与 §4 一致，无歧义。
