# F2R1 修复后重建结果（exp-20260920-f2r1-rebuild）

## 结论

修复后的 F2R1 开发集重建已经完成。120 个因子全部生成结果，运行状态为 `completed`，运行目录为：

`artifacts/runs/20260920T161436-f2r1-rebuild-d46c81e1/`

本次重建证明工程链路可以运行并保存新证据，但不等于策略达到收益目标。年化收益 10% 的目标尚未在本报告中验证。

## 数据和边界

- 开发集：2015–2020
- 验证集：2021–2024，本次未用于筛选
- 冻结区：2025 年及以后未读取
- 因子：A25+B15+C20+D30+E20+F10，共 120 个
- 旧运行：`artifacts/runs/20260919T180000-f2r1-factor-batch/`，未修改

## 运行检查

- 运行清单状态：`completed`
- 因子结果数量：120
- 结果文件：`factor_results.json`
- 全套测试：490 passed，6 个既有 Polars sortedness 警告
- 入口测试：日期冻结、六族发现、旁路文件排除和新目录创建均通过

## 新旧结果比较

按原预登记中的 IC、单调性和 2015 年方向规则做基础比较。由于旧报告中的覆盖率门槛基线没有作为新入口的独立输入重新保存，本节的数量不把覆盖率门槛伪装成已完成；最终名单仍需补做覆盖率台账。

| 项目 | 旧结果 | 新重建 |
|---|---:|---:|
| 因子数量 | 120 | 120 |
| 基础规则通过 | 42 | 43 |
| IC 均值发生变化 | — | 120/120 |

按族统计（旧 / 新基础规则）：

- A 量价：14 / 14
- B 估值规模：7 / 6
- C 微观结构：4 / 4
- D 财务 PIT：13 / 13
- E 事件：1 / 2
- F 横截面：3 / 4

名单变化：

- 旧基础通过但新未通过：B04
- 新基础通过但旧未通过：E13、F02

这说明修复后所有因子的数值都发生了变化，不能把旧 F2R1 的具体 IC、筛选名单或下游组合结果继续当作当前证据。D 族没有出现基础通过数量变化，但仍需以新产物为准；数量不变不代表数值没有变化。

## 当前判断

- 工程重建：通过。
- 开发集因子结果：已生成，等待覆盖率台账和人工口径审查。
- 旧 F2R1 具体数字：降级为历史记录，不再作为当前精确证据。
- 策略收益和年化 10% 目标：尚未验证。
- 2021–2024 验证：尚未重新进行。

## 下一步

1. 用与原 F1 池完全一致的月度合格池基线补齐覆盖率门槛。
2. 固定最终 F2R1 新存活名单并记录淘汰原因。
3. 只用新名单登记 F3 组合实验，不复用旧组合结果。
4. 组合实验完成后，再进行一次独立的 2021–2024 二次过手验证，并单独判断是否达到年化 10% 目标。

## 覆盖率台账补充

已用开发集面板重新计算历史合格股票池和因子覆盖率：

- 历史合格池月度中位数：2752.5 个标的；
- 覆盖率定义：每个信号月有效因子值股票数 / 当月合格池股票数，再取月份中位数；
- 门槛：中位覆盖率至少 50%；
- 120 个因子中 111 个通过覆盖率门槛；
- 43 个通过基础 IC/单调性/2015 方向规则的因子中，E13 覆盖率仅 6%，被覆盖率门槛淘汰；
- 最终 F2R1 新存活数：42 个。

最终各族数量：A 14、B 6、C 4、D 13、E 1、F 4。

最终名单和逐因子覆盖率见：

- `artifacts/runs/20260920T161436-f2r1-rebuild-d46c81e1/final_survivors.csv`
- `artifacts/runs/20260920T161436-f2r1-rebuild-d46c81e1/coverage_audit.csv`
- `artifacts/runs/20260920T161436-f2r1-rebuild-d46c81e1/coverage_audit.json`

因此，覆盖率补齐后新结果最终通过数为 42，与旧结果数量相同，但名单和具体指标不能视为相同。

## 下一步决定

下一步可以登记 F3 组合实验，但必须只使用这 42 个新存活因子，并重新计算相关性去重；旧的 F3 组合结果不能直接复用。组合实验完成后，再独立进行 2021–2024 二次过手验证，并判断是否达到年化 10% 目标。

## 覆盖率审计复核更正

前一版覆盖率脚本在复核中发现两个实现问题：历史长度没有严格按信号日计算，且分子一度统计了合格池外的因子行。该中间结果已不作为证据。

已按“信号日 + 股票”重新计算：

- 合格股票必须在该信号日前至少有 60 条行情记录；
- 当日必须可交易、非 ST、且不属于排除板块；
- 分子只统计当日合格股票池中因子值非空的股票；
- 全部 120 个因子的覆盖率均不超过 100%；
- 120 个因子中 107 个通过覆盖率门槛；
- 43 个基础规则通过者中，只有 E13 因覆盖率 4.3% 被淘汰；
- 最终存活仍为 42 个，名单未因这次严格复核发生变化。

严格审计产物：

- `artifacts/runs/20260920T161436-f2r1-rebuild-d46c81e1/coverage_audit_strict.csv`

此前的 `coverage_audit.csv` 保留作过程记录，不作为最终证据。

## E16 真实重建补充

E16 已从真实 2015–2024 分红批次重新生成，并在开发期评估：

- 原始读取文件：2180 个；
- 研究冻结区外行：9495 行，未进入开发期；
- 事件去重后：33085 个事件；
- 真实冲突事件：10 个，按最新 `ann_date` 解决并记录；
- 新 E16 因子帧：160952 行；
- 统一评估：68 个信号月；
- IC 均值：0.0422；
- IC t 值：约 2.95；
- 单调性：0.742；
- 覆盖率：通过开发期门槛。

新评估运行目录：`artifacts/runs/20260920T181904-e16-rebuild-eval-3f6c4ae0/`

E16 的真实重建和开发期筛选通过，但这不代表 120 因子重建完成，也不代表策略达到年化 10%。D01–D30 仍需从四类原始财务数据重新生成后，才能重新确定完整 F2R1 存活名单。

## D 族真实重建补充

D 族已从四类真实财务原始分块重新生成中间表和 30 个新因子帧：

- `fina_indicator`：254913 行；
- `income`：248798 行；
- `balancesheet`：235453 行；
- `cashflow`：199178 行；
- PIT 合并中间表：130784 行、42 列；
- 非季度末异常：25 行，已排除并计入台账；
- 新 D 因子评估运行：`artifacts/runs/20260920T185600-f2r1-rebuild-94bd2058/`；
- 30 个因子全部完成，68 个开发期信号月；
- 30 个因子覆盖率均通过严格门槛。

按基础 IC、单调性和 2015 年方向规则，D 族暂有 14 个候选：

`D07,D08,D09,D10,D12,D13,D14,D17,D18,D19,D20,D21,D25,D30`

这 14 个还要与 A/B/C/E/F 新因子一起重新汇总，才能形成最终 F2R1 名单。它们仍然只是开发期因子候选，不代表策略收益达到年化 10%。

## 完整 120 因子重评估补充

新 D 族和新 E16 已与其余因子帧合并，重新评估全部 120 个因子：

- 新运行：`artifacts/runs/20260920T190224-f2r1-rebuild-85e7f7e5/`；
- 输入因子：120 个；
- 基础规则通过：44 个；
- 严格覆盖率后最终通过：43 个；
- 最终名单：A14、B6、C4、D14、E1、F4。

完整名单和逐因子覆盖率在：

`artifacts/runs/20260920T190224-f2r1-rebuild-85e7f7e5/final_screen.csv`

当前 43 个只是开发期筛选存活者。下一步才是基于这 43 个新因子重新做相关性去重、等权组合和账户级回测；旧 F3 结果继续作废。
