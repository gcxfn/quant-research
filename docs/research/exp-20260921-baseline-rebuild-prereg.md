# exp-20260921-baseline-rebuild 预登记：审计修复后的可信基线重建

冻结时间：2026-09-21（运行前）。本预登记对应任务书
`stock_first_quant_review_20260921.md` 第 4 节阶段 A，交付审计
对照与 50 万口径股票+现金基线，不构成策略筛选，不设通过线。

## 1. 可证伪假设

H1（C1/C2 影响）：滚动训练窗口方向修复（取最近 24 个标签完整
截面而非最早 24 段）与预测全集修复（当期名单只由当期信息决定）
会改变 alpha158 ML 管线的逐月 Rank IC 与回放净值；若两者逐位
相同，则修复无影响（假设被否证）。

H2（Q2 标签口径）：分红除权污染（raw 标签）对月度 IC 的影响
可量化且非零；adj（preclose 桥复权）为修复后主口径。

H3（账户口径）：同一信号在 20 万与 50 万初始资金下的净 CAGR
差异由最低佣金 5 元、整手颗粒与现金余量的规模效应解释，方向
为 50 万口径更接近费前理论值。

## 2. 固定范围与参数（运行前冻结，不得事后调整）

- 窗口：dev 2015-01-05..2020-12-31；val 2021-2024 与冻结区
  2025+ 零接触（`assert_frozen` 强制）。
- 股票池：R16 可执行池（历史时点过滤，主板、非 ST、上市>120
  自身交易日、20 日成交额中位数≥5000 万、收盘≥2 元）。
- 特征：Alpha158 全集 158 个（与归档运行同一 `alpha158_cross_section`），
  截面 rank 归一（原口径保留——修复只针对时间语义）。
- 训练：LightGBM 冻结参数（n_estimators=300, lr=0.05, leaves=31,
  min_child=100, ff=0.8, seed=7）；滚动窗口=标签完整可得截面中
  最近 24 段，最少 12 段；两个目标各跑一次：
  - `reg`：连续标签（对应归档 20260921T125117 原版）；
  - `bin`：截面 top-20% 二分类 + 概率输出（对应归档
    20260921T142710 头部版）。
- 标签：`adj`（close/preclose 桥复权，主口径）；`raw` 仅作差异
  对照（IC 层面，不进回放）。清算口径：停牌/退市名按下一信号日
  后第一个自身成交收盘计标签；无成交则保留在预测全集、标签缺失
  并披露计数。
- 回放账户：初始资金 500,000 元（统一口径）；股票+现金，无 ETF
  防御腿；现金零收益。费用：佣金万一、最低 5 元，印花税分段
  （2023-08-28 前 0.1%、后 0.05%——dev 全程 0.1%），过户费不计。
- 席位：K=4 缓冲（进前 4、出 >8），MF 腿（ML 分数排序）；单票
  权重上限 25%、总仓 100%；整手。
- 个股退出：SingleNameRules 宽止损（3×ATR20，10%..20%，与归档
  HEAD 回放一致）；+1R 保护线、+2R 半仓、+3R 清仓。
- 账户级：D3 动态档（G3，与归档 HEAD 回放同一配置）与 G0 恒定
  对照。
- 执行：半日时钟、严格穿透、T+1、停牌/涨跌停保守处理（引擎
  v1.5+clip-price 语义，锚定当前 HEAD 引擎 sha，非归档 pin）。

## 3. 曲线矩阵（每层一次只变一个开关）

| 曲线 | 分数源 | 账户级 | 个股退出 | 作用层 |
|---|---|---|---|---|
| FULL-bin | bin 修复版 | G3 | on | 完整配置（参照） |
| SIG-old | 归档头部版 ml_scores.json | G3 | on | 信号层（C1+C2 影响） |
| SIG-reg | reg 修复版 | G3 | on | 训练目标对照 |
| NOACCT-bin | bin 修复版 | G0 | on | 账户减仓层 |
| NOSNR-bin | bin 修复版 | G3 | off | 个股退出层 |
| IDEAL-bin | bin 修复版 | 理想 fractional 模拟 | 无 | 合法入场层 |
| FEE（事后） | FULL-bin 的 fills 重算 | — | — | 费用层（毛现金流权益） |

IDEAL 曲线用 `r16.simulate_r16` fractional=True（B1(m) 先例），
无整手/涨跌停/停牌约束，月频再平衡、费率表计费。五层贡献 =
FULL-bin 分别与各消融曲线的净 CAGR 差。

## 4. 运行清单与预算

- 训练运行：特征构建一次（≈10 分钟）+ 两个 objective 各一次
  滚动训练；预算 1500 秒，超时 fail。
- 回放运行：6 条引擎曲线 + 1 条模拟；预算 1200 秒。
- 记录：manifest 含引擎/数据 sha、参数、逐月训练窗口诊断
  （train_start/train_end/label_end_latest/样本数/标签缺失数）。
- 淘汰标准：无（基线重建）。凡与归档结果矛盾处只报告不解释为
  策略优势。

## 5. 纪律

- 单次运行，不重试调参；发现缺陷即停并记录。
- 归档运行一律不覆盖；旧 20 万结果仅作历史对照引用。
- 本轮不消费 val；2025+ 冻结。
