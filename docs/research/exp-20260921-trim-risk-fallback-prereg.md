# 预登记：账户级降仓腿 risk 兜底口径修正对照（exp-20260921-trim-risk-fallback）

运行前冻结。主计划待办 4 前置项，源于任务①执行者登记的口径偏离。

## 1. 背景与目的

任务①（`artifacts/runs/20260921T024542-account-risk-full-8f16a4e9/`）的账户级
降仓腿（trim）登记为 `intent='profit'`（半日限价、到期失效、无兜底），与
AGENTS §1"风险减仓类订单连续三个半日未成交时按预登记规则进入下一半日
开盘兜底"的口径不一致。本实验把 trim 腿改为 `intent='risk'`（连续三半日
未成交 → 开盘市价兜底；引擎 `band_engine.py` M1 段已支持部分数量兜底，
无需改引擎），并与 profit 口径对照。

## 2. 设计

- 载体与三档参数：与任务①逐字相同（F3-ICW 载体、D1 单日损失/D2 阶段
  回撤/D3 高水位回撤，阈值、确认、冷却、E 系数全部不变）；
- 唯一变量：trim 腿 intent 类型；
  - **A 组 `trim_intent='profit'`（复现检查）**：在当前引擎身份 v1.5
    （`d4b6c3764780e6de`）重跑，预期 D1–D3 与任务① full 结果逐位一致
    （锚③已证 v1.5 对股票动态路径逐位不变；本组直接验证迁移性）；
  - **B 组 `trim_intent='risk'`（新口径）**：同载体同参数重跑；
- D0 控制组：两口径等价（D0 无 trim），跑一次；B 组 D0 须与任务① D0
  一致（一致性门沿用）；
- `DynamicOverlayHost` 增加 `trim_intent` 参数（默认 `'risk'` = AGENTS
  口径，显式传 `'profit'` 复现旧行为），取值校验 fail-closed。

## 3. 判据（对照，不选优）

1. A 组三档与任务① full 逐位一致性（逐帧 sha 比对 fills/events/
   daily_equity/clips_final）——不一致即运行失败（身份迁移论证破）；
2. B 组对照表：净年化、最大回撤、触发/恢复、降仓会话、trim 成交率、
   K=3 兜底次数、换手、费用——与 A 组同表并列，方向如实报告；
3. B 组披露部分成交与 armed 状态交互的任何异常结构（如实计数，不解释
   为优势或缺陷）；
4. 任何一组都不产生策略申报；三档方案选择仍留待固定方案实验。

## 4. 边界与预算

- 窗口 dev 2015-2020；val 2021–2024 与 2025+ 零接触；
- 预算：两组各 4 条曲线，墙钟 15 分钟、内存 6GB 上限；
- 试验台账：strategy_line 270 → 273（B 组 D1–D3 各 1；A 组为身份复现
  检查不计数；D0 复用任务①计数不重复计）；
- 引擎 pin `d4b6c3764780e6de`（v1.5），运行时校验。

## 5. 交付

- 运行目录 `artifacts/runs/<时间戳>-trim-risk-fallback-<短串>/`；
- 结果写入研究文档与主计划待办 4 前置项勾销。
