# 预登记：Alpha158 ML 信号 × 进攻宽度 10 只重估（exp-20260921-alpha158-k10）

运行前冻结。用户 2026-09-21 决定放宽进攻层到 10 只，行使 alpha158 预登记
第三步分支的"宽度重估"条款。

## 设计

- 信号：exp-20260921-alpha158 的 LightGBM 滚动分数（IC 0.046/t=4.94，
  模型与分数帧逐位复用，不重训、不调参）；
- 席位：K=10、entry top10 / exit rank>20（1:2 缓冲），每席目标权重
  0.40/10 = 4%（等分），单票 25% cap 不变；
- 单标的规则：R2A 宽止损（3×ATR20、10–20%）沿用；
- 账户级：两条曲线——MLK10-G0（无账户级对照）与 MLK10-G3（高水位
  降仓，参数同任务①）；
- 防御腿、引擎、执行时钟、费用全部同固定方案；基准 B1(m)_mix 不变。

## 判据

- 主观察量：净年化、回撤、换手、对基准超额 vs 4 席位版（−0.34%）；
- 八门 + 10% 目标列照报（参考）；执行率口径已知缺陷沿用；
- 解读锚点：IC 0.046 的理论毛超额（全池等权）约 4–6% 年化——K=10
  能否捕获其一半以上是"宽度假设"的判读线。

## 边界

- dev 2015–2020；val 与 2025+ 零接触；引擎 f3650f8440c713b8；
- 台账 285 → 287（两条曲线）；
- 禁止：依据结果微调 K（10 是用户指定值）；重训模型或改超参。
