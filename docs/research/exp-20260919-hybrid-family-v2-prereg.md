# 预登记：混合族 v2（S4/S2 结构修复 + 门 8 v3）

experiment_id: exp-20260919-hybrid-family-v2
状态：**已冻结（2026-09-19 06:2x）**。冻结后不得改配置/判据/宇宙。
前置：v1（`exp-20260919-hybrid-family-v1-prereg.md` 及其结果文档）0/8 淘汰；本 v2 只做结构性修复与判据原生化，**引擎零改动**（pin 不变，见 §5）。v1 预登记中与本 v2 不冲突的条款（数据 pins、分红分类规则、runner 断言、费用、窗口）逐字继承，本文只写差异。

## 1. 假设（可证伪）

v1 证据：HYB-05（债20+金20）距门 4 仅 0.3bp、优势年 5/6；腿收益线性于实际腿重且 M2 逐符号 ≤25% 是硬约束 → **更高 park 须拆多符号**。假设：逐符号 ≤25% 的更高 park 结构（45-60%）与防御型轮动能把回撤压入 20% 线内并保持净年化 ≥ v1 最优；若仍死于门 3/4，则"腿结构修复不足以过线"，混合线暂停转向新信号族。

**元过拟合披露（加重）**：本 v2 的 park 上调与结构选择直接源自 v1 死因（0.3bp 差距）与腿资产 dev 表现的已知数据；dev 判定即使全过仍属探索级，只有 val 一次性判定（须用户批准）可升级。

## 2. 宇宙与配置（冻结）

宇宙、剔除证据、带宽表、分红事件集（dev 内 18 次）与 v1 逐字相同；新增腿符号 159934（黄金，全窗 2,431 日、dev +8.14%、dev 内 1 次无跳变微偏离 2020-02-26 −0.22% 不建模披露、band 0.10、t_plus 0）。

| id | 股票 | 腿（逐符号权重） | park | 轮动 |
|---|---|---|---|---|
| H2-A0 | C05 | 无 | — | 无（回归锚，输出必须=P3R2 C05 四帧） |
| H2-01 | C05 | 511010×0.25 + 518880×0.25 | 50% | 无 |
| H2-02 | C05 | 518880×0.20 + 159934×0.20 | 40% | 无 |
| H2-03 | C05 | 511010×0.20 + 518880×0.20 + 159934×0.20 | 60% | 无 |
| H2-04 | C05 | 511010×0.25 + 518880×0.20 | 45% | 无 |
| ROT-03R | 无 | 511010×0.25 | 25% | top3×25% K6m，**7 成员宇宙**（8 成员剔除腿符号 511010） |
| ROT-04 | 无 | 无 | — | top4×25% K6m，7 成员宇宙（宇宙敏感性对照） |
| ROT-05 | 无 | 511010×0.25 + 518880×0.25 | 50% | top2×25% K6m，**6 成员宇宙**（8 成员剔除两腿符号） |

腿语义 Design B 与 v1 逐字（首决策点一次性 park 买入 + 月度减至目标；source_signal=决策日日期；expiry=第 4 信号日+1 日（初始买）/下一信号日+1 日（月度单）；priority ≥10^6 股票优先；m5-b 断言）。多腿=多行独立意图，逐符号 ≤25% 结构性满足 M2。S2 修复=腿符号从轮动排名宇宙剔除，意图键冲突不可能再发生。

## 3. 判据（八门 + 门 8 v3 + 目标列）

门 1-7 与 R16/v1 逐字；基准同源（B1(m)=R16 metrics.json C05 verbatim；B3′(T200-40)=−1.99%）。

**门 8 v3（判据原生化第二版，显式偏离）**：最终交付率 =（市价兜底成交单数 + 武装后限价自身成交单数）/ 兜底武装单数 ≥99%，期末武装后滞留持仓 = 0（硬断言）；两者分列披露（v1 实测案例：sh.600518 8 时段跌停顺延后限价离场，无滞留）。v2 的 8v2 口径与 R16 原口径均作对照列。偏离链：R16 95%（市价语义）→ v1 8v2（只计市价兜底，武装后限价离场不计）→ v3（最终交付，含武装后限价）。每次变更均已/将如实入台账与结果文档。

**目标列（非门）**：dev 净年化 ≥10%。

**晋级**：任一配置 dev 八门全过 → 停，报告用户申请 val；全不过 → 混合线暂停（停止条款），val 不动。**v1 已运行配置（HYB-05 等）不因 8v3 重判**（8v3 只影响门 8；HYB-05 的门 4 失败与门 8 无关，结论不变）。

## 4. 披露（v1 §8 全项继承，新增）

1. 逐配置：腿实际均重 vs 目标、股票腿 displacement（fills 数/成交率/入场延迟 vs H2-A0 差）、insufficient-cash 作废计数。
2. ROT-04 vs v1 ROT-01 的宇宙敏感性差（7 vs 8 成员）单列。
3. 159934 的 2020-02-26 −0.22% 无跳变偏离不建模（与 v1 159915 同类披露）。
4. park 60% 的现金竞争代价预期恶化（v1 park40 已有 677 次作废）——量化后如股票 displacement 侵蚀超额，如实报告。

## 5. 引擎与数据 pins（不变）

引擎 v1.2 sha256 = `ceb7be6414280e4c5d5f37dc6ca1148a85e306b2de1a216147f67f89cc4a746d`（与 v1 相同，零改动；runner 前后各核一次）。数据 pins、runner 三断言、费用、窗口（dev 2015-2020，val 冻结）与 v1 逐字。

## 6. 预算与停止

8 配置，预计 <1 分钟引擎时间、全程 ≤20 分钟；H2-A0 锚不一致即中止。停止条款：全族淘汰 → 混合线暂停，去向评估另议（候选：T1 事件驱动线待标注完成、新信号族）。
