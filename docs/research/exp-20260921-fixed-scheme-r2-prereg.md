# 探索登记：放宽止损 × 降低调仓频率（exp-20260921-fixed-scheme-r2）

## 性质声明（先于一切）

固定方案第一轮（exp-20260921-fixed-scheme）8 条全灭后，用户指示测试
"放宽止损标准"与"降低调仓频率"。本实验是**在已淘汰形态上的参数与
结构探索**：全部结果（包括任何过门数字）均为探索样本，按 AGENTS §5.10
不得作为策略申报或过门结论引用；其价值仅在于定位死因结构是否可修复。

## 设计

载体：固定方案最优曲线 **MF-G3**（F3-ICW 因子腿、K=4 席位、高水位
账户级降仓、40/60 攻防、防御腿/账户级/引擎全部不变）为对照（不重跑，
引用 `20260921T121615-fixed-scheme-6e8be/`）。三个变体：

| 变体 | 止损 | 调仓频率 |
|---|---|---|
| R2-A | **放宽**：3×ATR20、距离下限 10%、上限 20%（原 2×/5%/15%） | 月度（不变） |
| R2-B | 原参数 | **季度**（季末信号，71→24 个信号日；席位缓冲 entry4/exit>8 不变） |
| R2-C | 放宽（同 R2-A） | 季度（同 R2-B） |

止盈与保护线随 R 几何自然放宽（+2R/+3R/保护线规则不变）；防御腿
维持性再平衡（每 pm 重挂）不随信号频率改变；账户级 G3 参数不变。

## 判据（参考口径，非申报）

沿用八门 + 10% 目标列作**参考对照**（与第一轮同基准 B1(m)_mix
+5.65%/B3′_mix +5.64%，基准不随变体重算——季度变体对月度基准的超额
衡量调仓频率选择的代价，口径注明）。核心观察量：

1. 止损次数 / 止盈次数（对照 MF-G3：365 / 0）；
2. 净年化、回撤、换手 vs MF-G3（+2.74% / −17.4% / 4.7）；
3. 若放宽止损后止损仍远多于止盈 → "止损-频率冲突"诊断进一步坐实。

## 边界与台账

- dev 2015–2020；val 与 2025+ 零接触；引擎 `f3650f8440c713b8` 不改
  行为（仅 `SingleNameRules` 增加构造参数，默认值=原冻结值）；
- 台账 strategy_line 281 → 284（3 条探索曲线，标注探索性质）；
- 预算：每变体 <2 分钟，总计 <10 分钟；
- 禁止：从变体中挑最优申报策略；依据结果继续在本次登记外追加参数
  组合（再迭代需新登记）。
