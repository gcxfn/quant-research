# 混合族 val 一次性消费预登记（exp-20260919-hybrid-family-val）

**冻结时间：2026-09-19 15:0x（运行前冻结；冻结后不得改配置/判据/池）**

## 0. 权威与一次性声明

- 用户裁定（2026-09-19，"进行val"）：批准消费 val 窗（2021-01-01..2024-12-31）。**一次性、不可重复**：本预登记冻结的 3 个配置判定一次，无论结果如何不再重跑、不加配置；测试结果用于任何调整后须降级为探索样本（R16 既有纪律逐字）。
- 消费范围限定为下列 3 个 dev 存活纯 ETF 配置；**H2-01/03/04 不在本轮消费**（其股票引擎成分需 R16 信号层 val 端口，另行预登记与批准），保持 val-ready 未消费状态。

## 1. 配置（逐字继承，零新参数）

| ID | 腿 | 轮动 | 来源 |
|---|---|---|---|
| ROT-05 | 511010:0.25 + 518880:0.25 | UNI6 top2 ×0.25, lookback 6m | v2 判定（=v3 R3-A0 锚内容） |
| R3-05 | 518880:0.25 | UNI6 top3 ×0.25, lookback 6m | v3 判定 |
| R3-06 | 511010:0.15 + 518880:0.25 | UNI6 top3 ×0.20, lookback 6m | v3 判定 |

UNI6 = {sh.510050, sh.510300, sz.159915, sh.510880, sh.513100, sh.513500}；腿语义/费用/初始资金 200,000/意图层全部逐字沿 v2/v3 冻结规格。锚配置 ROT-05 同时承担回归锚角色（dev 窗），不重复计试验。

## 2. 窗口与信号

val 窗 2021-01-04..2024-12-31；月末日信号 2021-01..2024-11 共 47 个（2024-12-31 信号因执行日落 2025 丢弃、保留为到期锚——沿 dev 先例逐字）。**轮动冷启动沿冻结口径逐字**：`month_shift` 仅在窗口自身信号日内定位回看基准日 → 每窗前 6 个信号月无轮动（2021-01..06 轮动闲置、仅腿仓；2021-07 起 41 个排名月）——dev 首窗同构，非 val 特例，锚已验证。

## 3. 判据（冻结）

**R16 模块 `evaluate_val_gates` 逐字三门**（r16 预登记 §gates.val 冻结定义，2026-09-18 起未动）：

1. `1_excess_ge_1pp`：净年化超额 vs B1(m)(val) ≥ +1.0pp；
2. `2_advantage_years_ge_2_of_4`：优势年（逐年净收益 > B1m 同年）≥ 2/4；
3. `3_mdd_le_20pct_and_le_B1m`：maxDD ≤ 20% 且 ≤ |B1(m)(val) maxDD|。

非门披露项（照算照报，不进判定）：净年化 vs 10% 目标列、单边换手、任意日单票权重、门 8 v3 成交口径算术、现金竞争 displacement、逐年收益表。

## 4. 基准 B1(m)(val)

- 定义逐字沿 R16：`simulate_r16(fractional=True, fee_bands=B1_FEE_SCHEDULE_RATE_ONLY, T200-40 路径)` 于 val 窗现算（rate_only 为主口径；literal_min5 敏感性本轮不算——非门）。
- **端口验证（先于 val 数据消费）**：同一装配在 dev 窗重算，b1m_net_cagr / b1m_max_drawdown / b1m_net_return_by_year 三字段须逐位复现 R16 metrics.json configs.C05（容差 1e-9）；不等即中止，不得消费 val。
- B3′ 不进 val 门（R16 冻结 val 门集合无此门），dev 值仅历史留档。

## 5. 事件表（val 窗）

- **现金分红 12 笔**（检测规则逐字沿 v3：preclose<prev_close 且 1.0<因子比≤1.06）：510050×4、510300×4、510880×4（2026-09-19 实测盘点，其中 510880 2024-01-23 每份 0.131 元已获华泰柏瑞公告权威验证）。511010/518880/159915 val 窗零分红。
- **份额变换 2 笔（引擎 v1.3 corporate_actions）**：sh.513100 2022-01-14 ratio=**5**（factor_jump 5.0019）；sh.513500 2022-03-30 ratio=**2**（factor_jump 1.9848）。比例=公告整数（契约 §9 修订 1，国泰/博时公告原文证据）；引擎内置 |factor_jump/ratio−1|≤2% 交叉核验与护栏断言。
- dev 窗锚运行不传 corporate_actions（v1.3 缺省惰性，保证与 v3 锚逐字节可比）。

## 6. 引擎与回归锚

- 引擎 v1.3 pin `84a2443ac28fe1b87e4a18f988fd38cc67cf706d5778e262f11465147234082c`（运行前后校验）。
- **dev 锚（先跑）**：ROT-05 于 dev 窗 5 帧（intents/fills/events/daily/clips）.equals v3 R3-A0 产物 + net_cagr 匹配 1e-12；不过即中止。val 数据零接触前完成。

## 7. 披露与豁免重申

1. F-1 豁免口径（契约 §9.2-7 补充）：分红防误分类门在 preclose 不可评估时跳过——本轮 ETF 面板有 preclose 列，门全程可评估。
2. 2023-08-28 印花分段（0.1%→0.05%）在 val 窗首次真实经过（dev 全窗 0.1%）；ETF 免印花不受影响，仅影响……本 3 配置均为纯 ETF，**无股票腿，印花分段实际零触发**，照实披露。
3. S-v13-6：K=3 `k3_pnl_contribution` 披露统计若跨越份额变换失真（仅披露字段）；两变换日均停牌在先、在途卖单走重锚链，兜底跨越概率极低，照实报告。
4. val 窗内 513100/513500 的轮动动量信号用复权因子序列（连续），份额变换不影响信号层。

## 8. 试验计账与预算

- 试验 3 项：238 → 241（ROT-05/R3-05/R3-06 各 1）。锚与 B1m 基准不计。
- 预算：dev 锚（1 配置 + B1m dev 重算）+ val（3 配置 + B1m val），引擎单配置秒级、R16 模拟单次估 <60s；总墙钟估 <15 分钟、峰值内存 <6 GB。

## 9. 停止条款

- 锚失败或 B1m 端口验证失败 → 中止零消费，修复后重新预登记（不视为消费）。
- val 判定完成 → 无论过否，本预登记关闭；6 dev 存活配置的 val 状态在本轮后更新（3 判定 + 3 未消费）。

## 10. 边界修正（S-val-1，运行前登记；首次 val 运行于引擎前中止，零结果产出）

末日信号（2024-11-29）意图的到期日按冻结惯例 = 下一个信号日（2024-12-31 到期锚）+ 1 = **2025-01-01**，撞引擎冻结断言（≤2024-12-31；dev 窗同结构 expiry=2021-01-01 从未越界）。处置：**expiry 钳位至 2024-12-31**（运行时日志逐配置计数）。行为偏差如实披露：引擎到期语义为 `live_d >= expiry` 终止，钳位使受影响意图（每配置约 2-3 行，均为末日信号意图）在 **2024-12-31 当日**终止而非窗口后，损失该最后交易日当天的在途单存活（对账面权益的影响 ≤ 该日限价成交与收盘估值的差 + 费用）。dev 锚零影响（无钳位触发）。
