# 预登记：ETF 动量组合层回放（40/60 + D3）— exp-20260921-etf-momentum-comb

状态：**已冻结（运行前）**。用户 2026-09-21 批准。这是
`exp-20260921-etf-momentum`（阶段 A 淘汰：袖套 +13.31%/−29.70%，
信号活、裸形态死）后的组合层重试：同一信号、同一池，放进
40% 进攻 + 60% 防御 + 账户级 D3 的已验证容器，判据全部移到
组合级。除判据对象与组合形态外，信号与池子身份不变。

## 1. 可证伪假设

19 只权益 ETF 池的 126 日动量 top3 袖套，置于 40% 进攻预算、
60% 防御底盘（债 511010/金 518880/银 159934，月度再平衡+60日/10%
回撤退出）、账户级 D3 高水位降仓的组合结构中，开发期（2015-2020）
引擎回放能同时满足组合级八门；若任一门失败，假设证伪且本信号
家族的全部形态（裸袖套/组合层）均告淘汰，不再以新容器重试。

## 2. 池子与信号（身份不变，引用上轮预登记）

- 池：19 只，构建规则与清单逐字引用
  `exp-20260921-etf-momentum-prereg.md` §2（含科创排除条款）；
- 信号：月末收盘、`mom = adj[t]/adj[t-126 交易日] − 1`，
  `mom > 0` 中取前 3 等权，不足持现金；
- 袖套目标：每只 `0.40 × E / 3`，E = D3 状态机
  `exposure_multiplier / 0.90`（normal 1.0 / reduced ≈0.444）；
- 执行：band_engine v1.5 `f3650f8440c713b8`，
  `execution_clock='halfday'` + `etf_routing='pm_only'`，
  每 pm 决策点持续重挂（DefensiveLeg 同模式）；买入 intent=''、
  离场 'risk'、再平衡减仓 'profit'；初始资金 50 万。

## 3. 组合级八门（冻结）

基准：B1mix = 0.4×B1(m)_F3 + 0.6×防御（沿用 fixed-scheme-r2 已
归档基准，跨家族严格对照）；EQmix = 0.4×全池等权 + 0.6×防御
（同结构等权对照，月度合成、同费用口径）。

| # | 门 |
|---|---|
| 1 | 组合净年化 > 0 |
| 2 | 净年化 − B1mix ≥ 2pp |
| 3 | 净年化 − EQmix ≥ 1pp |
| 4 | 优势年（vs B1mix 分年）≥ 5/6 |
| 5 | 组合最大回撤 ≤ 20% 且 ≤ B1mix 回撤 |
| 6 | 单边最大年换手 ≤ 6 |
| 7 | 单票最大权重 ≤ 25%（AGENTS 契约口径） |
| 8 | 席位执行率 ≥ 95%（seat = distinct (symbol, side, source_signal)，成交须落在该 source 已生效的决策点） |

目标参照（只报告不设门）：≥6% 与 ≥10%。

## 4. 淘汰与纪律

- 任一门失败 → 本预登记淘汰，不调参、不换容器重试；
- dev 2015-01-05..2020-12-31 单次运行；val 2021-2024 零接触；
  2025+ 冻结；
- 台账 strategy_line 计 1 条（289 → 290）；
- 已知局限沿用上轮：行业代表 2016-2018 上市，2015-2017 池子以
  宽基/海外为主；引擎不建模 ETF 现金分红（防御基准用复权收益，
  策略侧略低估）；
- 过门后 val 消费须用户另行批准。

## 5. 证据路径

- 信号与池：`artifacts/runs/20260921T135642-etf-mom-7c2a/`（阶段 A
  运行，holdings 逐月落盘）；
- 防御与 D3 组件：`fixed_scheme_lib.DefensiveLeg`、
  `risk_overlay.AccountRiskDynamics`；
- 基准：`artifacts/runs/20260921T121615-fixed-scheme-6e8be/outputs/
  metrics_and_gates.json`；
- 结论将写入 `docs/research/exp-20260921-etf-momentum-comb.md`。
