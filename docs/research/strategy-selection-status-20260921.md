# 策略筛选状态（2026-09-21）

## 结论

截至当前已有冻结产物，没有候选同时满足：

1. 开发期费用后年化收益达到用户目标 10%；
2. 验证期通过预登记验证门槛；
3. 回撤、成交、换手、T+1 和费用口径均保持有效。

因此当前不能把任何策略称为“最终达标策略”。

## 证据表

| 候选 | 开发期结果 | 验证期结果 | 结论 |
|---|---:|---:|---|
| F3R2-A0 / R3-06 混合底盘 | 净年化 11.06%，8 项开发门槛全过，最大回撤 -16.22% | 原型 R3-06 已验证：净年化1.77%，最大回撤-15.14% | 未达到10%目标；不作为达标主候选 |
| H2-03 | 前序开发期未达到 10% 目标 | 验证期净年化 1.81%，验证门槛通过，最大回撤 -12.11% | 验证稳健但未达到研究目标 |
| F3R2-EW | 净年化 3.77%，开发期失败 3、4、5 门 | 未进入有效验证 | 淘汰 |
| F3R2-ICW | 净年化 6.55%，开发期回撤 -33.05% | 未进入有效验证 | 淘汰 |
| F3R3-EW / F3R3-ICW（动态引擎重测，2026-09-21） | +6.50% / +8.52%，均 0/8 门（失败门：优势年、回撤、换手；执行率门已修复至 96.9%） | 未进入验证（0/8 淘汰） | 淘汰；执行模型差异不改变结论 |

来源：

- `artifacts/runs/20260919T194700-f3r2-hybrid-chassis-e7a1/outputs/metrics_and_gates.json`
- `artifacts/runs/20260920T130406-val-batch-hybrid-9547/outputs/metrics_and_gates.json`
- `artifacts/runs/20260921T024449-f3r3-dynamic-45037d/outputs/metrics_and_gates.json`

## 当前主候选

此前遗漏了 R3-06 已保存的验证结果。2026-09-21 直接读取 `artifacts/runs/20260919T151630-hybrid-val-v1p3/outputs/R3-06_daily_equity.parquet` 独立复算：2021-01-04 权益200000，2024-12-31权益214510.7；按365.25天年化为0.01771369550217261，权益高水位回撤为-0.15141028293121084，均与该运行 metrics_and_gates.json 一致。原型已消费验证期且未达10%，不能因另取A0名称把它重新视为未验证候选。此复算只确认保存曲线的指标，未重跑历史成交。

账户级 C1/C2 风险覆盖曾建立 fail-closed 接入层；2026-09-21 已完成**动态账本回放**：三档预登记账户级方案（单日损失 3%/阶段回撤 60 会话峰 8%/高水位回撤 10%，E 0.90→0.40、2 会话确认 + 3 会话冷却）接入 `intent_provider`，控制组与 F3R3 载体逐帧一致。结果（`docs/research/exp-20260921-account-risk-dynamic.md`）：D3 高水位省 9.64pp 回撤、丢 2.04pp 年化（省÷丢 4.73），D2 为 0.35，D1 反而更差（回撤 −45.18%、年化 +2.68%）；四条曲线均未过八门、10% 目标未达，未产生达标候选，账户级降仓仍只是"以年化换回撤"的交换率证据。

### 账户反馈入口复核

2026-09-21 修复 `run_overlay_backtest` 的确认标志绕过：原先仅传 `dynamic_equity_confirmed=True` 就可运行静态 C2 意图，无法证明权益来自本策略实际成交。现在一次性入口拒绝 C2，包括确认标志为真及动态元数据被置为假的情况；C0/C1保留。完整动态回放尚未实现，不能称为账户止损已验证。

执行 `$env:PYTHONPATH=(Join-Path (Get-Location) 'src'); .venv/Scripts/python.exe -m pytest tests/unit/test_risk_overlay_runner.py tests/unit/test_risk_overlay.py -q`：31 passed。首次未设置PYTHONPATH时导入了 `.venv/Lib/site-packages/quant` 的旧副本，29通过、2失败；已确认不是本轮源码测试结果，不以此覆盖或更新环境副本。

## 验证推进记录

### 最新源码身份与工程检查（2026-09-21）

- `src/quant/backtest/band_engine.py` SHA256：`8850809d146b2b466eac2c54a83ed2083354812a6c21ff3470a3eab503a6685c`。
- `src/quant/research/risk_overlay_runner.py` SHA256：`6f4fb1b16d67f068a72e8d6762230fd5496bd410494245af34cc15d1bf6e0d99`。
- 半日路由、缺栏拒绝和上午权益未来扰动检查已加入 `tests/test_band_hybrid.py`。显式 `PYTHONPATH=src` 运行 hybrid、intents、contract 三文件：44 passed。此前风险层31 passed是另一批次，不能宣称全仓测试通过。
- 旧31022bab引擎的五帧一致性结果仅属于旧源码身份，本轮修改后未重跑历史全量回测，不能沿用旧证据声明新代码收益有效。
- 下一步仍为半日未成交建议失效、显式续发、风险兜底连续计数及真实账户反馈；无新增策略试验或收益结论。

- 此前一致性检查时 `band_engine.py` 哈希为 `31022bab...`，与开发期冻结哈希不同；它不是本轮当前源码哈希。
- 用当前引擎重跑 F3R2 开发期后，F3R2-A0 的 intents、fills、events、daily_equity、clips_final 五个帧仍与 R3-06 逐帧一致，说明这次引擎变化对该 intent 路径保持兼容。
- 2021–2024 验证尝试已按 fail-closed 规则停止：F3R1 冻结 composite 文件只含 2015–2020 的信号日，不能直接复用到验证期。
- 对 EW/ICW 股票袖套，需要在验证期重新计算冻结组合分数；这不是 A0 的必要输入。以下依赖复核纠正此前推进判断。

## 2026-09-21 依赖与验收纠正

- 已读取 A0 验证 runner 的 CFGS：A0 的 sleeve=None，使用 ETF rotation（top_n=3）和两只底仓 ETF。因此股票 composite 缺失只阻塞 EW/ICW，不能作为 A0 验证的必需前置任务。
- 当前派生验证 runner 仍无条件加载 EW/ICW，并将验证期五个输出帧与开发期帧比较，且 net_cagr_matches_v3_metrics=None 后执行 all(regression.values())，必然失败。此入口尚不能用于正式验证；应分离开发期一致性检查和验证期运行，不能直接删除全部身份检查。
- 当前 F3R1 outputs/dedup_clusters.csv 确认：43 个存活因子去重后是20个代表；C01/C11/C12归入F09代表的簇，C族实际代表只有C19。此前将43个存活因子称作代表、将4个C族存活因子都称作代表的表述错误。
- A0 最多持有5只ETF，与最新契约的防御层最多3只不一致。其历史收益只能作为对照候选证据，不能直接宣称完整主线达标；按新契约调整需要新预登记与独立运行。
- 未发现当前仍运行的D族脚本进程。此前后台启动命令未显式设置VAL_WINDOW，脚本默认dev；不能据启动命令认定验证期D族正在生成。已有产物保留，使用前须校验实际日期与输入身份。
- 下一步（已有验证证据复核后更新）：不再优先修复并重复运行A0对照验证。主线仍须完成股票/ETF层级上限、动态止盈止损、半日更新和账户风险三档对照；先在开发期建立完整契约的控制组与风险方案，不能依据已查看的验证收益反复调参。完整目标尚未达成。

## 2026-09-21 验证期数据访问事件登记（用户授权处置记录）

背景：用户确认 2026-09-21 当天仅授权任务①（账户级风险接入）与任务②（ETF 动态冒烟）两项工作。经盘面核查，当日凌晨（本地约 00:08–01:02）另存在三个与本文件"依赖与验收纠正"复核同链路、但未在任务授权范围内的运行。事实与处置如下，全部留痕。

### 三个运行的事实披露

| 运行 | 状态 | 实际数据接触 | 产出 |
|---|---|---|---|
| `20260921T091000-f3r2-current-engine-parity` | completed | 纯开发期（2015–2020），val 零接触 | F3R2 三臂 dev 期帧；A0 五帧与 R3-06 逐帧一致（本文件 §依赖复核已引用） |
| `20260921T090000-f3r2-a0-validation` | failed | **读取了 2021 年起的信号日与池构建**（47 个信号日、pool n=1524）后死于 composite 期身份校验 | 无任何指标产出 |
| `20260921T100000-f3-validation-composite` | failed_partial | **生成 2021–2024 验证期因子帧共 60 个**：A 族 25 + B 族 15 + C 族 20（合计约 1,060 万行）；D/E/F 未做 | 无验证 composite、无收益结论 |

更正：该运行自身 status.md 称 C_micro "无输出"，与事实不符——C_micro 20 帧已生成（首次因相对路径散落于顶层 `artifacts/runs/outputs/`，重跑后写入运行目录 `outputs/factor_val/C_micro/`）。

### 纪律评估

- F3R1（旧引擎）与 F3R3（动态引擎）两轮均为 0/8 门淘汰，预登记停止规则写明 "no arm passed all dev gates; val not consumed"。上述两个验证尝试（a0-validation、f3-validation-composite）在无过门配置的情况下读取/加工了 2021–2024 验证期数据，违反该停止规则。
- A0（R3-06）的验证期此前已被消费（+1.77%），重复验证尝试不产生新信息，只会加深折扣。
- 影响边界：未发生以 val 数据为输入的因子筛选、调参或绩效评估；无验证 composite 与任何结论产出。损害为验证期数据被部分读取加工，验证期证据折扣进一步加深——今后任何配置使用 2021–2024 验证时，本事件必须随附披露。

### 处置（2026-09-21，用户授权"看着执行"）

1. 三个运行目录整体保留留痕，不删除、不修改其内容；
2. 顶层散落副本 `artifacts/runs/outputs/`（C_micro 40 文件与运行目录副本逐字节一致，核验 identical 40 / different 0；另含空 `factor_dev/D_fund`）已删除，消除目录规范违规；
3. 不执行 `20260921T100000` manifest 中遗留的四条复现命令（即不续跑 C/D/E/F 与 composite）；
4. EW/ICW 的验证期 composite 维持不生成；F 线任何后续验证期使用须用户明确批准并预登记；
5. 证据表已同步补入 F3R3 动态引擎重测行（两臂仍淘汰，执行模型差异不改变结论）。

### 关联

- 引擎侧当日工作见 `docs/research/exp-20260921-factor-round-f3r3.md`（F3R3 重测）与 `docs/decisions/2026-09-21-etf-pm-only-routing.md`（ETF pm-only 路由决策）；
- 任务②数据身份核验（ETF 半日批零覆盖实证）：`artifacts/runs/20260921T023606-etf-dynamic-identity-e41c/`。
