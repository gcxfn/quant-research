# F3 新因子组合阶段状态（exp-20260920-f3r1-rebuild）

## 当前状态：暂停，原因已定位

F3 去重不能直接开始。审查发现，F2R1 新入口虽然重新计算了 120 个因子指标，但读取的因子 Parquet 仍来自旧 F2R1 产物。这样只能验证评估入口，不能证明修复后的财务派生逻辑已经重新生成 D 族因子帧。

因此，先前生成的：

`artifacts/runs/20260920T170000-f3r1-rebuild-local/`

已在 manifest 中标记为 `invalid_intermediate`，不作为研究证据，也不进入组合。

## 需要先完成的工作

1. 从原始开发期输入重新生成修复后的因子帧，至少覆盖 D 财务族和 E16；
2. 确认新帧使用 `fund_derive.py` 的确定性版本；
3. 重新用新帧评估 120 个因子；
4. 重新做严格覆盖率审计；
5. 只有这一步完成后，才对新存活因子做 F3 相关性去重。

## 已确认

- 2025 年以后数据仍冻结；
- 历史 F2R1 和旧 F3 结果均未覆盖；
- 入口测试和开发集边界测试已经通过；
- 这次暂停是研究证据保护，不是运行错误。

## F3 新因子账户级结果

基于新 F2R1 43 个存活因子、20 个相关性代表因子，按原 F3 账户级规则运行两条组合：

- F3-EW：净年化 9.47%，最大回撤 -39.75%，执行率 74.9%，0/8 gates；
- F3-ICW：净年化 15.28%，最大回撤 -41.19%，执行率 76.7%，0/8 gates。

F3-ICW 虽达到 10% 收益目标，但回撤、优势年份、换手和执行率均不满足要求，因此当前没有合格策略。验证集没有消费。

运行目录：`artifacts/runs/20260920T190500-f3r1-rebuild/`

该运行的 manifest 开始时间仍受旧计时器缺陷影响，目录和日志中的运行顺序及结束时间可核对；后续运行必须修复时间记录后再作为正式证据。

## 后续：动态引擎重测（F3R3）

本报告的两臂数字是旧引擎（v1.3 静态帧、legacy 时钟）结果。2026-09-21 已在重建后的动态引擎（半日时钟 + intent_provider 席位重发）上重测同一组合：两臂仍 0/8 门淘汰，净年化降至 +6.50%（EW）/ +8.52%（ICW），执行率修复到 96.9%，换手升至 9.6–9.9。详见 `docs/research/exp-20260921-factor-round-f3r3.md`；本报告数字保留为旧引擎对照，不再单独作为当前口径。
