# QuantDesk 阶段0：恢复核查报告

日期：2026-09-17。执行人：主对话（ZCode）。核查对象为 `docs/plans/QuantDesk-重建实施方案-20260917.md` 阶段0。

## 1. 服务器核查（117.72.219.131）

只读核查 + 一次真实 API 探测，未停止或修改任何现有容器。`price-comparison` 项目共存且正常运行，未受影响。

| 项目 | 结果 |
|---|---|
| SSH 访问 | 可用（root） |
| `/opt/quantdesk` | 存在，229M，含 server/desktop/pi/infra/ops/verification/private/backups；**不是 Git 仓库** |
| 容器 | backend / pi-manager / model-gateway / edge(Caddy 443) / egress-proxy / postgres 全部 Up 5 days (healthy) |
| 备份 | `/opt/quantdesk/backups` 每日备份，最新 `quantdesk-20260916T190004Z` |
| 数据库 | quantdesk 库：9 个用户、2588 条新闻，最新新闻 2026-09-17 15:44（**采集仍在实时运行**） |
| HTTPS 公网入口 | `https://117.72.219.131/api/v1/status` 返回 200：database ok、model_gateway_connected true、algorithm_configured false（策略未接线，符合阶段5未完成状态）。注意：本地 curl 使用 `-k`，证书信任状态未核验 |
| 端到端模型调用 | 实测通过：注册 `stage0_probe_0917` → 登录 → POST /news/{id}/analysis 返回 202 queued → 25 秒后 status=completed，生成约 150 字中文简析（DeepSeek 真实调用） |
| Pi 最小会话 | 未直接实测对话；pi-manager 容器 healthy。列为待验证项 |

## 2. 代码拉取

已拉取到 `D:\量化\quantdesk-server\`（不入 Git，含服务器私有配置，注意其中 private/model.env 等）：

- `quantdesk-source.tar.gz`（91MB，250 文件，服务器上的打包源码）
- `qd-live-snapshot.tar.gz`（91MB，2026-09-17 服务器 /opt/quantdesk 当前快照，排除 backups）
- 解压后：`quantdesk/`（live 快照）、`apps/quantdesk/`（源码包）

## 3. 关键发现

1. **重建计划的大部分已经实现过**：服务器上 backend、桌面、Pi、网关、沙箱代码均已存在且容器健康运行 5 天。阶段1–4 的代码基础大概率已在，需逐阶段审计验收而不是从零重建。
2. `verification/REPORT.md`（09-12）当时记录"backend 尚不存在"；之后 5 天内已完成部署，说明报告滞后于实际状态。
3. 服务器源码没有版本管理（非 Git），不满足方案 §2.2 "从第一天起提交 Git"。
4. `algorithm_configured=false`：阶段5（策略接线）未完成，与计划一致。

## 4. 待验证 / 阻塞项（2026-09-18 更新）

- [x] HTTPS 证书信任：Let's Encrypt 真实签发、SAN=IP:117.72.219.131，本机 curl 不加 `-k` 即 200。注意为短周期证书（2026-09-15 至 09-22），依赖服务器上的续期 timer，需持续观察续期是否成功
- [x] Pi 私人会话端到端：创建会话 → 提交 research 消息（task 52723ad8，5 秒完成）→ 拉取到正常中文回答
- [x] 端到端模型调用（共享新闻分析，见 §1）
- [x] 备份可恢复性（2026-09-18 演练）：`quantdesk-20260916T190004Z` 全部 SHA256SUMS 通过；database.dump（PG 自定义格式）恢复到隔离临时容器，17 张表完整，9 用户 / 2029 新闻（备份时点）/ 14 会话 / 13 任务；演练容器已删除，生产容器未受影响。注：pg_restore 有 3 个被忽略的错误（--no-owner 权限类），正式恢复流程仍以 infra/scripts/restore.sh 为准
- [ ] 桌面 EXE 在干净 Windows 机器启动（子代理构建中）
- [ ] 生产 PostgreSQL 执行 `cli init-db` 建 `news_archive_jobs` 表（阶段2修复部署时）

## 5. 遗留测试数据

- 服务器新增测试账号：`stage0_probe_0917`（user id `ccbab404-5e0e-4b02-a07c-b16fbd9ba0ae`），用于阶段0探测，后续可删除。
