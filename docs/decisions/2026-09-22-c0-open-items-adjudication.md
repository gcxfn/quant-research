# 决策记录：C0 待裁决项全量关闭（2026-09-22）

**决策人：** 用户（O）｜**记录会话：** C0–C4 独立审阅会话｜**依据：** `docs/plans/stock-first-trust-rebuild.md` §7 的 12 项缺口清单

用户于 2026-09-22 裁定"都按建议来"，并补充"创业板个股无权限"。本记录关闭 §7 全部 12 项；`docs/evidence/trust-rebuild/20260922T013755-trust-c0-c17494/contract_conflicts.csv` 中的 `open_needs_user` 状态是 C0 时点的存档快照，自此以本记录为准（存档不改写）。

| # | 事项 | 裁定 | 落实 |
|---|---|---|---|
| 1 | 未提交工作处置 | 提交，拆两段：归因批次（C0-attrib）/ 信任重建 C0–C4＋返工＋裁决轮 | 本日两次 git 提交；**未授权推送远端** |
| 2 | 失败运行 `20260922T001213` 补登记 | 无需再裁决 | 已由 C4 完成（affected_runs.csv 新增 4 行 attrib2，含失败状态与取代关系） |
| 3 | 独立分支 / main 保护 | 不启用 | 单账号仓库 PR 审批自锁；维持独立会话审阅＋台账留痕，`enforcement_level=procedural_only` 如实申报 |
| 4 | 最小 CI | 建立 | `.github/workflows/tests.yml`（uv sync --frozen＋pytest tests -q）。为使 CI 可运行，5 个读真实数据的测试加数据缺失跳过守卫（v13 四项读 `etf-daily-20260919`、band_contract 06b 读 rqalpha bundle），与 `tests/contracts` 既有惯例一致；数据存在处断言全量执行，无数据环境实测 796 passed＋8 skipped、0 failed，本地 804 passed |
| 5 | K 角色与数据权限 | 用户承担 K | 接受同机全量数据现状（技术上不可隔离），靠接触台账（access_ledger）与逐段授权约束 |
| 6 | 金额舍入规则 | 用户追认审阅者裁定 | 费用/现金按分 ROUND_HALF_UP；引擎 float 不舍入与参考 Decimal 的差为数值表示差（日权益差 ≤0.14 元、精确残差 ~1e-10）。已登记于返工 R1 |
| 7 | 入场限价语义 | **确认为有意设计** | E0 臂维持"×1.01 是签发门槛、实际限价＝决策锚价"；C6-E1 臂（`floor_to_tick(anchor×1.01)`）按预登记另行实现，两臂差异即实验对象 |
| 8 | 创业板权限 | **用户确认账户无创业板（300/301/302）权限** | 维持现行排除（`screen.py` 默认），代码不动；本行即账户权限证据（2026-09-22 用户声明） |
| 9 | 研究范围口径 | 收窄 | 已并入 AGENTS §3：开发期 2015–2020；2021–2024 仅一次性验证、单独授权；2025+ 冻结 |
| 10 | AGENTS 六处修订 | **接受并合入** | 个股＋现金主线、四股票席位、50 万不可覆盖、验收来源指向 `stock-first-trust-rebuild.md`、新增 §1.3 阶段与权限、研究范围收窄；"防御资产退出条件"条款按提案保留（与资产类别无关，未来恢复防御资产仍适用） |
| 11 | 旧"C0"改名 | 同意 | 改称"复审修正批次（C0-attrib）"，README 已同步 |
| 12 | C1-S 预登记 | **授权起草（不授权运行）** | 草案 `docs/research/exp-20260922-c1s-seat-priority-prereg.md`；进入条件＝C5 证据门通过＋用户启动 C6 |

## 影响

- 主线自此为**个股＋现金**；ETF 旧结论降为历史档案，不得用于替代个股目标（AGENTS §1.2 新文本）。
- `find_repo_root` 依赖根目录 AGENTS.md 存在，属预期（仓库必含）。
- 本记录不改变交易合同费率、订单语义、样本冻结与"第一阶段仅研究"边界；不授权 C5 及之后任何阶段。
