# 更新日志

所有显著变更记录于此，格式参考 Keep a Changelog。策略版本号见 `configs/strategy.json` 的 `strategy_version`；重要设计决定的理由与证据见 `docs/decisions.md`。

## [未发布] - 2026-09-13

### T1/T3实施审核与CPU/GPU优化路线

- 新增并完善[T1/T3实施审核报告](docs/experiments/t1-t3-implementation-review-20260913.md)：T1已交付能力保留`PASS_WITH_LIMITATIONS`；T3运行准备和性能优化包维持`CHANGES_REQUIRED`。明确1430条合成汇总通过不代表真实全量因子计算完成。
- 记录四项整改意见：worker输入摘要未覆盖实际值及逐日期池掩码、旧批跑器只核数据清单文件而未核原始数据内容、A/B比较器可能误标精确一致、性能推算及占比口径需修正。输入值/掩码变化而摘要不变已独立复现；本轮未实施这些代码修复。
- 写入“CPU去重复＋GPU批量统计”详细方案：输入绑定与恢复、Spearman冗余排序消除、重复装载和数组转换优化、聚类月度输入提取与日频面板释放；GPU先做批量排名并保留CPU原顺序归约，再按实测决定是否扩展IC计算。补齐资源预算、测试矩阵、性能采用标准、新快照切换及CPU回退条件。
- 合成GPU稳定排序微基准：128日期×5552证券、float64、含并列，NumPy中位34.61ms，GPU显存内1.89ms，含上传/索引回传4.04ms；最后一轮排序索引精确相同。仅支持继续验证局部GPU原型，不代表完整Spearman或全量因子加速。
- 同步[GPU计划](docs/plans/gpu-factor-engine-plan-20260913.md)与[T3审核入口](docs/tasks/new-factor-research-review-20260912.md)，标明此前“无GPU批量收益来源而停止”的技术判断已修订，旧实施事实保留。
- 验证：本轮审核限定pytest 16 passed、35 deselected；worker检查点矩阵通过；T3合成正常汇总1430/1430接受；性能包38个文件摘要匹配。方案文档链接与一致性检查通过。这些证据不构成全量研究或引擎切换验收。
- 范围：本次完成审核、合成微基准及方案/日志更新；未修改算法、未恢复真实全量、未切换主仓、未更新策略版本。2025以后与生产冻结、统计UNKNOWN、晋级BLOCK不变；当前任务状态仍以各任务书末尾为入口。

## [v2.9.11] - 2026-09-07

### P3 修复后全窗重跑 + 独立评审 PASS（D-2026-09-07-13/14）
- 修复后首次 `p3-p1pool` 全窗重跑（约 55 分钟，池 5411 只，ST/停牌行保留）：**严格池八组 × 全窗口全部为负**（G1 −32.80%、G5 −12.84% 相对最好），修复前 G3 +0.393% 不成立；G1 等价断言通过。
- 匹配幸存者对照（117 只共同股票同价格序列仅切掩码）：掩码只解释约 1.1pp，旧池 vs 严格池 54.8pp 差距主要来自池构建/幸存者本身——旧策略正收益确认为池构建偏差的产物。
- 独立评审 PASS：八组读数、等价断言（对抗实跑验证非摆设）、匹配对照实现、新旧对比表均独立复核一致；低危修复 G1 断言 assert→显式 raise。
- 基线事实：无偏池上没有任何已测规则在任何窗口有正的扣费后收益；P4 新规则设计以此为出发点。全部研究/不可执行。324 项单测与 smoke 通过。

## [v2.9.10] - 2026-09-07

### P2 整包独立 PASS（D-2026-09-07-10/11/12）
- 入场校准包独立复审：数值全部独立复算通过（149/149 分钟覆盖、参与率分布、8 档位成交等价、98.66%/90比92、尺度转换、门禁），但 2 笔「日线乐观成交」归因被第二来源证伪（用户本地数据集逐位佐证日线低点，实为 baostock 分钟线漏极值）。
- 窄修复：入场差异改标 `DATA_QUALITY_MINUTE_EXTREME_MISSING`（三结构性条件 + UserDataset 佐证）；尺度回退 `or 1.0` 改 fail-closed；「日线触价 2.17% 乐观」叙事作废（比较数字仍正确）。修复后复审 PASS。
- **P2 整包独立 PASS**：引擎代码（D-03）、入场校准包、持仓/退出全生命周期对照三块均完成"实现→独立复审→窄修复→修复后复审"闭环。**分钟账本解锁可用于收益实验，P3 严格池重跑解锁**；`participation_rate=0.005` 转为 10 万元规模校准值。
- 最终口径（2024 严格池 91 笔）：MATCH 87、DQ 3（方向未定须仲裁）、锁单递延 1、退出原因分歧 0，已配对执行价差 +2627.12（剔 DQ 后 +1845.08，n=88）；全量 324 项单测通过、smoke PASS。

## [v2.9.9] - 2026-09-07

### P2 收尾独立复审与窄修复（D-2026-09-07-07/08）
- 三路独立复审：测试/纪律 PASS；代码正确性与实验证据 CHANGES_REQUESTED——发现 replay 未按交易窗口过滤 bar 的潜伏 lookahead、入场日复评 EXIT_PENDING 被过滤丢单、两笔「日线虚触发」归因不成立（T0050 实为 baostock 分钟线漏低点 0.32 元、T0033 实为分钟计划漏单）。
- 窄修复：窗口过滤消除 lookahead（补回归）；入场日复评事件纳入分钟计划（T0033、T0039 转 MATCH）；T0050 改标 `DATA_QUALITY_MINUTE_EXTREME_MISSING`（今后「分钟未触价」类结论须第二来源仲裁）；「±1 tick」披露更正为实测可达约 1.5%；一致率补 91 分母口径；净差分解为已配对价差 +2627.12 与未入场反转 −972.22 两分量；状态掩码死缺省改 fail-closed。
- 修复后 91 笔分类：MATCH 87、分钟未入场 2、数据质量分歧 1、锁单递延晚退 1、退出原因分歧 0。新增 8 项回归测试，全量 319 项通过、smoke PASS。
- **修复后独立复审（D-2026-09-07-09）两路均 PASS**：F1/映射修复经对抗实跑与全量重跑逐位复现，DQ 守卫无误标，旧工件零改动；遗留提示——解读已配对净差应剔除 DQ 笔（+1845.08，n=88）。持仓/退出对照至此取得独立 PASS；P2 整包剩余未审范围为 D-2026-09-07-05 入场校准包，该范围独立通过前整包继续锁定。

## [v2.9.8] - 2026-09-07

### P2 收尾：持仓/退出分钟生命周期对照（D-2026-09-07-06）
- `quant/short_strategy.py` 新增 `review_journal`（入场/复评续持/复评退出逐日审计事件），不改排序/成交/账本行为；与既有 149 单订单审计逐位一致。
- `experiments/short_foundation_research.py` 新增 `p2-target-trades`（2024 严格池日线交易真值落盘，构池/门禁逻辑抽为共用体）、`fetch-trade-minutes`（按持仓窗口补抓 5 分钟线，601 个 code/date 零请求失败）、`p2-minute-full-compare`（止损/止盈/复评续持/时间退出完整分钟回放，分段订单处理复评上调止损与公司行为尺度变化）。
- 对照结果（91 笔平仓交易，participation_rate=0.005）：MATCH 86/91（94.5%），退出原因一致率 97.8%；2 笔分钟不成交为已知日线乐观触价，2 笔日线虚触发（净差 −800/+782 元），1 笔日线低点 1 tick 偏差致晚退一日；净盈亏差异（去滑点基准）合计 +1009 元。
- 新增 21 项离线单测，全量 311 项运行、1 skip、全部通过；V3 fixture smoke `[SMOKE] PASS`。
- **状态仍锁定**：本轮为实现者记录，P2 整包独立复审未完成前账本不得用于收益实验，P3 收益实验不得开始；participation_rate=0.005 仍为候选值。

## [v2.9.7] - 2026-09-07

### 最终产品目标确认与 P2 后续
- 用户确认最终形态为两段式人工决策辅助：每日收盘后/次日盘前给出未来 1—5 交易日短线候选；用户选定标的后，盘中按实时行情刷新建议买卖价、数量、费用后保本、止损与目标。新买入遵守 T+1；只有已有可卖底仓时才给同日做T买卖对；继续保持 `MANUAL_ADVICE_ONLY`，不接券商、不自动成交。
- 当前 `short` 仍是研究输出，`t0 --symbol` 只覆盖已有底仓做T。下一阶段先完成目标策略订单样本、对应 5 分钟数据、参与率校准和日线/分钟成对撮合；P2 独立通过后才重跑 P3，P3 有样本外优势后才接入选定短线标的的盘中跟踪入口。
- 现有分钟目录只有 5 份有效事件样本和 1 份空样本，没有目标 universe 计划订单，不能据此冻结 `participation_rate=1.0`。后续只补抓策略实际命中的代码/日期，不扩成全市场分钟数据工程。
- P2 校准链路已补齐：`run_pipeline` 新增不改变行为的 `order_audit`；新增 `p2-target-orders`、`fetch-target-minute`、`p2-minute-entry-compare`，支持严格池订单导出、按订单日断点补抓、前复权计划价还原到原始分钟价格尺度及日线/分钟成对比较。一次性全窗构池因峰值内存约 4.2GB 主动中止，改为逐年度携带上一年预热，保持完整状态门禁并降低内存。
- 2024 严格池生成 149 个实际订单日（股票149、ETF 0），对应分钟数据 149/149 抓取成功，每日48根、零请求失败。7143根非零 bar 上计划股数/单bar成交量 p50=0.0273%、p95=1.5038%、p99=5.5556%；按顺序撮合参与率从100%降到0.05%仍不改变90笔可复现成交，0.01%首次少1笔，因此配置采用 **0.5% 候选值**，等待独立复审与完整持仓/退出分钟回放。
- 0.5% 入场对照：149/149 有分钟数据，成交与否一致率98.66%；日线92笔成交中90笔被分钟完整复现。两笔差异为日线最低价低估触价（600611：5.24 vs 分钟5.26；002583：19.86 vs 分钟19.89），证明日线只能粗筛、分钟撮合必须作为最终口径。首次未统一复权尺度的84.56%结果已判无效，保留工件但不得引用。
- 当前回归：全量 unittest 290 项运行、1 项 skip、其余通过；V3 fixture smoke `[SMOKE] PASS`；`git diff --check` 无空白错误。Smoke 中的合成收益只证明旧入口未破坏，不作为短线收益证据。

### 短线底座三包独立审查与修复（D-2026-09-06-05/07）
- 独立审查否决 P1/P2/P3 放行：发现严格池物理删除 ST 行导致跨年价格拼接、停牌日可产生信号、上市 252 日被按自然日计算；P2 存在 bar 参与量按订单重复使用、零股不可卖清、无 bar 公司行为漏账及信号时点可绕过；P3 存在 `high == low` 双向禁成交、G6 未真正强制退出以及幸存者偏差归因混杂。
- P1 修复：保留 ST/停牌历史价格行，用逐日资格掩码阻断信号与成交并在恢复后重新预热；配置改为 `min_listing_sessions=252`；流动性继续只读 t 日之前正常会话；复权因子与公司行为准入改为 fail-closed，并修正停牌、退市日与 qfq “6/6 严格零差异”的错误文案。
- P2 修复：同代码同 bar 的参与量上限在全部订单间共享递减；允许持仓零股一次性卖清；公司行为日期即使没有 bar 也进入账本；`submit`、`make_order` 与批量 `run(orders=...)` 均强制校验 `data_as_of <= signal_at < activate_at`，不得自动补造审计时点；账本明确为税前分红口径。
- D-2026-09-07-02 窄范围复审修复：QFQ 预登记样本改为全体 fail-closed，事件日差异限定为显式 `sz.300750` 数量/幅度 allowlist；退市因子跳变按研究窗与窗外分层，窗口外异常不再冒充全历史覆盖通过；执行回放对显式 `isST=None` 默认拒绝（仅 `status_eligible=True` 可覆盖），并校验 `participation_rate` 为有限 `[0,1]` 数。
- P3 修复：一字涨跌停改为按买卖方向判断；G6 `no_extend` 在第五个持有会话强制退出；G5/G6 降级为组合退出政策；严格池保持完整价格序列与逐日状态门禁。修正历史工件解读为 **7/8 转负，G3 全窗 +0.393%**，2023 起点是实验选择而非数据可得性强制。
- 验证：P2 专项 23 项通过；全量 unittest 275 项运行、1 项 skip、其余通过；fixture 端到端 smoke `[SMOKE] PASS`；`py_compile` 与 `git diff --check` 通过。
- **状态仍锁定**：`participation_rate=1.0` 已从“冻结参数”降为临时研究假设。目标 universe 参与率校准、匹配幸存者偏差对照、滚动/更早窗口及修复后分钟级对照尚未生成正式工件；完成并独立复审前，P2 账本不得用于收益实验，P3 不得作为“策略已反转”或可执行依据。本条取代 v2.9.3/v2.9.4/v2.9.6 中与当前状态冲突的验收、冻结及“全部转负”表述。

## [v2.9.6] - 2026-09-06

### P3×P1 严格池复测：旧结论全部反转（D-2026-09-06-04）
- 新增 `fetch-adjust`（343 个复权因子）与 `p3-p1pool` 子命令；`quant/history.py` 新增 `qfq_apply_factor`/`compare_return_series`（因子→qfq 构建经在市股验证组 6/6 严格零差异对账）。
- 同窗（2023→2026）对照：八组全部由正转负——G1 +24.31%→−21.46%、G2 去门槛→−53.34%、G7 杠杆→−40.57%/回撤 −61.7%。momentum_market 的历史正收益确认为幸存者偏差产物，D-2026-09-05-03 的历史筛选结论不成立。
- 基线事实：无偏池上无任何已测规则有正的扣费后优势；P4 重建以此为出发点，新规则先过严格池。相对排序部分稳健（宽基门槛仍最重要）。解读待独立评审。

## [v2.9.5] - 2026-09-06

### P3 机制归因：拆零件、复现门禁、八组对照（D-2026-09-06-03）
- 新增 `quant/short_strategy.py`：`run_short_term` 分解复刻（五类零件可切换，baseline 与旧引擎逐位等价，等价测试内建）；漏斗逐层计数（信号→排名落选→资金拒绝→成交→退出→资金占用）。
- 基线复现：`--replay-from` 重跑用户费用实验逐位一致（momentum_market +28.29%）。
- 八组单因素对照（旧池快照、日线撮合）：宽基门槛为最大正贡献（+36pp）、回撤限价入场负贡献、条件续持 +9.7pp、R20 优于 R60、固定 1/3 资金为纯杠杆且回撤超约束；漏斗显示 91.5% 信号死于排名落选、资金使用率 5.8%。
- 全部为初步读数，待独立评审；P1 严格池复测与 P2 分钟对照未做。252+9 项单测通过。

## [v2.9.4] - 2026-09-06

### P2 收尾：真实事件案例回放与参数冻结（D-2026-09-06-02）
- 新增 `fetch-minute`/`p2-cases` 子命令：6 个事件案例（一字涨停/触板打开/深跳空/大振幅/除息/停牌，按事件确定性选取）在真实 5 分钟数据上回放，6/6 通过；分钟量纲 1104 根 bar 零违例。
- 执行参数冻结（frozen-2026-09-06）：参与率 1.0、激活纪律严格晚于信号 bar；发现并披露 baostock 日线/分钟线同日最高价 1 tick 不一致（撮合以分钟为准）。
- P2 工程与验证完成；独立审查仍是账本用于策略研究前的前置条件。

## [v2.9.3] - 2026-09-06

### 全市场数据补齐与严格池达成（D-2026-09-06-01 补记）
- baostock `--all` 断点续抓 5552/5552 只完成（新增 4713、零失败、1.9GB）；退市股 337 只先行补齐。
- 池行来源修订为 baostock 全字段行（含 ST/停牌门禁；价格与用户数据集双源验证逐位一致），用户数据集转前复权/后复权信号序列与复核源。
- 严格全市场池达成：eligible 2570~4299/日，st_unknown=0、no_data=0，ST 排除 128~200、流动性剔除 651~2328、退市 250→337——P1 验收达成。

## [v2.9.2] - 2026-09-06

### 用户本地数据集接入（D-2026-09-06-01）
- 全市场在市日线包（5501 只，三套复权）迁入 `data/raw/user_dataset/2026-09-03/`；`quant/history.py` 新增 `UserDataset`（zip 按需读取、字段映射、停牌推断、isST 未知标记）与 `compare_daily_sources` 双源对账。
- 双源验证：52/52 只在市股与 baostock 全部共同交易日 OHLC/量额零不一致；数据源分工冻结——在市价格用本地数据集，退市股与逐日 ST/停牌状态由 baostock 补齐（`fetch-daily --delisted` / `--all` 断点续抓）。
- 池构建接入双源并加日期窗口内存过滤，全市场端到端跑通；严格模式 st_unknown 显式排除，allow_unknown_st 为披露性宽松口径。252 项单测通过。

## [v2.9.1] - 2026-09-06

### P2 撮合引擎与账本（D-2026-09-05-09）
- 新增 `quant/execution.py`：按时间顺序撮合（limit/stop 订单）、T+1 可卖、涨跌停方向不可成交、无量不成交、参与量上限、费用逐笔（含最低佣金分段）、公司行为开盘前入账、止盈止损歧义保守标注；订单激活严格晚于信号时刻。
- 修复 `market.board/is_etf` 对点号代码误判板块（baostock 口径科创板规则失效）；17 项手算预期单测，全量 248 项与月频 smoke 通过。
- 执行参数为草稿值（冻结前置条件写入配置）；真实分钟样本验证、事件案例回放与独立审查未完成，未接任何策略入口。

## [v2.9.0] - 2026-09-06

### P1 历史时点数据层与按日可选池（D-2026-09-05-08）
- 新增 `quant/history.py`：证券状态/未复权价格规范层、按日可选池（board→上市退市→no_data→no_row→ST→停牌→上市年限→流动性，逐只排除原因）、公司行为对账（preclose 跳变×分红明细双向）。
- 新增 `experiments/short_foundation_research.py`（fetch-daily 全市场断点续抓/连续失败熔断、pool、reconcile、check-units）与 `configs/short-foundation-research.json`（P1 冻结参数）。
- 实测：日线 volume=股（17.1 万行 VWAP 违例 0.016%）；停牌行仅 8.7% 为前收填充，规则收紧为不可成交；600519 全历史 28 跳变对账 26 笔精确匹配（转增字段 `dividReserveToStockPs` 须并入），unmatched 即 baostock 中期分红遗漏（新增 2022-12-27 案例）。
- 20 项 P1 单测固化验收失败模式；全量 231 项测试通过；50 只真实抽样选池示例与全市场后台补齐见 `docs/experiments/short-data-coverage.md` P1 部分。

### 短线底座 P0 数据覆盖探查：baostock 接入（D-2026-09-05-07）
- 新增 `experiments/short_data_coverage.py` 事件抽样探查（退市/ST/停牌/除权/板块/ETF/分钟深度），证据按批次保存且不覆盖；证券名单规范件入 `data/history/baostock/`。
- 实测：退市股日线完整（1999 起名单）、逐日 ST/停牌可用、未复权口径证实；5 分钟线仅 2020-01-03 起、无 1 分钟、无 ETF/北交所 K 线；分红明细有遗漏案例须与价格跳变对账。结论与缺口见 `docs/experiments/short-data-coverage.md`。
- baostock 设为股票侧历史研究数据源（免注册、无 SLA、仅研究用）；AGENTS.md 同步依赖与节律纪律。新增 8 项离线单测，全量 211 项单测与月频 smoke 通过。

### 三类周内入场固定对照（D-2026-09-05-06）
- 新增7组股票/ETF分开研究，固定回撤企稳、放量突破首次回踩、ETF超跌修复条件，保持五日退出与账户约束一致。
- 组合回放新增实际尝试订单与期末待成交数量，研究报告提供成交率、资金使用率和标的利润集中度；保存全部失败和成功结果，不替换默认策略。
- 203项单测及原月频端到端smoke通过。方法和证据见`docs/experiments/weekly-entry-research.md`。

### 周内交易标签研究（D-2026-09-05-05）
- 新增第五交易日收盘退出研究选项；用同一组合引擎生成限价交易标签，未成交与未成熟独立标注，模型收益幅度仅来自训练期。
- 修正逐笔标签与组合市场日历不一致的问题，保留修正前后证据。固定三方案对照，不更改默认策略。
- 199项离线测试与原月频端到端smoke通过，详见`docs/experiments/weekly-trade-research.md`。

### 年化10% / 夏普1.5研究与概率留档（D-2026-09-05-04）
- 新增年度滚动正则模型、标签隔离、日期等权概率指标及现金日夏普；固定参数对照未达目标，失败结果完整披露。
- 用户授权独立`forecast`研究输出；事前报告禁止覆盖，成熟评分拒绝晚于入场生成的报告，不自动修改账户或策略。
- 修复有官方上市公告证据的两只股票上市期合法大幅涨跌被整体排除；其他校验不放松。修正池重跑：原候选年化4.47%/夏普1.114，提高资金使用率8.12%/0.789（2020起同窗），仍不达标。
- 194项离线单测及新旧端到端smoke通过。详见`docs/experiments/short-target-research.md`。
### 短线盈利规则研究与用户费用校准（D-2026-09-05-03）
- 固定六项规则，先按2016—2021训练选择再评估后续历史；新增趋势内回撤信号、宽基趋势新开仓过滤及配置费用与账户覆盖的统一口径。
- 用户确认10万元、股票万2.5最低5元、ETF万1无最低；20%回撤用于历史筛选。校准选出`momentum_market`，全期+28.29%、最大回撤8.43%，2022—2023为+7.15%；费用加倍全期+14.50%。默认费用试验失败也完整保留，不混用两组结果。
- 新候选通过`configs/short-term-candidate.json`显式研究调用；市场条件不足暂停新买入、旧仓继续退出复评。仅建议，仍不可执行，独立审查与全新样本外验证未完成。
- 新增实验逐笔/行情/配置快照与`--replay-from`复现，保留全部六项训练结果、预先冻结选择及费用压力。证据见`docs/experiments/short-profit-research.md`。
- 验证：185项离线测试通过；原月频smoke与新候选650日/10笔平仓的合成回放通过。按2026-09-04缓存生成的新计划因宽基趋势偏弱暂停新开仓。

### 账户约束、人工做T续算与一周短线候选（D-2026-09-05-02）
- `t0-v2`：指定标的；完整最低佣金资金上限、过滤净亏损目标、区分当日可卖数量，零现金反T可用卖出所得买回；加入风险预算、板块数量规则、快照有效性和当日价格限制。
- `--account`支持现金、持仓、费率、风险预算、仓位上限及人工录入的`open_t`；首笔真实成交价跨日保留，只给建议，账户不被自动修改。
- 新增`short`：固定趋势回撤研究候选，输出下一交易日限价/数量/净盈亏；一周复评与条件延长；共享现金的日线组合回放、T+1、次日成交、止损优先。
- 新候选验证段收益为负，保持研究状态；V3规则与旧daily研究算法保留。详细证据与运行方法见`docs/short-term-usage.md`。
- 持仓测试改用临时文件，避免测试操作真实`data/positions.json`。
- 验证：178项离线单测、V3端到端smoke及新短线fixture组合回放通过；真实历史回放完成，收益验证失败如实披露。

### 日常入口回到 V3（D-2026-09-05-01）
- 日常主入口：`signal` / `plan` / `backtest`（V3 Top3 等权）+ 有底仓 `t0`。
- `daily`（daily-swing-v1）降为研究，命令保留。
- V2 Top1 退役，不再生新信号。
- 未改 V3 排名或门禁代码。附加门禁（RSI/波动/量能/距高点）待独立研究。

## [v2.8.0] - 2026-09-04

### 日常入口：`daily` 短线 Top3 + 底仓 `t0` 刷新（D-2026-09-04-06）
- **`python -m quant.cli daily`**：合并 ETF 池与个股池（按 `symbol` 去重），v1 只按 20 日流动性 + 费用覆盖倍数排序，最多 3 只，不足不补位。现价锚定 ATR 买入/短线卖出/风险离场区间；文案禁止会涨会跌与胜率。样本外验证前输出保持研究/不可执行。
- **`t0` 联网单标默认 300 秒刷新**腾讯 qt 并重算费用后保本区间；`--refresh-seconds` / `--once` / `--offline` 可改。无底仓、不足一手、或 `t0_max_shares=0`（现金不够一手）不给 T 对、不循环。
- daily 复用正式池日线门禁（`validate_bars` + 跳变原始价复核）；`VALIDATION_FAIL` / `CORPORATE_ACTION_GAP` 不得进入 selected。
- 参数 `configs/daily-swing.json`。V3 `backtest` / `signal` / `plan` 保留不改语义。
- 单元测试 **134** 项 + fixture smoke PASS。

## [v2.7.1] - 2026-09-04

### 做T 扫描模式：`t0` 不带 `--symbol` 即给"今天做T 什么"的建议
- **两种用法并存**（用户要求）：`t0 --cash N` 扫描模式 = 底仓（positions.json，真实可执行）+ 池内适配度 Top5（假设满资金建仓的参考），按**做T 适配度**（日内典型振幅 ÷ 费用后最小可行价差）排序，判定档位 适合(≥2)/勉强(1~2)/不可行(<1)/无底仓；`t0 --symbol X` 保持单标深度量化（价位阶梯）。
- 扫描用腾讯 qt **批量快照**一次取全池实时价；适配度口径为波动/费用比值筛选，非涨跌预测（输出显式标注）。
- 测试 101 → **105 项**（适配度阈值档位、无底仓一手参考口径、半幅净利、假设建仓整手/资金约束）。
- 实测（10 万资金、默认费率）：池内 Top5 = 科创半导体 19.4 / 黄金股 18.4 / 有色 14.3 / 消费电子 14.2 / 稀有金属 14.1。

## [v2.7.0] - 2026-09-04

### 新增：做T 价位辅助 `t0`（个股/ETF 通用，D-2026-09-04-05）
- **`quant/t0.py` + CLI `t0`**：实时行情（腾讯 qt 快照，含行情时点）+ 底仓（positions.json 或 `--shares`）+ 资金量（`--cash`）+ 费用结构（配置默认 + `--commission-bps/--min-commission/--stamp-sell-bps/--slippage-bps` 覆盖）：
  - **费用后保本价**：双边佣金（含最低佣金分段，二分求解天然覆盖非线性）、卖出印花税（个股 5bps/ETF 免，自动按代码区分）、滑点；价格按最小变动单位取整（ETF 0.001/个股 0.01）；
  - **正T/反T 价位阶梯**：以实时价为锚、按近 20 日典型日内振幅（(high-low)/prev_close 中位数）给三档低吸/高抛价，各档给保本离场价、目标价与净利；
  - **最小可行价差**：一手与满额规模各算一份——量化最低佣金的"死亡区间"（如 5 元最低佣金下一手 10 元面额需 2.3% 价差，满额 6400 股仅需 0.22%），超过典型振幅一半时显式告警；
  - **走势情景带**：±1σ/±2σ × 1/5 日波动率区间 + MA20/60/120 趋势状态——显式标注为波动率度量**非预测**（与 TimesFM/Kronos 实验结论一致的口径纪律）；
  - **约束**：无底仓拒绝给做T 买卖对（T+1）、资金不足一手拦截、产物落 `artifacts/t0/<symbol>/`。
- `parse_qt_snapshot` 增 `quote_time` 字段（做T 建议标注行情时点）；`t0` 无本地日线时自动经腾讯补抓。
- 单元测试 90 → **101 项**（保本价零费/比例费公式、最低佣金分段、印花税个股 vs ETF、手数/整手/资金约束、tick 取整、振幅/波动/情景带对称性、趋势状态）。
- AGENTS.md（v2.6.1）预声明的做T 边界全部落实：费用后保本口径、底仓约束、禁止预测表述。

## [v2.6.0] - 2026-09-04

### V3 生产切换 + akshare 数据源 + 长历史证据（D-2026-09-04-04）

> **状态说明**：关键路径已完成（代码+测试+长历史证据+首条生产信号）；东财探针余 2 只标的与 ak 缓存铺设待接口恢复（见下方「仍待东财恢复后完成」与「恢复清单」）。

### 已完成（代码与测试全绿）
- **生产切换 V3**（用户决定，提前于 D-2026-09-04-03 预登记的 6–12 个月前向门槛）：`configs/strategy.json` → `strategy_version v3.0.0`、新增 `portfolio.top_n=3`；`quant/signal.py` 重写为 Top-N 等权组合信号（各持仓独立 ATR 区间 + alternates）；`quant/plan.py` 重写为多持仓计划（目标集合=组合，跌出卖/新进买/持有不动，预算口径与回测引擎对齐，逐 lot 止损保留）；`cmd_backtest` 接 V3 引擎（`portfolio_candidate.run_rank_portfolio`）；`run_rank_portfolio` 补齐生产消费所需字段（equity 行 `symbol/shares`、sh510300 基准）；`backtest.save_run` equity.csv 增 `holdings` 列；`cmd_smoke` 断言 V3 不变量（持仓 ≤Top-N、权重和=1、台账守恒、T+1）；`scripts/show_signal.py` 渲染组合；`fetch-all` 增 `--only-missing`（断点续抓）。**单元测试 90 项通过 + 离线 smoke PASS**。
- **forward_shadow 降级为只记 V3**：`experiments/forward_shadow.py` 记录去 V2 字段；结算逻辑兼容旧记录（含 V2 字段的历史记录只追加不覆盖、按原口径结算）。
- **akshare(东财) 数据源接入**（用户选择全面替代同花顺，ths 代码保留为备用）：`quant/data.py` 新增 `parse_ak_records`（纯函数，列序陷阱：东财返回 日期/开盘/收盘/最高/最低）与 `fetch_ak_daily`（惰性 import akshare，ETF/个股双端点，12s 节律 + 90s 冷却重试）；manifest 增 `ak` 分支；`cli` fetch-all/analyze 增 ak 分支；新浪复核深度 2600→6000（匹配全历史窗口，sh510050 已覆盖至 2005-02-23）；新增 `tests/test_data_ak.py`（3 项）。
- **依赖变化**：akshare 1.18.94（引入 pandas 依赖栈；项目原零第三方依赖，`data.py` 惰性 import 保持离线测试不依赖）。
- **探针脚本**：`experiments/ak_probe.py`（量纲 VWAP 验证 + vs 同花顺近 5 年 + vs 腾讯全历史双源交叉 + >21% 跳变新浪复核，预登记门槛，逐标的增量落盘）。**sh510300 完整通过**：3471 天 VWAP 违例 0；vs ths 1208 天 median |Δ日收益|=0.0、>2% 占比 0；vs tx 3469 天 median |Δ|=0.0。sh588170/sz159985 待补。
- **README** 已更新（V3 策略描述、akshare 数据源行、已知局限去掉 5 年上限、命令示例）。
- **数据集体检**（副产物）：`experiments/baidu_dataset_check.py` 对用户百度网盘数据集（上证/深证 × 前/后/不复权）结论——**不含退市股**（6 个退市抽查代码全缺席，5501 只全终止于 2026 年），仍是当前名单回放；qfq/hfq 日收益中位数差 0.0；**000001 平安银行深历史存在单日 qfq/hfq 差 21.8% 的异常**（未解释，作数据质量警示）；资产按研究用途挂起，不接生产。证据 `artifacts/experiments/baidu-dataset-check.json`。
- 首次 V3 信号链路验证（ths 缓存驱动，`artifacts/signals/2026-09-02/etf_rotation_v3.0.0.json`）：Top3 输出与 9/4 前向记录一致（豆粕/银行/黄金股系），数据源字段标 ths，仅作管线验证产物。

### 网络环境实测（记录备查）
- 东财 `push2his.eastmoney.com`：同 IP 连续请求后断连，封禁窗口分钟级且随持续打点拉长；代理链路（Clash 127.0.0.1:7897 → 东财）故障不可用；直连时好时坏；`33./64.push2his` 编号镜像共享封禁桶；`push2delay.eastmoney.com` 可达但 kline 返回空。**恢复策略：间隔较长时间（≥数小时）后重跑，逐标的增量续传**。
- Windows 注意：requests 除环境变量外读注册表系统代理（清 env 无效），须用 `NO_PROXY` 域名后缀排除（`"*"` 不生效）。

### 未完成（阻塞于东财断连）
东财 push2his 对本网络全路径断连（直连/代理/编号镜像均 000，持续数小时）。**传输通道临时降级为腾讯**（`configs/_tmp_tx.json`，仅 data_source=tx 不同）：依据探针已验证 tx 与 ak 在 510300 全历史 3469 天逐日收益差中位数 0.0、且 tx 于 v2.4.1 与同花顺交叉验证偏差 0.0000%——数据等价，只是传输通道不同；信号/前向记录的 `data_source` 字段均如实标注 tx。**待 EM 恢复后删除临时配置**。

### 已用腾讯通道完成（2026-09-04 晚）
- **全历史数据**：55/55 全部抓取成功（`fetch-all --only-missing`，tx 深度提升 2600→6000）；sh510050 覆盖 2005-02-23 起 21.5 年；sh510050/sh510880 各删除 150/72 条前复权负价非法行（manifest 披露，按停牌缺口处理）。
- **长历史回测实验**（`experiments/strategy_v3_long_history.py tx`，证据 `docs/experiments/strategy-v3-long-history.json`；全窗口 2005-02-23~2026-09-04 共 5107 交易日，合格池 ≥10 只自 2020-07-31 起）：

| 口径 | 年化 | 波动 | MDD | Sharpe |
|---|---|---|---|---|
| V3 全历史 | +11.0% | 28.1% | **−68.2%** | 0.39 |
| V3 adequate（2020-07 起） | +8.3% | 32.5% | **−55.4%** | 0.26 |
| V2 全历史 | +6.7% | 46.8% | −84.8% | — |
| V2 adequate | +2.7% | 43.6% | −78.8% | 0.06 |

  结论：**V3 在所有窗口、所有指标上优于 V2**——D-03 的结构性归因（分散化降低回撤/波动）在 5 年窗口外成立；同时**修正预期**：5 年窗口的 MDD −34.7% 是幸运窗口（避开 2021H1 核心资产崩塌），长样本显示可预期回撤量级为 −55%~−68%；年化 8~11%（含幸存者偏差偏袒）为量级参考而非预期收益。
- **首条正式 V3 生产信号**（`artifacts/signals/2026-09-04/etf_rotation_v3.0.0.json`，data_as_of=2026-09-04，data_source=tx，12 只合格）：Top3 = sz159985 豆粕 / sh512800 银行 / sz159992 创新药（各 1/3），候补 4~8 = 黄金股/医药/红利/养殖/医疗。
- **forward_shadow V3 生产基线**已记录（第 2 条，data_as_of=2026-09-04）。

### 仍待东财恢复后完成
1. 探针补测 sh588170/sz159985 → `docs/data-source-ak-probe.md` 正式门槛结论；
2. `fetch-all`（ak 源）铺 `data/processed/ak_*` 缓存后删除 `configs/_tmp_tx.json`，生产回到 strategy.json 默认路径；
3. 用 ak 数据复核一次信号与长历史实验（应与 tx 完全一致）。

**恢复清单**（按序）：
```bash
python -u experiments/ak_probe.py            # 断连解除后；通过后再继续
python -m quant.cli --config configs/strategy.json fetch-all --only-missing
python -m quant.cli --config configs/strategy.json signal --top 8 && python scripts/show_signal.py
rm configs/_tmp_tx.json
python -m unittest discover -s tests && python -m quant.cli --config configs/test-fixture.json smoke --fixture tests/fixtures/rotation/pool.csv
```

## [v2.5.0] - 2026-09-04

### 收编 V3 优化候选包 + V2/V3 并行前向记录（D-2026-09-04-03）
- **独立复核通过后收编**外部候选包：修复 `plan.py` 永不触发的止损（改为持仓以来最高收盘价 −2×ATR 跟踪止损）、`buy_max_lots` 重复滑点、流动性门禁优先显式 `amount`；新增 `quant/portfolio_candidate.py`（V3 候选）与 `experiments/strategy_v3_candidate.py`；测试 79→83 项。
- **独立消融归因**：收益改善全部来自 Top-N 分散化（MDD −52%→−35% 在 N=2~5 稳健）；+71% 为 Top3 尖峰（Top2 +63%/Top4 +28%/Top5 +19%），有选择偏差；百分位排名变换本身无贡献（与原始收益合成结果完全相同）。**V3 状态 CANDIDATE_NOT_PRODUCTION，生产维持 V2**。
- 新增 `experiments/forward_shadow.py`：V2/V3 每月并行纸面信号 + 前向自动结算（20 交易日 close-to-close），`artifacts/forward_log/forward.jsonl` 只追加。首次记录：V2=sh588170；V3=sz159985/sh512800/sh517520。
- 文档口径修正：回测不含日频止损（原「止损为回测纪律」表述不实）；止损属持仓执行层。

## [v2.4.1] - 2026-09-04

- `analyze` 修复：ETF 在 tx 配置下误落新浪未复权源（存在份额折算断层），统一走腾讯前复权（与同花顺 250 日收盘 0.0000% 偏差交叉验证）；分析面板增加 >21% 跳变检测披露。
- 新增 `experiments/kronos_analyze.py`：Kronos 单标的一次性推理（非信号；601179 三种子一致看空 −24%~−27%/20 交易日，未验证模型仅供参考）。

## [v2.4.1-opt] - 2026-09-04

### 优化候选包（不直接替换生产策略）
- 新增 `quant/portfolio_candidate.py` 与 `experiments/strategy_v3_candidate.py`：V3 候选采用 R20/R60/R120 横截面百分位排名等权平均 + Top3 等权月频轮动。
- 当前本地 5 年样本：V2 +22.9% / MDD -51.9%；V3 候选约 +71.0% / MDD -34.7%。由于历史样本已多次查看，V3 状态为 `CANDIDATE_NOT_PRODUCTION`，需前向跟踪。
- 修复 `plan.py` 不可能触发的动态止损：实际持仓改为“持仓以来最高收盘价 - K*ATR”跟踪止损。
- 修复 `buy_max_lots()` 重复叠加滑点的仓位估算问题。
- 清洗层可保留显式 `amount` 字段；流动性门禁优先使用成交额，旧 6 列缓存兼容。
- 新增 4 项回归测试，总测试 79 项通过。

## [v2.4.0] - 2026-09-04

### 新增：个股研究线（数据面扩展 + 因子库 + 标的分析，D-2026-09-04-02）
- **数据源**（全部免 key，`docs/data-source-stock-probe.md`）：腾讯前复权日线（个股全历史，回溯分页）、腾讯 qt 批量快照（PE-TTM/PB/市值/换手）、新浪 A 股全列表 + 行业分类 + 未复权复核价、东财 F10 季报主要指标（ROE/毛利率/增速/负债率，最近 9 期）。同花顺仍为 ETF 日线 primary。
- **个股池**：`scripts/build_stock_pool.py` → 120 只高流动性个股（全市场门禁 → 当日成交额短名单 → 腾讯日线 20 日中位成交额复核 → 行业分散约束）。规则不使用任何回测收益。
- **因子库**：`quant/factors.py` 新增 RSI14、MACD、20 日年化波动、量能比(5/20)、距 252 日高点、均线偏离；新增 `quant/fundamentals.py`（EP/BP/规模/ROE/增速/毛利率/负债率；报告期 Q+90 天可见规则防未来函数）。
- **标的分析 `analyze`**：任意个股/ETF 的基本面+技术面分析面板与买卖推荐价——买入区间、止损触发价/硬底线、止盈参考带（ATR 波动率带），个股含池内 PE/PB/ROE 分位与风险标记；输出 JSON 存 `artifacts/analysis/<symbol>/` 并打印可读摘要。
- **个股轮动对照回测**：回测引擎支持卖出印花税（`stamp_sell_bps`，个股 5bps/ETF 0）；2014-2026 对照回测产物入库。
- **因子 IC 实验**：`experiments/factor_ic.py`，115 个月末截面 × 15 个预登记因子（`docs/experiments/factor-ic-stocks.json`）。
- **结论**（D-2026-09-04-02）：个股池动量 IC≈0（−0.017），技术面扩展因子与财务因子 IC 无一显著为正；个股动量轮动回测波动 73.7%/回撤 −78.5%——**生产策略维持 ETF 月频动量轮动不变**，个股线产出为分析辅助与研究证据。
- **工程修复**：腾讯 volume 量纲按板块区分（科创板=股、其余=手，错配会虚增百倍）；web.ifzq 带日期 URL 的 WAF 501（改 proxy 域名）；接口限流节律控制与连续失败熔断；F10 偶发 gzip 透明解压；前复权负价伪影行删除 + manifest 披露；清洗层 dataset_id 稳定化（原地更新 + content_changed 标记）。
- 单元测试 34 → 75 项；`fetch-fund`、`analyze` CLI 子命令新增。

## [v2.3.1] - 2026-09-04

- Kronos 实验终止（用户决定，D-2026-09-04-01）：探针通过、walk-forward 仅 2/44 月末即中断，不作结论，实验代码归档入库。

## [v2.3.0] - 2026-09-03

### 实验分支：TimesFM 3.0 对照评估（结论：不接入）
- 新增 `experiments/timesfm_probe.py`：GPU 推理探针（timesfm 3.0.1 新 API `TimesFM3Forecaster`；torch 2.11.0+cu128；权重缓存 `D:\AI\model\hf`）。实测：55 只 × 512 日批量 20 步推理约 1.2 秒，峰值显存 1.4 GB，Windows + RTX 4060 可用。
- 新增 `experiments/timesfm_rank.py`：月度 walk-forward 对照实验——同一合格集上动量因子 vs TimesFM q50/P10 预测收益的排名能力（Spearman IC、Top1 前向收益、简化轮动净值），严格只用 ≤t 数据，不调参。
- **结果**（生产 55 池，44 个有效月末）：动量简化净值 1.953 > TimesFM P10 1.548 > TimesFM q50 0.891。预注册主方案（q50）失败；P10 为事后观察，按反数据窥探纪律不据此改策略。
- **结论**：生产选基排名维持月频动量因子。证据入库：`docs/experiments/timesfm-rank-q50.json`、`timesfm-rank-p10.json`。详见 `docs/decisions.md` D-2026-09-03-08。
- **补充消融**（回应「是不是信息给少了」）：`experiments/timesfm_ablation.py` 在同一任务集对比 6 种输入——**对数收益表示是唯一实质改善**（净值 0.891→2.034，与动量 1.953 打平）；znorm 无变化、全历史/加量/横截面联合均无益或有害。维持不接入。副产品假设：收益表示版 TimesFM 与动量 Top1 重合仅 39%，可作独立第二意见（待样本外验证）。证据 `docs/experiments/timesfm-ablation.json`，详见 D-2026-09-03-09。

## [v2.2.0] - 2026-09-03

### 新增：实际持仓跟踪与执行计划（闭环 item 1）
- `quant/positions.py`：人工成交录入（buy/sell/set/clear），整手与价格校验、加权成本、卖出不改成本、清仓自动移除；状态存 `data/positions.json`（不入库）。
- `quant/plan.py`：持仓 vs 最新推荐 → 换仓计划（BUY/HOLD/SWITCH/MOVE_TO_CASH/STAY_CASH，含估算卖出所得与可买整手股数）+ 每只持仓的止损/止盈检查（`STOP_ALERT` / `TAKE_PROFIT` / `OK`，基于自身 ATR 区间）。
- CLI 新增 `position`（show/buy/sell/set/clear）与 `plan`（--capital 指定可用资金）子命令；新增 5 项持仓单元测试（总数 34）。

### 新增：Git 版本控制（闭环 item 10）
- 初始化仓库并完成首次提交（代码/配置/测试/文档；`data/`、`artifacts/`、`logs/`、凭据文件经 .gitignore 排除）。

## [v2.1.2] - 2026-09-03

### 新增
- `signal --top N`：候选名单数量可调（默认 3）。备选标的从「仅代码+动量分」升级为完整信息：收盘价、R20/R60/R120、买入区间、止损带、止盈带、ATR20。
- `scripts/show_signal.py`：信号可读摘要工具——表格化 Top N、数据截止日与风险状态、被排除标的提示、四条操作规则（买入/换仓/止损/止盈）、免责声明与信号文件路径。供桌面脚本与日常查看调用。
- 桌面快捷脚本 `ETF推荐.bat`（Windows 桌面，仓库外）：双击完成「更新行情 → 生成 Top 8 信号 → 打印摘要」；首次运行引导将 `THS_API_KEY` 存入本机环境变量（key 不落项目文件）。

### 决策记录
- exe 打包（命令行版 / 带界面版）完成技术评估：可行，需处理冻结路径与杀软误报；带界面版（tkinter，含止损盯盘页与回测页）为推荐方向。**用户决定暂不实施**，备选方案（Windows 任务计划自动运行）一并记录。详见 `docs/decisions.md` D-2026-09-03-07。

## [v2.1.1] - 2026-09-03

### 信号语义重构
- 原 `sell_zone`（现价下方 2~3 ATR）实为止损带，更名 **`stop_zone`（止损带）**：收盘跌破上沿（close − 2×ATR20）→ 次日开盘离场；下沿（close − 3×ATR20）为硬底线。
- 新增 **`take_profit_zone`（止盈参考带）**：[120 日最高收盘, 120 日最高收盘 + 1×ATR20]。收盘进入该带 = 价格到达阶段高位，可分批止盈或将止损上移至 close − 1×ATR。**止盈带未纳入回测纪律**（回测离场仍是止损带），作为参考项在信号中披露。
- 信号 JSON 新增 `action_hints`（买入/换仓/止损/止盈的操作规则，消除语义歧义）。

## [v2.1.0] - 2026-09-03

### 数据源升级
- 接入**同花顺数据接口**为 primary（`X-api-key` 鉴权，key 只走环境变量 `THS_API_KEY`）：前复权 ETF 日线（锚定最新价）+ 成交额，近 5 年窗口。
- 新浪 K 线降为 secondary：未复权原始价，用于**除息伪影交叉复核**与降级备选。
- 探针证据：`docs/data-source-ths-probe.md`（新浪 8/16 只 ETF 存在份额折算/拆分断层，同花顺 0 断层）。

### ETF 池 16 → 55 只
- 新增 `scripts/build_pool.py`：全市场 ETF → 剔跨境 QDII（含 513 段）/债券/货币/北交所 → 名称分主题组 → 组内按 20 日中位成交额取第一名。**选择规则不使用任何回测收益**。
- 按用户要求只覆盖 A 股（保留黄金、豆粕两只国内商品 ETF）。

### 回测引擎
- 日期交集 → **日期并集日历**：各标的按自身序列计算因子，新上市 ETF 从历史达标之日进入候选；持仓停牌日用最后已知收盘估值。
- 成交记录新增 `EXIT_BLOCKED` / `BUY_BLOCKED` 审计（停牌/买不起整手）。
- 跳变门禁阈值 20% → 21%（区分涨跌停+复权漂移与份额折算）；除息伪影经新浪原始价自动复核后保留并披露（159915、159949）。
- 样本下限因 5 年数据上限显式下调：1260 → 1000 总样本、252 → 240 分段，配置内披露。

## [v2.0.0] - 2026-09-03

- 策略从单 ETF SMA20/60 基线改为**月频动量轮动**：月末按 `0.5·R20 + 0.3·R60 + 0.2·R120` 排名选 1 只 ETF，否则现金；门槛 `close > SMA120` 且 `R60 > 0`、上市满 252 日、流动性达标；next-open 先卖后买、整手、显式成本。
- 新增 ATR 波动率买卖区间输出；信号含推荐标的、区间、备选、元数据与免责声明。

## [v1.0.0] - 2026-09-02

- 项目重建（废弃旧 V1/V2 规划文档）：单 ETF SMA 趋势基线闭环。
- 建立数据分层（raw 只追加 + SHA-256 / processed + manifest）、数据门禁（OHLC 不变量、日期唯一递增）、离线 fixture 测试、端到端 smoke。
- 发现新浪未复权数据的折算/拆分价格断层并建立跳变检测门禁。

## 未实施 / 已评估

| 事项 | 评估结论 | 状态 |
|---|---|---|
| TimesFM 3.0 接入选基排名 | q50 净值 0.891 < 动量 1.953（44 个月末）；P10 事后观察不作数 | 已否决（D-08），代码留 `experiments/` |
| TimesFM 信息量消融（6 变体） | 收益表示净值 2.034 与动量打平；更多信息（历史/量/横截面）无益 | 已否决接入（D-09）；「第二意见」假设待样本外验证 |
| TimesFM 分位带宽作风险信号 | q10/q90 差作仓位调节——仅假设，未验证 | 开放问题，未实施 |
| exe 打包（CLI 版） | PyInstaller 可行；需冻结路径适配；杀软误报需加白 | 暂缓（用户决定） |
| exe 打包（GUI 版） | tkinter + PyInstaller 可行；止损盯盘页 + 回测页 + 可选K线图 | 暂缓（用户决定） |
| Windows 任务计划自动运行 | 与 bat/exe 不冲突，月末自动跑 + 弹窗提醒 | 备选，未实施 |
| 长历史回测（新浪全历史） | 需获取分红/折算记录自建复权因子，工作量较大 | 备选，未实施 |
