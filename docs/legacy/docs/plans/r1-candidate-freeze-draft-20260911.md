# R1 候选冻结清单与 R0 后路径工程草案（r1-candidate-freeze-draft-20260911）

- 状态：**工程草案，未冻结、未生效、不执行任何研究**。不含任何未经授权的预算数字与统计阈值（涉统计口径处一律标注"待经审统计处置路径冻结"或"待裁决"）；按 handover §3.4/§5.1，冻结清单与预登记**须用户确认后方可生效**。
- 依据：docs/handover-codex-20260911.md §3.4（R1 冻结清单+预登记草案待办）、docs/plans/factor-mining-program-20260909.md（纲领三规与流程）、factor-miner-engine-preregistration-draft/amendment-20260910（冻结引擎协议）、factor-miner-blueprint-reference-20260910（历史蓝本）、artifacts/system-audit-20260911/candidate-asset-map/REVIEW.md（2026-09-11 更正版，含 §4 公式交集新证据）、artifacts/experiment-index-20260911/dead_zones.jsonl（EX-D 输入）。
- 起草：Arendt（GLM-5.3/high）；审核：主审；生效：主审审后提请用户确认。本草案零代码改动、零评分运行、零新挖因子、未触 VALIDATION/LOCKBOX。
- 修订（2026-09-11 夜，主审定点意见）：统计前置去 v4.1 唯一化、计数纪律、证据表述降格至实测覆盖、纯元数据冻结准备解卡、用户归属注明权限来源；留痕见 artifacts/system-audit-20260911/plan-consistency-final/REVIEW.md。

## 1. 输入与实测证据（2026-09-11 20:56:36，candidate-asset-map §4）

- **R1 池**：215 行 / 17 研究员文件 / 17 家族（LIQ13、VAL_PX14、FLOW11、STATE15、PATH_EFF13、INFO_DIFF12、VOL_EST14、NOMINAL11、DIST_SHAPE12、BETA_SHAPE12、ACCDIST12、UVOL14、TURN_DYN13、QUAL_RATIO13、LEADLAG12、INTRADAY_MICRO12、COST_EVENT12）；15 个 schema 字段键存在计数全行覆盖、仅 formula 单独检查非空（零缺失）；其他字段类型与语义合法性尚未验证。
- **R1 内部去重输入（仅字符串归一化口径）**：归一化（去空白+小写）唯一公式 213；内部完全重复 2 组共 4 行（合并吸收候选）。
- **R0 参照（仅字符串归一化口径）**：250 行 / 4 家族（META77、REL59、REV74、STATE40）；归一化唯一 247、重复 2 组 5 行。
- **计数纪律（主审 2026-09-11 意见）**：215/213、250/247 仅为行数与字符串归一化统计，只作 lineage 线索；不得替代编译器结构去重（R0=246）或统计评估单元数（248，stop-rules §1），两类口径不相加、不互替、不当独立搜索数。
- **R1×R0 精确归一化公式交集＝0**（组/行均 0）；家族层交集＝STATE（15 vs 40）。语义/结构层重叠（残差化/条件化/交互变体）与财务家族（⑤/F6+ 31 构造）lineage 交集**未做**——均属待核。
- **EX-D 输入**：artifacts/experiment-index-20260911/dead_zones.jsonl 实测 20 个 sealed zones（含 identity_keys/scope/reopen_conditions）；handover §3.4 要求先移受控位置并登记。
- 工具工件：candidate-asset-map/r1_r0_formula_overlap.py + _output.txt（exit 0，纯元数据）。

## 2. 冻结流程工程步骤（仅流程；所有授权决策见 §3）

1. **元数据完备性校验**：15 字段键存在计数＋仅 formula 非空校验（已实测）；其他字段类型/语义合法性尚未验证——parent_factor_ids 等允许空列表的字段须依 schema 判定（非机械非空），冻结清单按 schema 语义口径校验；本条为措辞更正，不新增全量测试；冻结时对最终身份重跑并把两轮结果记入清单头。
2. **去重裁决**：归一化公式完全重复 → 合并吸收并记录 lineage 映射（保留最早 id、其余标 absorbed_by）；近似重复（仅窗口/参数微调）按纲领"禁止参数凑格子"处理——吸收、拒绝或要求增量机制说明，规则待裁决（D-1）。
3. **EX-D 机器比对**：dead_zones.jsonl 迁移至受控位置（D-2：configs/ 或 docs/data/）并在冻结清单登记源路径与哈希；比对键（zone identity_keys 语义 vs 归一化公式 vs 家族×构造名）与命中处置（直接拒绝 / 标注不评 / 裁决豁免）待裁决（D-3）。初步观察（非裁决）：R1 家族名与 20 zones 的家族域（事件/MR/P4 等）表面不同名，但 COST_EVENT/QUAL_RATIO 等与事件、财务域的**语义邻接**存在，EX-D 必须机器比对不得人工跳过。
4. **canonical lineage 交集**：本轮完成公式归一化精确层（R1×R0=0）；待做＝①R1×R0 语义/结构层（同字段近邻、算子骨架相同仅常数差异）；②R1×财务 31 构造层（VAL_PX/QUAL_RATIO 等价值/质量族与 F1–F31 的公式对应）；度量与命中处置待裁决（D-4）。
5. **冻结清单输出**：逐行 id/name/family/formula/expected_direction/holding_days/factor_type/来源文件/内部去重映射/EX-D 判定/R0 与财务重叠标记/parent_factor_ids；机器可读 jsonl＋人审 md 双件；是否按 W3/W4 波次分卷待裁决（D-5）。
6. **邻域稳健性冻结矩阵条款**：按 handover §3.4 并入清单附件；矩阵维度（窗口/池/条件邻域）与判据待裁决（D-6；判据涉统计口径，须等经审统计处置路径冻结（不预设 v4.1 为唯一路径），不在本草案私定）。

## 3. 冻结前需裁决事项（逐项拍板；本草案不给答案）

| # | 事项 | 决策者 | 依赖 |
|---|---|---|---|
| D-1 | 去重吸收规则与近似重复度量（完全重复吸收已可执行；近似层规则待定） | 主审 | 无 |
| D-2 | dead_zones.jsonl 受控位置与登记格式 | 主审 | 无 |
| D-3 | EX-D 比对键与命中处置 | 主审 | D-2 |
| D-4 | R1×R0 语义层与 R1×财务 lineage 交集的度量与处置 | 主审 | 财务 lineage 待核（asset-map §1C） |
| D-5 | 冻结清单格式/分卷 | 主审 | 无 |
| D-6 | 邻域稳健性矩阵条款内容 | 主审→用户（若涉口径） | 统计口径经审冻结（不预设唯一路径） |
| D-7 | R1 评估预算（家族上限/总量） | **用户**（新研究轮次立项保留决策） | 本草案不提数 |
| D-8 | R1 评估统计口径 | 用户+主审 | 沿用冻结协议；经审统计处置路径未生效前不评估 |
| D-9 | R1 冻结与 R0-4 的时序（建议：冻结可先行、评估必须在统计门禁冻结与 R0-4 完成后；非授权） | 主审 | R0-4 状态 |
| D-10 | R1 Diagnosis 输入格式（失败摘要而非全量）与每家族下一代候选数上限 | 主审→用户（若涉预算） | R0-4 结果 |
| D-11 | R2 扩矿触发判据（"一致微弱信号"定义）与预算倍率 | **用户** | 蓝本仅建议 ×2–3，未冻结 |
| D-12 | 统考协议预登记（判据/多重检验/失败处置） | 用户+主审 | 经审统计处置路径生效＋队列成员身份核验 |
注：用户归属事项（D-7/D-8/D-11/D-12）的权限来源为已生效 stop-rules（§1.3 R1 预登记须用户确认、§1.4 每轮单独预登记+用户确认、§5.4 统计协议变更须用户单独授权），非本草案自设审批。

## 4. R0 后路径工程（记录既定与缺口；不授权任何执行）

- **R1 Diagnosis**：蓝本＝TRAIN 失败摘要驱动（研究者不见全量结果/排名）、每家族最多 3–5 下一代候选、无增量家族预算减半；已冻结＝无；缺口＝输入格式、预算规则（D-10/D-11）。
- **R2 扩矿**：蓝本＝多构造一致微弱信号家族预算 ×2–3；已冻结＝无；缺口＝触发判据（D-11）。
- **稳健性**：邻域矩阵条款（D-6，并入冻结清单附件）。
- **统考（攒批留出窗）**：已授权＝队列构成（PV1＋财务 7 构造＋R0 幸存者，D-33；财务 7 只为历史报告资格，复用按身份核验）；未冻结＝协议本体（D-12）。
- **共同前置与执行边界（主审 2026-09-11 定点修订）**：实际评估/评分与留出接触的前置＝R0-4 正式 TRAIN 完成＋工件验收（当前完整正式结果缺失、晋级数未确定——asset-map 更正口径）＋经审有效的统计处置路径冻结——v4.1 为已知候选、不排他（stop-rules §5.1：v4.1 生效不是唯一 R0 通道），工程/描述性处置不得产生科学晋级。纯元数据冻结准备（§2 各步）按既有工程授权允许先行（本草案 §5「候选仅限元数据处理」），不受此二前置卡阻。

## 5. 纪律

- 未冻结不执行研究：本草案生效前不运行任何评估/评分/MC/正式批；候选仅限元数据处理。
- 不触 VALIDATION/LOCKBOX；不改冻结协议与生产代码；批次目录只新增不覆盖。
- 本草案任何内容不构成 PASS、放行或运行授权；与上位冻结文件冲突时以上位为准。

