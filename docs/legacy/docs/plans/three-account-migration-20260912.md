# 三账户统一2023–2024迁移方案

> 2026-09-12新交接：当前执行按新主计划P0/P1：`docs/plans/strategy-to-backend-20260912.md`。旧批1–3全部数据中止；用户指定baostock分钟主源，小幅汇总差异改为研究披露，代码接线未完、新批未启动。研究模式不得清除数据不确定性或放行生产。

最新状态（覆盖下方历史准备记录）：主审已依据用户攒批指令限定放行，见`docs/experiments/three-account-migration-prerun-review-20260912.md`。25项迁移定向测试通过；配置授权true，先导出三账户信号并审核，再进入市场输入/分钟账户。2025以后和生产继续冻结。

信号阶段已完成：`artifacts/three-account-migration-signals-20260912-1`全5218只；独立公式及输出复核覆盖68182个合格股票月份、72条账户信号，最大公式差2.7755575615628914e-17，缺值/完整截面排名/权重/执行日期一致。主审接受限定数值PASS，进入共同潜在持仓的市场输入准备；不是P1/供应商独立认证、收益结论或Alpha PASS。真实2023–2024信号已观察，后续新候选不得称此段未见。

共同市场输入第一批已完成（`artifacts/three-account-migration-market-inputs-20260912-1`）：420只潜在持仓，逐只输入哈希/日期顺序/截止日与请求并集核对通过。15个公司行为为NO_UNIQUE_PRICE_MATCH，对应15个未解决原始前收跳变，原始条款/对价证据保留。主审接受进入依赖受限的账户回放以识别实际持仓/订单影响，不接受未解决事件为零损益；受影响账户标DATA_ISSUE，不能报告有效收益。没有因这些问题删股或改规则。运行目录`artifacts/three-account-migration-accounts-20260912-1`。

2026-09-12，配置`configs/research/three-account-migration-20260912.json`。用户已要求其他因子验证完成后一起解锁，本轮有限TRAIN接续已完成并由主审接受，原REV11单独提前迁移申请不再采用。当前状态是三账户运行前准备，尚未读取窗口行情；按用户攒批要求统一推进，不把自动续作当作扩大窗口或参数搜索授权。

## 固定账户与问题

REV_vola_11原式正向、低换手对照原式不变，新增已完成TRAIN分钟复核的PV1/REV11固定50%组合。三账户各自20万元，Top10各9.5%、不足不补位，各自共享现金/持仓；不相加收益、不将两策略择优切换为第四个策略。

组合PV1严格为自然月隔夜收益和，至少10个有效正价格对，上一市场会话收盘可来自上月。分别在原P1有效截面做平均并列排名，百分位(rank−1)/(n−1)，然后取两者共同股票各50%，不在共同样本重新排名。缺任一项不补值、不重新分配缺位权重。名称沿TRAIN固定结构身份，窗口不同不新造原式。

## 24月与读取边界

账户收益仅2023-01-01至2024-12-31。2022年最后市场会话收盘生成首信号、2023首会话执行；末次可执行信号为2024年11月末，12月末按持仓市值结账，不读2025、不强平。预热覆盖2022年12月PV1首日的上一市场会话以及REV11窗口和完整P1历史上市年龄。预热不计研究收益。

股票超集须按原始上市/退市元数据覆盖这一窗口，不能照搬TRAIN5552或只取今天在市者。P1按当时上市龄、历史ST/停牌/流动性判定，不使用今日行业快照。本步骤先核现有元数据和本地文件覆盖，再锁定具体名单/输入身份；不能因为曾见过2023–2024而放松PIT。

## 执行和报告

同一现有5分钟执行路径、opening_cash_only、下一会话GFD原参考限价、0.5%逐bar共享容量、真实板块数量/限幅及T+1、历史状态、公司行为持仓权益/卖出限制。源数据缺口依实际依赖分类，不删股；差异化分派须分别核实际权益与参考价，无公告证据不放行。

佣金2.5bps最低5元、过户费0.1bps、执行摩擦每侧5bps；卖出印花税2023-08-28前10bps、当天起5bps，使用已测试的migration_costs。沿既有模型按成交片段计最低佣金，个人完整红利税未实现，必须叫模型成本后而非完整税后收益。

三个账户全24月均报告：净收益/年度收益/回撤、暴露、费用、换手、集中度、数据问题与失败订单。主要描述是各策略相对固定低换手对照的月度差；组合减REV11为增量描述。无新的p值、阈值或动态择优。当前全历史搜索分母UNKNOWN，两策略高度关联且经过TRAIN筛选，旧财务2023和MR/事件2024等观察污染必须披露，不能称完全未见锁箱或正式Alpha确认。

技术故障保留原批，在规则不变、原因有证据的前提下新批重算；不因亏损改窗口/参数/成本。迁移负收益、年度失稳或优势消失按实际情况归类失败/证据不足；正收益也只增加证据，不解锁生产。2025以后与前向验证另行处理。

## 当前具体核对

三账户纯信号适配`export_three_account_migration`已接通，复用TRAIN组合排名函数；10项信号/日期/税率边界测试通过，覆盖完整24月、首末执行月份、缺月和2025拒绝、原截面分母、并列/缺值/不补权。没有真实行情输入。

尚需主审完成：完整窗口证券元数据覆盖、自然月PV1装载预热、三账户运行身份、分钟入口与历史费用接线。配置在运行前审核完成前保持读取标志false；既有源冻结不做全局翻转。完成这些具体核对后，依据用户的统一解锁指令对该三账户范围作最后放行裁决并留痕，不增加候选、不打开2025。主对话负责最终审核，当前没有活跃实跑进程。

### 元数据核对留痕

元数据清单以`artifacts/three-account-migration-metadata-20260912-2/universe-inventory.json`为准：按原P1板块白名单和上市/退市日期筛得5218只，包含2023–2024上市313只、同期退市97只；日期冲突/异常及必需文件缺失均为0。主审发现初版通过宽泛板块分类误纳入`sh.689009`，现改为复用原`board_allowed_dotted`，两批集合差仅此一只，初版保留不覆盖。这是保持既有池定义，不是根据收益删股。

这里只读取证券基本资料、文件存在性和zip成员清单，未读取2023–2024行情；文件存在不代表日期、价格和历史状态完整。上市后资格仍须按当时P1逐日判断，数据商历史名单完整性也未因此得到证明。窗口继续冻结，等待其余具体运行前核对后统一放行。

### 读取与预热接线（2026-09-12）

新增专用`experiments/migration_runtime_20260912.py`，未接实际行情运行。共享原始日线读取函数本来解析整文件，且运行时日历默认截止2026年；本适配在数值转换前筛掉2024年末之后的原始行/复权因子，对用户QFQ请求限制右端，暂设运行时截止并清理日历缓存，结束/异常后恢复原函数。只用于独立进程，不改变既有TRAIN默认代码。P1所需更早历史仍保留；61日因子预热从2022-11-01开始，只用于首信号，不计算预热期账户收益。

主审执行13项定向测试全部通过：包含未来行毒值不解析、旧日历缓存不继承、异常恢复、未授权不初始化、首月跨月前收、24个执行月、2025请求拒绝、三账户排名及税率边界。命令：`python -m unittest tests.test_migration_runtime tests.test_migration_signal_export tests.test_migration_costs tests.test_pv1_small_portfolio_signals`。这只证明适配与合成边界，不代表真实三账户运行已验收。

真实交易日历元数据工件`artifacts/three-account-migration-calendar-20260912-1/calendar-review.json`：PV1首日2022-12-01的上一会话为2022-11-30；首信号2022-12-30、首执行2023-01-03；末信号2024-11-29、末执行2024-12-02；最终估值2024-12-31，共484账户会话。没有读取留出行情值。下一步只做真实信号导出入口与分钟回放/费用的三账户接线、运行前终审，不增加TRAIN搜索或额外审计层。

### 三账户信号入口接通（尚未实读）

新增`experiments/three_account_migration_signals_20260912.py`：默认只验证配置、原式哈希和已核证券清单；`--out`在配置授权false时于创建批次/读取行情之前拒绝。授权后按单股加载限定窗口P1与价格，复用原编译器计算REV11、负换手率，PV1复用原自然月计算；所有股票完成后再统一作原截面排名与固定50%组合，不在单股内误做截面排名。保存各股可选月的REV11五日输入、PV1全月价格对、换手值及覆盖信息，不生成未来收益标签。

准备命令已实跑成功、未读取行情；16项限定测试通过（上节13项加原公式直接复算、跨月输入、P1未知/缺历史及未授权入口拒绝等3项），最后测试中授权状态改为显式fixture，不依赖日后配置是否解锁。尚未称真实导出数值PASS。接下来是市场输入/公司行为、分钟Provider与迁移税率接线；完成后主审统一放行三个账户，不单独提前读取。

### 分钟与独立费用复核接续

现有`rev11_dynamic_minute_20260912.Provider`增加显式固定窗口参数，仅允许原TRAIN或2023–2024两种完整范围；默认仍TRAIN，旧CLI不解锁迁移。沿用同一48根分钟校验、逐日对齐与容量口径，无新撮合实现。窗口外请求在下载前拒绝，新增测试使用空订单，仅验证日期和调用范围，没有请求真实分钟数据。

主审发现独立账本`r0_pilot_independent_audit_20260912.fee_parts`原本卖出印花税恒为10bps；已按冻结迁移费用在2023-08-28起切换5bps，仍独立用Decimal计算，不调用生产费用函数。万元卖出切换前10元/后5元、买入0及TRAIN过户费边界直接金额测试通过。迁移相关测试合计19项通过；这不是实际三账户账本验收。

剩余具体接线：市场输入生成器与公司行为resolver仍写死TRAIN，需限定迁移适配；随后组装三个账户的统一执行与独立复核入口，再作运行前放行。没有新因子/参数搜索，也没有读取2023–2024行情。

### 市场输入与公司行为适配完成（覆盖上节待办）

`r0_pilot_market_inputs_20260912.main`与`resolve_event`已接受固定迁移窗口，默认TRAIN不变；迁移实读须既有配置授权true。市场输入保留首信号日的价格对，实际账户日历仍从2023开始；迁移限幅仅在账户日生成，历史ST卖出规则不变。分红文件扫描可覆盖以前报告期，按实际除权日限定2023–2024并晚于首信号，避免把报告期当成实施期；公司行为仍要求公告/登记/除权顺序和原始价格配对，未放宽一分钱对账容差。延迟到账和差异化分派仍按原依赖问题处理，不能因适配而直接接受。

`UserDataset.load`新增可选start/end，在数值转换前筛日期，默认调用输出不变；仍读取zip成员文本，不宣称物理文件字节从未包含未来行。原始价格生成使用此有界通道，保留在市原始主源与baostock历史状态/退市主源分工。

新增合成迁移公司行为价格匹配/不匹配、历史板块ST限幅、zip未来毒值不转换测试，与原公司行为/市场输入/ST退出测试共15项通过。没有真实行情读取或账户运行。下一步是三账户统一运行及独立复核入口组装、运行前终审，保持既定攒批范围。

### 统一账户入口与证据独立性

用户提出仅两候选是否过少：本批明确是两套共享REV11的策略结构与一个低换手对照，不是三个独立因子或独立Alpha。当前有限迁移可检验失效与稳定性，不能据此验收整个多因子产品；后续独立机制研究仍属于系统目标。不能为凑数复活已失败构造，也不能看完这段后把它重新称为新候选的未见留出。

`experiments/three_account_migration_run_20260912.py`统一顺序运行三账户，核对完整24个执行月、信号与输入日历身份，复用分钟Provider与执行日费用，每个账户完成后调用现有独立账本/分钟证据复核并保存结果；实际数据问题仍单列，账户不相加。尚未实跑。

合成接线发现共享引擎要求首信号日位于价格日历，已明确采用包括2022年末信号日的内部账本，单独保存并验证该初始日现金20万、零持仓、零费用，再把报告账户日历限定2023–2024；不是丢弃有收益的预热日。1900股10元买卖例子的费用14.69元/24.19元、最终199961.12元与手算一致，并已通过独立账本逐日复核。迁移相关24项定向测试通过，新增独立复核接线后两项账户测试再次通过。下一步补输入请求串接与统一运行前检查，配置授权仍false，无真实留出行情读取。
