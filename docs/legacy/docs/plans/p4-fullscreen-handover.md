# P4 全池选择窗筛选——新对话交接书

- 生成：2026-09-08 主对话（修复线闭环后）
- 适用：在新会话中接管 P4 全池筛选的主 agent（零上下文自包含）
- 项目根：`D:\AI\workspace\个人量化`（Windows + Git Bash，python 命令一律 `python -X utf8`）

## 0. 必读（按顺序）

1. `AGENTS.md`——项目边界、数据源分工、工程纪律（取数节律、离线测试零依赖、工件只新增）
2. `docs/plans/p4-factor-research.md`——P4 预登记计划，**筛选的全部规格以此为准**（§3 标签、§4 因子清单、§5 方法、§6 Gate A–D、§10 性能策略）
3. `docs/decisions.md` 顶部条目：D-2026-09-08-02（P3 闭环、八组全负基线）、D-2026-09-08-01（障碍筛选层）、D-2026-09-07-23（外部复审 PASS）、D-2026-09-07-18（交易目标重定义为单轮 5 日 +2% 障碍口径）

## 1. 现状（已验证事实）

- 测试基线：**390 项通过、1 skip**；V3 fixture smoke `[SMOKE] PASS`（命令：`python -X utf8 -m quant.cli --config configs/test-fixture.json smoke --fixture tests/fixtures/rotation/pool.csv`）
- P4 代码栈（均已提交并做单测，**但 P4 模块尚未经外部 Codex 复审**，复审已发出、与筛选并行——若 CHANGES_REQUESTED，筛选项工件作废重跑）：
  - `experiments/p4_factor_research.py`：因子/线性标签层（P4-1）
  - `experiments/p4_screen.py`：IC/十分位/Gate A/相关矩阵（P4-2），**有 `run_full_screen` 全池入口**
  - `experiments/p4_barrier_labels.py`：障碍标签（t+1 开盘、止盈净 +2% 逐只换算、4 档止损、双触记负）
  - `experiments/p4_barrier_screen.py`：障碍指标 + Gate B 二项检验，**目前只有小样本 sanity 入口（`--sample N`），无全池入口**
- 小样本（50 只、≤2023）sanity：6 因子 × 4 档均未过 Gate B，阴性对照正常——管线行为符合预期
- 机器教训：P3/P4 类全池任务内存峰值 ~2.6GB；此前两次主进程闪退疑似内存。**一律后台 + 日志文件运行，不在前台阻塞**；长时间任务启动后立即返回等待

## 2. 主任务：全池选择窗筛选（P4-2 正式运行）

**数据纪律（最高优先级）**：只用选择窗 ≤2023-12-31（构池预热从 2019-01-01 起属正常）；**任何代理不得读取/输出 2024+ 验证窗结果**，泄漏即整轮作废。

**步骤 1（现成入口）**：IC/十分位/Gate A/相关矩阵全池运行

```bash
python -X utf8 -m experiments.p4_screen > artifacts/short-foundation/p4-screen/full_screen_<日期>.log 2>&1 &
```

（`--out-dir` 默认即 `artifacts/short-foundation/p4-screen/`；工件只新增）

**步骤 2（需先实现）**：障碍指标全池运行——`p4_barrier_screen.py` 缺全池入口，需参照 `run_full_screen` 与 `run_small_sample_sanity` 增加 full-pool 模式（复用既有函数、配离线单测、不改已有语义；按 AGENTS.md 分工派 k2.7-code 子代理实施）。完成后后台运行，输出 6 因子 × 4 档止损的十分位指标与 Gate B 判定。

**步骤 3（汇报）**：汇总 IC 全表（含失败因子）、Gate A 通过名单、Gate B 通过名单、阴性对照 C1(R20)/C2(-RSI14) 状态。

## 3. 停止条件（任一触发，立即停下汇报，不自行变通）

- 阴性对照 C1/C2 通过 Gate A 或 Gate B（管线异常信号）
- 发现自己正在读取 2024+ 数据或池构建包含 2024+ 资格信息
- 内存/进程异常、数据缺失（如某源日线断档）
- 与计划 §4–§6 规格矛盾的实现需求（**执行中不得新增因子/改门槛**）

## 4. 红线

- 子代理不改 `docs/`、`CHANGELOG.md`、`AGENTS.md`，不执行 git 写操作（主对话负责记录与提交）
- 不改 `quant/` 与 P1/P2/P3 代码；P4 既有模块语义冻结，只允许「新增全池入口」类扩展
- 工件目录只新增不覆盖；取数遵守东财/baostock 节律
- 全部输出为研究用途，不得标为可执行；任何「存活候选」在 Gate D（2024+ 一次性验证）前都不可声称为有效

## 5. 完成后的固定动作（主对话）

1. 复跑全量单测 + smoke
2. `docs/decisions.md` 新增实现者记录（含全表关键读数与 Gate A/B 名单）
3. 提交 git；P4-3 独立评审（派未参与实现的新 agent 或交 Codex）
4. Gate A/B 有存活者才进入 Gate C（组合引擎 + P2 分钟回放抽验）；无存活者则结论「本轮无可执行候选」如实记录——这同样是合规交付

## 6. 挂起事项（与本任务并行）

- **Codex 全量复审终审已 PASS（11/11，D-2026-09-08-08）**：P4 代码栈定稿，基线 commit `2461670`，全量 422 项单测 + smoke 通过。
- **当前状态：全池筛选已解锁**。执行要求（终审约束）：①不得读取任何旧 P4 中间缓存与旧工件（仅历史留档），首次正式运行全部重建；②只用 2020–2023 选择窗，2024+ Gate D 继续冻结；③阴性对照显著或缓存身份不一致时 fail-closed 停止。按本交接书第 2 节步骤执行即可。

## 7. 分工调整（2026-09-08 用户决策，基于五轮复审教训）

P4 五轮外部复审的 21 个阻断发现几乎全部属于「规格理解偏差」而非计算错误——该模块是项目中规格敏感度最高的代码。据此调整：

- **P4 剩余代码活（障碍全池入口及后续小改）由主对话模型（K3）直接执行**，不再派 k2.7-code 子代理；改动量小，省掉审阅往返更便宜。机器长跑（全池筛选）仍可派子代理或后台命令看护。
- **结果解读阶段（IC/Gate 名单判读、存活判定、Gate C 设计）建议把主模型思考强度调至 high/max**——这是 P4 剩余工作中唯一真正吃智力的环节。
- 既有分工纪律的其他部分不变：docs/decisions/AGENTS 只由主对话写、外部实质审阅由 Codex 执行、机器重活不走 token。



