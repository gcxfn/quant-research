# 任务书：P3 全窗重跑（新口径）与新旧对比汇报

- 生成日期：2026-09-07（主对话）
- 执行者：用户指定的新 agent（本任务书自包含，无需其他上下文）
- 项目根：`D:\AI\workspace\个人量化`（Windows + Git Bash；所有 python 命令前加 `-X utf8`）

## 1. 背景（只需知道这些）

本项目是 A 股短线研究仓库。`p3-p1pool` 流水线在 P1 严格池（无幸存者偏差、含退市股、逐日 ST/停牌掩码）与旧池对照上，跑八组短线规则（G1–G8）的全窗口组合回放。

外部审阅共发现 5 个实现问题（流动性门禁含信号日、窗口截止超数据快照、涨跌停价 round() 舍入、G7 口径表述、`<t` 流动性 provider 交集语义），**修复已全部完成并提交，第三轮外部复审已 PASS**（git `83820b4`，记录 D-2026-09-07-21/22/23）。2024 分钟对照链已以最终口径重跑完毕（20260908 版工件）。

**现在唯一剩下的工作：重跑 P3 全窗，产出新工件与新旧对比。**

## 2. 任务

运行：

```bash
python -X utf8 experiments/short_foundation_research.py p3-p1pool --config configs/short-foundation-research.json
```

要点：

- **全程约 75–85 分钟，内存峰值约 2.6GB**。必须以「后台运行 + 定期查看日志」方式执行，不要在单个阻塞调用里等它跑完（执行环境有 2 小时上限，且此前已因此中断过两次）。建议 `nohup ... > artifacts/short-foundation/p3-p1pool/p3_p1pool_2024_fix_v4.log 2>&1 &` 形式，日志只新增文件、不覆盖已有 v1–v3。
- 流水线无断点续跑能力，中断只能整体重来——所以后台化是第一要务。
- 跑完后复跑 `python -X utf8 -m unittest discover -s tests`（基线：378 项通过、1 skip）。

## 3. 交叉验证（必须做）

1. **硬性核对（不一致即停下汇报）**：**old_pool 八组** vs 旧工件 `artifacts/short-foundation/p3-p1pool/p3_p1pool_20260907_150952.json` 的 old_pool 读数（该分支代码路径未变，必须完全相等）。参考值（full_from_warmup）：G1 +21.97%、G2 −7.72%、G3 +20.60%、G4 +5.09%、G5 +20.21%、G6 +14.38%、G7 +221.18%、G8 +7.14%。
2. **对比汇报（不要求一致，差异需解释）**：**p1_pool 各组** vs 上次中断前日志（`p3_p1pool_2024_fix_v2.log`）的读数：G1 −33.44%、G2 −71.55%、G3 −23.35%、G4 −23.74%、G5 −19.09%、G6 −17.94%、G7 −68.16%。注意：v2 日志产生于「交集语义」代码，现行代码已改为纯 <t 替换语义（复审 PASS）——2024 样本两种语义零差异，但全窗可能含差异样本，因此**逐组列出差异即可，出现差异不是停止条件**；仅当差异伴随订单/成交计数异常（如信号数大幅跳变）时停下汇报。

同时确认日志中：qfq 构建验证 6/6 通过；退市因子覆盖「研究窗通过、窗外 94 异常仅披露」；严格池 5411 只；「生产 setup 等价断言通过」。

## 4. 交付物（汇报格式）

1. 新工件路径（`artifacts/short-foundation/p3-p1pool/p3_p1pool_<时间戳>.json`，**只新增**）。
2. **新旧对比表**：p1_pool 八组 × {full_from_warmup 收益、mdd、使用率} + 两个滚动窗口收益，对照 `p3_p1pool_20260907_150952.json` 的对应读数（旧：G1 −32.80%、G2 −71.75%、G3 −19.96%、G4 −23.42%、G5 −12.84%、G6 −15.46%、G7 −62.31%、G8 −22.87%）。
3. 匹配幸存者对照两臂数字（共同股票、仅切掩码：status_free vs P1 掩码），新旧对比。
4. 第 3 节全部交叉验证的核对结果（逐项列出 一致/不一致）。
5. 全量单测结果。

## 5. 红线

- **不改任何代码**——运行中若发现 bug（含交叉验证不符），立即停止并如实汇报现象与证据，由主对话决策。
- 不修改 `docs/`、`CHANGELOG.md`、`AGENTS.md`；不执行任何 git 写操作。
- 原始数据与既有工件只新增、不覆盖、不删除。
- 全部输出为研究用途，不得标为可执行。
