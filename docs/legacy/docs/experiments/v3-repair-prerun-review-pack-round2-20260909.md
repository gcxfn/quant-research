# V3 修复轮 — Codex 运行前审核材料包 第 2 轮（2026-09-09）

**审核性质：第 1 轮 CHANGES_REQUESTED（2 项 P1 + 8 条歧义裁决）后的修复复审。A/B/C/AB/ABC 正式回测仍未执行。**

## 第 1 轮裁定 → 修复对照

### P1-1 R120 负索引前视
- 修复：`daily_rank_positions()` 内 `_mom(w)` 守卫 `idx >= w`（禁一切负索引路径）；任一窗口不足 → 该标的当日无排名。
- 新反例单测 `test_mutating_future_tail_does_not_change_any_rank`：idx=119 当日篡改数据末尾价 50→500，排名与 ranked 列表逐位不变（旧实现下 R120 会从 +338% 变 −56.2% 并污染横截面）。
- 主对话独立审查：另造边界场景亲测——idx=119（120 根收盘）无排名；idx=120 有排名且引用 closes[0]。

### P1-2 臂A/臂C 窗口按市场日历截取
- 修复：新增 `market_calendar_up_to()`（全池日期并集 ≤ d）与 `calendar_window_returns()`：先截最近 n+1 个**市场交易日**，日收益只在「该会话与其前一市场会话都有行情且均在窗内」的日期对上计算；替换原"跳缺日后取末尾 n 条"路径。`correlation_blocked()`（A）与 `vol_target_weight()`（C）均改用；豁免判定逻辑未动。
- 新反例单测 `test_gap_in_middle_not_counted_as_recent_returns`：41 市场日缺 20 日 → 缺口标的窗口内有效收益 10 个、无跨缺口收益、双成员重叠 10<15 豁免 w=1.0；无缺口对照满窗 20 不豁免。
- 主对话独立审查：逐行读了窗口构造与豁免路径；确认日历=全池日期并集（无外部日历依赖、确定性）。

## 8 条歧义裁决落实
- #2/#3：读法保留，按市场日历修复后成立（共同样本不足/零方差不约束并披露）。
- #4：**送审包第 1 轮描述有误已纠正——纯 C 也按 w/3 槽位**（代码本就如此，未改行为；docstring 已注明"槽位规则适用于含 A 或 C 的形态"）。预登记 V4 同步澄清。
- #1/#5/#6/#7/#8：接受现状，未动。

## 验证（主对话亲跑复验）
- `python -m unittest tests.test_v3_repair_round`：23 项 OK（含 2 个新反例）。
- `python -m unittest discover -s tests`：602 项 OK（跳过 1）。
- **baseline 重跑逐位复现 R3**（新批次 `artifacts/v3-repair/v3-repair-20260909-p1fix-verify/`，原批次只增不覆盖）：年化 10.194589%、回撤 −58.854843%、夏普 0.459087、现金日 52、水下 1543、engine_check 双 True。

## 实现者披露
- 日历窗口下，连续无缺标的恰好得到 n 个日收益（与旧满窗数量一致，阈值语义不变）；仅缺行情标的收益数减少并可能触发豁免——属修复目标行为。

## 复审重点建议
1. 两处修复路径的负索引/跨缺口封堵是否彻底（尤其 `market_calendar_up_to` 并集日历边界）；
2. 两个新反例测试的判别力（是否真能在旧实现下失败）；
3. baseline 逐位不变性。

## 回执格式建议
PASS（解锁 A/B/C/AB/ABC 正式回测）/ CHANGES_REQUESTED（逐条列差异与位置）。
