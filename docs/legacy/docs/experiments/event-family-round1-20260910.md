# 事件家族挖掘轮 R1 工件摘要（event-family round-20260909）

**判定（主对话按冻结判据宣布，待 Codex 工件复算确认）：无构造晋级 → 本轮 FAIL 封存。**
阶段一（2020-01-01→2023-12-31 选择窗）四个做多构造（E1_bull/E2/E3/E4）无一通过晋级线（某 h∈{3,5,10,20} 超额毛均值 ≥34bps 且事件日块 bootstrap 95%CI 下界>0）；阶段二留出窗无检验对象（n_constructs=0）。按预登记不追加参数搜索。

## 阶段一结果（选择窗，可交易组口径：次日开盘→第 h 市场会话收盘，超额=绝对−全 universe 等权基准）

| 构造 | h=3 | h=5 | h=10 | h=20 | 晋级 |
|---|---|---|---|---|---|
| E1_bull 业绩预告利好 | +0.31% | +0.36%（CI −0.10%~+0.81%） | **+0.69%（CI −0.033%~+1.41%）** | +0.73%（CI −0.48%~+1.95%） | 否（CI 下界均<0） |
| E2 业绩快报·发布效应 | −0.09% | −0.38% | −0.97% | −1.54% | 否（全负） |
| E3 重要股东增持 | −0.04% | −0.15% | −0.30% | −0.76% | 否 |
| E4 股票回购·公告日 | −0.15% | −0.15% | −0.33% | −0.37% | 否（且带幸存者偏差披露） |

- **E1_bull 是唯一正超额构造，且随持有期单调上升**（+0.12%→+0.73%，h=1→20），与 PEAD 经济逻辑一致；n_exec=7,354（h=10）。**距晋级线最近的是 h=10：超额毛均值 0.692% 远超 34bps，但事件日块 bootstrap 95%CI 下界 −0.033%——差 3.3bp 未过**。解读：点估计有信号，但事件日聚集（同日多股公告）使有效样本量缩水，显著性不足。
- E2 全部为负且随期限恶化——"快报发布效应"在本窗为负（快报发布后跑输全市场基准）；与预登记的解释限定一致（发布效应≠利好漂移）。
- E3/E4 均无信号；E4 受"回购源缺失退市公司"幸存者偏差影响（note_20），不得声称代表完整无偏事件家族。

## 覆盖与数据披露

- 事件计数：总事件 29,781（去重/冷却后）；可交易事件 E1_bull 24,384 / E2 3,011 / E3 16,137 / E4 13,593；选择窗左界前剔除 19,536（2018—2019 预热）；周末公告 15,232；解禁索引 25,691 条事件级记录（自 5,661,143 行持有人明细流式去重，note_23）。
- 全 universe 基准任务 5,551 只；装载失败 104 只（no_rows 101 / no_data 3），逐条留痕入机会口径。
- E3 同股同日合并 17,096 行；E4 冷却剔除 42,445；E1/E2 同日去重 2,800。

## E5 解禁过滤器（描述性对照，非正式口径）

E1_bull h=10：解禁窗口内事件 82 个，其自身可交易均值 +0.759% **高于**窗内其余事件（+0.304%）；剔除前后均值 0.309%→0.304%。**结论：本窗内解禁避雷过滤无增量价值（反而略删好事件）**——与"解禁=利空"直觉相反，仅描述性留档。

## E1 利空"不接飞刀"过滤器工件

`stage1.json e1_bear_filter_artifact`（利空预告事件的逐期限表现），供未来任何均值回归系构造复用。

## 局限性

- 2020—2023 选择窗内筛选，留出窗未动用（无晋级构造）；2024—2026 已被其他研究观察的口径约束不变。
- E4 幸存者偏差（源缺退市公司）；解禁源早年覆盖偏薄（2016—2019 事件数显著少于 2021+）。
- 基准为全 universe 等权（价格有效即入），未做流动性过滤（预登记 V4 读法）。

## 后续（按因子挖掘总计划）

本轮按预登记封存。下一候选：③资金流家族（龙虎榜 top_list/top_inst 机构席位、两融异常变化——数据已齐）或按用户指示调整顺序。E1_bull 的"差 3bp"不得作为追加参数或换判据的理由；若未来重开预告家族，须按家族代表制另行预登记（可含公告类型细分/时点分层等经济区别构造）。

## v1.1 计数字段更正（2026-09-10，工件审核 CHANGES_REQUESTED P2 附注；原文保留不改）

本节更正上文引用的计数字段语义错误，更正源自 r2 再生成批次（`artifacts/event-family/round-20260909-r2/`，统计结果与原批次逐位一致，见其 manifest `reproduction_check`）：

1. **"总事件 29,781（去重/冷却后）"有误**：旧 counts 的合并字段 `n_events=29,781` 实为 E3 构造器事件数与 E4 构造器事件数**之和**（两构造器各自产出的 `n_events` 被同名键相加）。按构造分层后：E3 事件数与 E4 事件数分别以 `n_events_E3` / `n_events_E4` 披露，全家族事件总数应按构造分别陈述，不得引用合并值。
2. **"E1/E2 同日去重 2,800"有误**：2,800 实为 **E4 回购源同股同日重复行剔除**（现更名 `e4_same_day_dup_dropped`，仅 E4 构造器内去重）；E1/E2 的同日覆盖计数（forecast 在先覆盖 express，`e1e2_same_day_forecast_wins`）为 0。
3. **"可交易事件 E1_bull 24,384 / E2 3,011 / E3 16,137 / E4 13,593"更名**：该计数的实际语义仅为"存在下一市场日（entry_date 非空）"，未经 ST/停牌/无量/一字涨停/池门禁等任何成交门禁，更名 **`n_entry_possible_<构造>`（入场可能事件）**，不得再称"可交易事件"。
4. 计数现全部按构造分层：`n_events_<构造>`、`n_entry_possible_<构造>`、`n_events_in_window_<构造>`（入场≥2020-01-01）、`n_complete_label_<构造>_h<h>`、`n_executed_<构造>_h<h>`；语义见工件 note_24。摘要 md/工件渲染已同步更正。
