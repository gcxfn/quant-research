# P4 修订轮 R1 执行记录

依据：`docs/plans/p4-revised-round-20260908.md`。本报告由实现者归档，独立工件复审尚未完成。

## 当前结论

G5 同窗基线、全池统计、原固定随机诊断均已完成，运行退出码0。4进程在257.79秒处理970个交易日，日志为本轮 `parallel-full-run.log`。详见 `p4-parallel-speed-20260908.md`。最终 `round_summary.json` 状态为 `PENDING_INDEPENDENT_REVIEW`；独立工件复审未完成，障碍步骤2及Gate D保持冻结。

随机诊断工件 `null_diagnostics.json`：全日期有效965日、市场门槛开启有效352日；各20种子均为0次通过Gate A，预登记异常次数阈值均为2，诊断PASS。日数少于970是主期限h=5窗尾标签不可得等有效样本条件造成，未补读截止日之后数据。此PASS仅指设计型随机对照未触发异常，不是候选盈利或独立审阅PASS。

全池统计工件：`artifacts/short-foundation/p4-screen/revised-r1-20260908/screen/p4_screen_parallel.json`。12个候选中7个通过原Gate A统计门槛：f1_r10、f2_overnight_ratio、f3_vol5_vol60、f4_atr14_price、f4_rv5_rv60、f5_dist_20d_low、f6_f2_x_market。Gate A只按原绝对IC等判据，不代表方向与预登记相符或扣费盈利；特别是F2实际正IC与原negative方向相反，不得看结果后追改方向。C1虽通过统计门槛仍仅描述性对照，不纳入候选。尚未运行本轮障碍筛选或组合盈利验证。

## 同窗 G5 基线

工件：`artifacts/short-foundation/p4-screen/revised-r1-20260908/g5_selection.json`。

- 选择窗2020-01-01至2023-12-31，实际970个交易日；10万元共享账户，原G5固定第五日退出政策和用户费用，严格<t流动性。
- 扣费累计收益−8.0847%，年化−2.1663%，最大回撤−25.2118%，228笔平仓，累计费用3617.59元。
- 该基线自身亏损且回撤超出约20%目标；新候选须自身扣费后为正并优于同窗基线。它只是日线组合研究，不能当分钟可成交或实盘证据。
- 原有效P3工件只有2023年选择窗（−1.5825%），没有2020—2023完整选择窗；本次是相同政策的新同窗运行，不能伪装为旧工件数值复现。

## 全池输入及运行中修正

新建批次目录，5231只股票另加市场序列；原始生成5260731条因子、4177687条标签，截止2023-12-31。两类中间件于16:41命名，16:45前完整保存。原暂停轮工件保持原样。

16:28启动，原全池筛选在19:08仍统计未结束；发现8套重复标签查询表（其中4套未使用），进程提交内存约14.6GiB、机器可用物理内存约1.1GiB。停止此进程，保留当前轮已生成输入。改为逐日标签引用及向量复用，恢复加载只取必要字段并共享日期/代码字符串。恢复运行不重算因子、不换种子、不改研究参数，不引用旧暂停轮缓存。

测试：初始116项相关测试及合成smoke通过；内存修正后33项相关测试通过，额外恢复端到端测试通过（修订轮测试现为4项），合成smoke再次通过。优化前后四年、多期限、并列值/缺失标签、两种市场状态的完整统计dict相等；这是实现者等价证据，非独立PASS。

## 独立复审状态

尝试续派原独立审阅子代理，工具返回 `agent thread limit reached`，未能启动本次复审。此前窄范围读取修复PASS保持有效，但不覆盖本轮新增随机诊断、同窗G5工件、内存修正和最终筛选工件。阶段一不能据此宣布整体完成。

用户要求继续下一步后，新独立子代理 `p4_r1_artifact_review` 已成功启动，只读审阅本轮新增统计/随机诊断/同窗G5及最终工件。前述线程限制是上一次派发失败的历史记录，当前等待新裁决。

主对话准备检查（不替代独立审阅，未运行障碍标签）：正式障碍入口的 `_validate_p4_screen_results` 接受本轮最终工件；对7项Gate A通过候选使用原方向化B1，只有f1_r10、f3_vol5_vol60、f4_atr14_price通过。其余四项B1失败；这些价差是双腿研究指标，不是用户多头账户收益。仍待独立阶段一裁决后才能运行48项障碍检验。
