# 因子引擎速度优化：阶段主审

实现代理：Leibniz，GLM-5.3/high。隔离目录：`D:/AI/workspace/engine-speed-opt-isolated-20260912`。主仓尚未合入。

## 已核查

主审读取 `_speed_work/diff/operators_np.diff`、新测试与真实数据 benchmark 源码。改动为新增向量化单列排名内核，修改排名包装、矩阵构建和结果取回三处。截面有限值过滤、并列平均秩、字段覆盖集合和跨单元清理规则保留；从逐日期字典改为矩阵，优化范围合理，无须全面二维重写或全系统重审。

主审直接读取 out 中 147/556 股四份 JSON，确认新旧 operators_np 身份不同，而股票数、日期数、预热截断位置和 universe 身份一致；各三公式的因子序列冷/暖哈希、train_test 哈希、逐月 IC、方向和 gate 输出均相等。两个排名公式的 train_test 倍数分别为 1.466/1.379（147 股）、1.790/1.898（556 股）；非排名对照为 0.988/1.293，显示计时存在噪声。这仅是该脚本描述性统计调用的有限样本证据，不外推整批加速。

直接解析 `_speed_work/test_factor_miner_baseline.log` 与 `test_factor_miner_all.log`：两侧均为 642 项，6 failures + 8 errors，失败/错误测试 ID 集合相等，新增失败集合为空。不能表述为全测试通过。其中包含隔离副本缺 Git/预登记文件，以及锁箱测试被 statistical_validity 前置条件拒绝，不能一概称为环境故障。本轮不修锁箱测试，不据此解锁留出数据。

## 需补齐后收口

1. 完整股票池旧/新对比尚无落盘结果。主审曾根据空查询输出错误判断旧侧进程 27212/25940 已退出；随后代理 Get-Process 与主审 CIM 均确认原 PID 仍在、启动时间未变，原 session 29947 仍运行。已明确更正并要求保持原任务，不重启。空观察结果不能单独证明进程终止。
2. 原 benchmark 未传 n_tests，走描述性 95% CI，需从保存的 monthly IC 补 N=248 汇总对比；无须为此重复加载全池。
3. 报告须区分字段冷暖缓存、跨公式遗留缓存及进程累计峰值。单次并行计时只能作为初步性能证据，不能保证整批倍数。

停止规则：完成同输入新旧数值、掩码和 N=248 汇总一致性，真实单元测速及相关回归后即终审；不再扩展其他算子或新增整批重算。任何采用结论仅覆盖本次 CS_RANK 优化，不代表统计方案、成本模型或 Alpha 有效。

## 全池结果落盘后的主审补记

2026-09-12 13:54:19新侧`ab_new_n5552.json`落盘，实进程24788随后消失，主审同时核对工件和进程状态确认全池完成。原/new均5552证券、972日期、244预热位置、同universe身份。三公式的冷暖序列hash、monthly_ic逐项、完整train_test hash、公式和方向一致。主审独立对比工件 `artifacts/r0-numeric-recompute-20260912/engine-full-ab-main-review-1.json`。

两个排名公式冷算36.662→19.923秒、13.026→9.765秒；暖缓存16.008→2.033秒、11.813→1.825秒。非排名冷算1.006→0.995秒。但新侧train_test为91.190/30.720/50.616秒，旧侧29.756/26.865/9.029秒；bundle构建815.152→1834.481秒。本机内存争用和运行环境不对称，**不据全池结果承诺整批总提速**，也不以暖缓存倍数替代正式冷算路径。

待代理完成N248口径补验和终稿后按既定范围终审。主线九条信号导出已于13:55:50启动，期间不合入优化源码。主线在此前优化运行期没有启动全池bundle，不能把新侧性能下降归因于主线并行大任务。

## N248主审与限定裁决

主审已读取`ab_old_nt248_n147.json`/`ab_new_nt248_n147.json`及benchmark源码，确认实际传入`director.train_test(n_tests=248)`，147证券同输入三公式冷暖序列hash、monthly_ic和完整train_test hash均相等。证据`artifacts/r0-numeric-recompute-20260912/engine-n248-main-review-1.json`。N248仅指原实现统计汇总口径等价，不使原统计方法有效。

裁决：本次CS_RANK代码改动的既定数值等价验证已达到停止线，限定接受；不追加第三层测试或全池重测。主仓合入须待正在运行的九条信号导出结束，不能在运行中改计算依赖。合入时核旧源码hash与受审基线相等、应用受审差量和相关定向回归即可，不重做全系统审核。

报告rev3中两处归因不采纳，已要求作者只改表述：非排名对照同样变慢支持环境争用解释，但不能证明所有差异均不是实现退化；12个锁箱测试使用合成tempfile前置，异常只能证明既有准入条件未满足，不能无证据说只是缺主仓artifacts或主仓必然通过。新旧相同14个失败不构成全测试绿，不影响本次排名等价的限定裁决，也不解锁留出。

## 合入完成

作者报告表述已实际修正（标题仍保留rev3，不因代理自称rev4当成另一个版本）；最终交付身份见`engine-optimization-acceptance-1.json`。主审在九信号导出退出0、manifest完成后，核主仓原`operators_np.py`=D71B17D1…与受审基线字节相同，再拷入受审新实现EC7A99E3…和新增测试F4537DDF…；未覆盖其他实现。

主仓运行`tests.test_factor_miner_numpy_path`＋`tests.test_factor_miner_rank_vectorization`，42项通过、0.122秒；含非有限值测试的NumPy运行警告，不是测试失败。本次CS_RANK优化交付与合入完成，Leibniz已关闭；不再扩大审计。该优化可用于后续同算子研究，不能保证全部单位或整批按同倍率加速。
