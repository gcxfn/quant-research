# B01 主审与 B04 事前冻结

2026-09-12。按新计划 P0 检查实际实现和输出，通过 B01，允许启动新的三账户迁移批。没有证明策略晋级或可执行。

## 实际证据

- `artifacts/strategy-backend-b01-20260912-1/tests.log`：9 项限定测试通过。包含缺失/重复分钟、身份/日期/复权错配、NaN/Inf、非法 OHLC、负量额、超异常线拒绝；strict 保持默认。三账户合成端到端经过 24 次调仓、48 个合成会话与独立现金/市值/费用核算，最大现金和净值误差均为 0；不冒充实际 484 会话。
- 报告正常、仅分钟差异、首账户中止和未解决公司行为路径覆盖。仅分钟差异输出 `DATA_UNCERTAIN_DESCRIPTIVE` 及比较；公司行为仍无 metrics。总体 `UNKNOWN/BLOCK/executable=false`。
- `experiments/migration_b01_evidence_20260912.py` 与同目录 `evidence.json`：实际 002356/2023-09-01 在 strict 拒绝，披露模式返回原始48根并保留差异；无网络查询。
- `baostock-reuse-sources.json`：288 个唯一证券日，逐文件 SHA 检查，保留输入清单摘要。只收旧批原始 minute-raw 和已核 baostock inventory；不继承 Tushare 归一化覆盖路径。
- 缓存来源改为校验前登记，失败股票日不再遗漏。中止 data-issues 保留之前的差异与最终错误。原文件不修改。

## 裁决和限制

价格1%、总量5%继续仅作为运行异常线，没有放宽，不表示收益误差安全界。差异已知会影响分钟路径/容量的可能性仍保留。遇到新超线样本先分类，不自动改参数。限定测试及真实记录足以进入完整迁移，停止追加泛化审计。

进程检查未发现可明确识别的迁移回测；另有 python stdin 进程，无法仅凭命令行确认旧探针身份，因此不重启。小时探针状态文件仍 WAITING_HOURLY_QUOTA，只作状态文件描述。

## B04 固定联合压力情景（新批收益前登记）

全三账户一次：单边摩擦5→10bps，参与量0.005→0.0025；其他信号、窗口、Top10权重、20万独立账户、现金规则、历史费率与数据政策一致。入口 `--stress`，manifest 显式记录参数，独立账本核对按参数取值。无额外参数网格、无新增价格触发情景。该压力不能覆盖供应商全部价格触发/分钟容量误差；任何翻转和集中度必须如实报告。

下一步 B02：`artifacts/three-account-migration-accounts-20260912-4`，使用上述纯 baostock 集合和 `research_disclose`。完整结果后处理 B03，公司行为依实际持仓而定；B04 独立新目录运行一次。

## B02 批4实际中止与限定修复

批4 REV11完成484日/292成交/14项已入账公司行为，独立现金误差2.91e-11、权益5.82e-11元；200项分钟差异，没有实际公司行为未解决问题。低换手随后在002505/2024-08-01空分钟中止，组合未运行。

实际输入明确当日 `tradestatus=0`，分钟CSV仅表头；这是已知停牌日，却被动态调用方仍要求提供成交分钟的实现缺陷。修复只在显式分钟路径对当日日线明确停牌的订单记录 `suspended_on_execution_date` 未成交/到期，保留持仓及原订单；不伪造分钟、不删除股票、正常交易日缺分钟继续阻断。47项相关测试通过（suspension-tests.log），含实际问题等价回归、正常分钟/缺失/重复及三账户端到端。股票随后退市的估值问题仍须按实际持有处理，不能因修好停牌请求而认定收益有效。

批4完整留存；修复后统一三账户在新批5重跑，复用批4已下载来源。这是原规则实现修复，不是新增策略尝试；B04压力规则不变。

批5在同股2024-09-02再次中止：输入明确 out_date=2024-08-30，原入口还请求退市后分钟。Provider新增只读历史退市状态入口，调用方对已退市订单记录不可成交；不填充任何价格/分钟。48项测试通过，关键负例证明保留持仓、final_equity=None。批5保留，统一新批6运行；低换手可能完成账本但无法报告有效收益，此为数据不足而非零损失。2025+未读取。

批6三账户均有最终状态：REV11完整描述净收益−10.1285%、回撤33.3264%；低换手484日账本完成但80日持有退市股缺价，收益无效；组合在600187/2024-08-01 high差0.03元（1.1194%）超临时线中止。该日实际唯一买单5100股、限价2.75，首开2.58两源一致；首根量79245000，压力容量198112股，远超该单。其买入触发/价格不依赖全天high，且open/low/close一致。`artifacts/migration-600187-adjudication-20260912-1/review.json`保留原记录/订单/费用独立核算。

主审仅批准这个哈希绑定原始源、日线值、实际开盘跨价买单且压力首根容量足够的例外披露。通用异常线不变；strict仍拒绝，卖单/容量不足负例仍拒绝。实际48根探针已通过，无网络、不改任何bar。这不证明数据源正确，也不外推任意价格差，差异仍记未解决。统一批7使用这一记录的审查元数据，保留批6全部失败；压力也须满足相同订单条件，否则中止。
