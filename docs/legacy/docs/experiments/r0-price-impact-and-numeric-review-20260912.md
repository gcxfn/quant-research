# R0 PRICE修复影响与两单元数值终审

2026-09-12，主对话直接执行及审阅。范围为TRAIN和事前选定两个诊断单元，不是全248统计有效性验收。

## 已验证事实

- 旧快照与修复后均使用NumPy、同一5552股正式名单、同一TRAIN及有界预热，统计N=248。两次进程均正常退出0。
- PRICE实际服务：旧4760股，新4996股。新增236股均由stock_basic.outDate确认为退市股，走raw_apply_factor；556股因读取窗口无原始价格未服务。首因子日前原价兜底行数为0。
- 新增股票在35个成熟月均有P1合格样本，每月53–116只；首月105只、末月72只。该缺口真实影响可评估截面，不能归为无关历史股票。
- 旧批两单元均值复现差0，CI端点误差最大5.56e-17。新旧各自全月IC/n/月键均与独立oracle一致；首/中/末月份原始配对重算一致。

| 执行单元 | 旧mean IC | 修复后mean IC | 差量 | 原协议布尔结果 |
|---|---:|---:|---:|---|
| META_combo_01\|w:N | 0.016436027421129457 | 0.016085356025000858 | -0.000350671396128599 | false→false |
| REL_cond_05\|w:N20,5 | 0.046451973504912376 | 0.04725964722285234 | +0.0008076737179399662 | true→true |

这些是IC变化，不是收益变化；原统计协议尚无有效性主张，true不等于因子有效或科学晋级。

## 裁决与后续

1. 接受PRICE加载修复及两单元数值复算的限定证据，停止本路径继续递归审核。
2. 两个单元均含截面排名，新增可用股票已改变数值；两个布尔结果不变不能证明其余单元或交易/成本结果不变。旧整批不能继续用作修复后候选筛选依据，应保存为旧源历史结果。
3. 后续需要用修复后完整截面重算当前248单元的月序列及统计；不得只重算原15个布尔过线者，从而遗漏修复后可能改变结论的候选。该步骤是已有候选的数据修正，不是新增假设搜索。
4. 重算入口、输出身份和预算审阅后另行启动；如果先只重算因子/月序列，必须明确其不是完整成本回测完成。成本/成交结论要在相应评估前刷新，不能把旧成本工件拼接成同源新批。
5. 新月序列落盘已接入director候选输出，使后续统计复核可直接利用工件。统计协议候选v5仍未生效，VALIDATION/LOCKBOX仍锁，不能以本验收宣布Alpha。

## 可核验证据

- artifacts/r0-numeric-recompute-20260912/frozen-replay-1/two_unit_export_frozen_snapshot.json
- artifacts/r0-numeric-recompute-20260912/frozen-recompute-1.json
- artifacts/r0-numeric-recompute-20260912/post-fix-replay-1/two_unit_export_post_fix.json
- artifacts/r0-numeric-recompute-20260912/post-fix-recompute-1.json
- 同目录frozen-replay-1.log与post-fix-replay-1.log：阶段计时、自然退出证据见主对话exec输出。

旧阶段计时总708.853秒，修复后约752秒；后者峰值工作集6011.4MB。旧导出内存0是已修采集显示错误，不是实际零内存。两单元耗时不能直接当248全回测的线性预测。
