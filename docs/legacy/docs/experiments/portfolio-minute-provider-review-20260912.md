# 组合分钟成交入口限定接线

2026-09-12。fixed_capital_portfolio新增可选execution_bars_for_orders(date, orders)，仅opening_cash_only模式启用。日线继续负责信号时点规划、参考价、历史状态和日结净值；每次完整请求生成后调用提供者，不使用旧已成交记录作为订单来源。Replay继承同一Ledger，按分钟时间/证券排序撮合，部分成交实际持仓进入下一月，主循环公司行为仍只记一次。默认日线入口不变。

调用者收到订单快照，包括原可能未成交订单；若提供者缺请求证券、返回其他日期/证券或重复时间行则拒绝，不回退旧日线、不把缺数据当未成交。提供者仍须检查合法分钟时间网格、源价格量和历史状态；本接线自身不是数据校验全集。

69项组合/执行/新接线测试通过。独立手算例：10000元、计划500股、0.5%×20000日内bar量只能买100股；下月卖单基于实际100股，最终现金9989.5元。新增缺分钟/重复分钟反例通过。测试为合成TRAIN时间数据，无真实留出读取。

主审限定接受入口接线，真实全量分钟回测未启动。下一步实现按实际请求拉取/缓存TRAIN分钟的提供者，优先覆盖第一个完整调仓日并核共享现金；全量跨月回放须中断于缺失/异常数据，记录请求缺口，不生成不完整盈利结论。暂不改变最低佣金合并口径，不增加策略变体，不重跑R0因子统计。
