# PV1 精测 Codex 运行前审核送审包 第 3 轮（2026-09-10）

审核性质：运行前代码审核第 3 轮。第 3 轮（上一轮）2 项 P1 均修复并经主对话工件级复验；留出窗未运行（`HOLDOUT_UNLOCKED=False` 实测仍锁、中央台账未触碰）。前包：round2。**留出窗考试时点按用户决定暂缓（D-2026-09-10-18），攒因子批次后统一开考**——本轮审核为代码资格审核，PASS 不触发立即运行。

## 一、两项 P1 处置（主对话复验）

### ①全卖全买 → 差额调仓
- 目标名义额=调仓日开盘（卖出前）NAV/N（口径不变）；出局全卖；留任按差额（开盘市值−目标）超配卖/低配买；新入按目标买。`do_sell` 支持按股数部分卖出（basis/contrib 按 frac 摊分）；`do_buy` 改**增量并入**既有持仓；费用只按实际成交金额计。
- 留任股补买遇涨停开盘→保留原仓不补（`topup_limit_up_open` 计数，不重试）；差额卖受阻→冻结股数顺延（`pending_trims`）。
- **过程自纠披露**：首版 r3 实现 `do_buy` 整体替换留任持仓（补差额丢原仓，net17 年化 −34.15%），由工件暴露后修复为增量合并并加反例单测；buggy 批次留档 `round-20260910-inwindow-r3-rejected-buggy-do-buy/`。
- 主对话复验：34 项模块测试（+9 反例：涨停留任不卖[旧实现必失败]、差额调平精确 NAV、等权零交易、topup 并入原仓）复跑全绿；r3 工件计数核对一致（entry_limit_up=84、topup_limit_up=1、trim_deferred=1、pending 全 0）。

### ②候选身份冻结代码与数据身份
- `candidate_identity()` = 参数集 + `code_sha256`（本文件/预登记/screen_pricevol.py/mr_statarb.py 四文件）+ `data_version`（与 factor_miner DATA_VERSION_PARTS 同源，失败回退冻结字面量）+ `universe`（P1 basic 名单 canonical 哈希+index_member_all 快照 sha256）→ canonical JSON sha256。主对话实测成分齐全。
- 语义披露：任一成分（含源码）变化=新候选=新的唯一留出窗机会。
- 台账消费顺序不变（先写台账→独占目录→再读数）。

## 二、in-window 对照（r2 全量调平 → r3 差额调仓，描述性非判定）

| 指标 | r2 | r3 |
|---|---|---|
| 净年化 17bps | +7.301% | **+7.682%** |
| 月收益 95%CI | [−0.891%, +2.614%] | [−0.861%, +2.645%]（仍含零） |
| 27/37bps 年化 | +6.052%/+4.815% | +6.581%/+5.492% |
| 月均单边换手 | 99.33% | **86.32%**（全池对照 99.80%→17.31%） |
| 夏普/最大回撤 | 0.420/−28.60% | 0.435/−28.50% |

如实披露：PV1 月度换名极高（留任少），差额调仓省费有限、年化 +0.38pp；CI 仍含零。顺带修 md 渲染夏普单位 bug。

## 三、剩余读法（6 条）

1. 留任差额买受阻：保留原仓；涨停不重试，缺价/无量经 pending_buys 按冻结差额顺延。
2. 差额卖受阻：按冻结股数顺延，成交价与冻结折算价可能不同（如实）。
3. 卖出费用现金残留使低配补仓略低于目标（topup_no_cash 计数）。
4. 差额判零阈值 1e-12（浮点噪声；等权留任=零交易）。
5. do_buy 增量并入（r3-rejected 批次为该 bug 留档反例）。
6. 身份任一成分变化=新候选=新的唯一机会。

## 四、建议复算

1. r3 net17 月序列 bootstrap 重放（块3/B=10000/种子20260909）。
2. 任取一月调仓：留任股差额计算（开盘市值−NAV/N）与成交股数、费用按实际成交额核对。
3. 涨停留任不卖反例测试复核（第 3 轮 P1 直接对应）。

## 五、解锁与运行协议（结合 D-18）

本轮 PASS → 代码资格冻结（HOLDOUT_UNLOCKED 保持 False）；留出窗运行时点=用户决定的候选批次统一开考日，届时联合多重检验口径另行冻结预登记后方可解锁运行。
