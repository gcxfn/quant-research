# 低波动10会话研究审核

## 运行前（2026-09-09）：主对话PASS

主对话审读Sol三个新增文件，确认截止数据门禁、逐证券生成5/10标签、16MB每日落盘、仅sl015投影、旧完整因子截面排名、共同成熟日期、双有效交集按完整市场日历隔10会话、全期去重后拆年，以及双正才保留探索线索的判定，均与运行前预登记一致。正式运行只限 `artifacts/short-foundation/p4-lowvol-h10/round-20260909/` 新目录。

初稿的全市场行情/标签同时驻留问题已由Sol修为流式生成。主对话沿旧生成/分区代码确认排名总体并澄清预登记，不新增资格过滤来暗改对照。日历函数与原排名的两个初步疑虑经代码核对不成立，不计为缺陷。

Sol相关60测与合成CLI smoke通过；追加收尾后主对话现场运行新入口8测及CLI smoke通过，并独立构造共同成熟最后允许日期/次日排除的反例，结果正确。测试中的截止验证器注入断言覆盖原始价、复权因子及本地qfq加载器；检查目的是发现越窗读取或配对端点偏移，发现时会阻断正式运行。未跑含2024+读取的无关全量测试。

最小源标签保存在daily-source，每条包含入场日期/价格/股数、净额、退出价、结局与会话；摘要的全目标索引可回连这些原始标签，配对明细单独保留。manifest包含研究配置、原P4配置、foundation配置、市场源和每日计数。后续主对话从原始标签独立聚合验证，不以Sol自报或测试数代替工件验收。

本PASS仅允许运行用户已批准的5/10单一探索，不解锁Gate C/D，不表示策略有效。正式研究结果与最终裁决待工件完成后补充。

## 工件后（2026-09-09）：主对话PASS，假设FAIL

正式工件完成；主对话实际运行 `tests.p4_lowvol_h10_artifact_audit` 退出码0。每日计数与manifest相同，源最大日期2023-12-29、无越截止入场；独立逐日重建/逐证券去重后的全期、年拆、覆盖和排除数与正式摘要全部精确一致。5日期限复现旧94,651轮及净均值（仅2.35e-15累加差）。工件审核PASS，10日净均值−0.13414118%、配对净改善−0.00228514个百分点，两个继续条件均失败。终止该假设，详见 `docs/experiments/p4-lowvol-h10-conclusion-20260909.md`。
