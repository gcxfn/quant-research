# R0 系统计划独立审查报告（71 行版原计划）

- 审查对象：`docs/plans/r0-completion-and-acceptance-plan-20260911.md` 71 行版（修订前）。
- 版本指纹：sha256 `C7EB9ACF7671D39E326A18BDA105AD05BE5C07557C23B0EFA819D6617AE9623B`；物理 71 行；全文见附录A（防行号失效）。
- 审查方式：GLM-5.3/MAX 只读静态审查 + 主对话实审 A/B 摘录合并记录；未跑测试、未触碰进程、未读取留出窗数值。
- 结论：**原计划 CHANGES_REQUESTED**；修订计划（同路径替换稿）已落盘，待独立复核。
- 本轮仅两处文档写入，无测试、无正式研究、无门禁变更。

## 1. 八项缺口（引用原 71 行行号）

| # | 缺口 | 定位 | 问题摘要 |
|---|---|---|---|
| G1 | 范围与退出 | L7、L71 | 只有 R0 工程口径成功/退出（L7「R0 成功指冻结候选按冻结协议完整筛选…」、L71「退出条件是 R0-1..4 的适用证据闭合」），无 R0 完成/系统可信/策略有证据/产品可执行四门分离，无数据/实现/回测三链 VALID/INVALID/UNKNOWN 状态与系统级退出定义 |
| G2 | PIT 与身份 | L35、L46 | L35 身份账只要求「记录」，无首尾双采、无数据内容哈希清单、无字节归档；L46「时间截断性质」无定义——缺真实批次 LHB t+1 抽查、复权双源逐股路径披露、截断/扰动未来数据的 prefix 不变性检验边界 |
| G3 | oracle 边界 | L38、L56 | L38 检查器只要求外部预期全集，未禁止复用被测统计/撮合/费用核心；L56「独立复算」未定义独立性——无手算金标准微账本与独立统计实现，纯/np 同源一致未被排除为正确性证据 |
| G4 | 统计校准 | L40、L44 | L40 负例全为工程性（漏项/翻转/空工件），无零效应 Monte Carlo FWER/FDR 校准与功效正例；L44 合成路径无已知答案正例，防不了静默清零式假绿 |
| G5 | 尝试史与污染 | L60 | 仅「R1 死区簿受控化、完整历史证据图收口」一句；无跨轮/挑选/手工看数/LLM 提案全账、无污染图逐次记账规则、无锁箱技术读取入账要求 |
| G6 | 成本 | L63 | 成本全部推迟至组合精测预登记，但 R0 链内 cost_ladder 用固定 bps——未声明固定粗筛不得作为产品逐笔成本证据，逐笔最低佣金/共享现金/权益守恒未入 R0 工件核对 |
| G7 | 科学分类 | L37 | 区分完整成功/研究淘汰/技术失败/中断，但无三链证据状态分类；技术失败可与证据不足混淆，「不缩 N」只覆盖计数不覆盖证据状态 |
| G8 | 系统阶段无退出 | L58-63 | 后置工作四条均无完成条件/验收/退出定义；无「每关键风险正负证据、冻结字节复现、影响链复审」的系统通过判据 |

## 2. 主对话实审 A/B 摘录（静态证据）

(1) `experiments/factor_miner/data_fields.py:129-131`——数据身份=批次目录 id 字符串串联，非内容哈希：

```python
def data_version():
    """registry 用：各来源批次目录 id 串联（换批次=新研究身份）。"""
    return "|".join("%s=%s" % (k, v) for k, v in sorted(DATA_VERSION_PARTS.items()))
```

(2) `experiments/factor_miner/director.py:1006-1009`——evaluate_units 全量评估先于输出目录创建，该阶段异常零工件：

```python
train_results, bt_results, factors = evaluate_units(
    units, tr_bundle, tr_dates, tr_mask, tr_rows, cost_ladder,
    region_start_idx=tr_idx)
os.makedirs(out_dir)   # 独占新批次（竞态下已存在会直接抛）
```

(3) `experiments/factor_miner/stats.py:163-175`（逐月产出 n）与 `experiments/factor_miner/director.py:232-235`（门禁调用只传 IC 序列）：

```python
# stats.py:172-175
ic = rank_ic_cross_section(fac, fwd.get(m, {}))
n = sum(1 for s in fac
        if fac.get(s) is not None and fwd.get(m, {}).get(s) is not None)
out.append({"month": m, "ic": ic, "n": n})
```

```python
# director.py:232-235
ics = stats.monthly_rank_ic_series(factor, closes, dates,
                                   pool_mask=pool_mask)
ic_vals = [r["ic"] for r in ics]
gate = stats.continuous_gate(ic_vals, direction, alpha_total=alpha_total,
```

静态事实：逐月 n 在 stats 层产出，该门禁调用只消费 IC 序列；每月 n 分布/覆盖/缺失归因是否进入任何工件字段不在本两段内，未核证（对应修订计划 S5 要求的披露工件）。

(4) `experiments/factor_miner/stats.py:183-194`——bootstrap 复用 mrs.block_bootstrap_mean_ci 同一实现核心；月数 ≤ 块长 3 为退化保护：

```python
def monthly_block_bootstrap(monthly_ics, ci=IC_CI):
    """月块 bootstrap：块长 3 个月、B=10000、种子 20260909、CI 默认 95%。
    ...
    月数 ≤ 块长 3 → degenerate（CI 不可用、门禁不通过，不静默改块长）。
    ...
    return mrs.block_bootstrap_mean_ci(
        pool, b=BOOTSTRAP_B, block=BOOTSTRAP_BLOCK_MONTHS,
        seed=BOOTSTRAP_SEED, ci=ci)
```

静态含义：被测统计与 MR 家族共享同一 bootstrap 核心实现，跨家族独立性依赖实现正确性本身（对应 S3 独立 oracle 要求）；≤3 月退化保护存在，非「毫无下限」。

## 3. 证据限度与 UNKNOWN 项

- 上述均为静态代码阅读证据，不构成真实运行复现结论。
- F5（复权双源静默切换：`experiments/factor_miner/engine.py:140-153` 数据集 qfq 缺失即无报错落入 raw×因子路径，两路径锚点不同，截面水平秩与时间变换等价性未证）与 F12（去重窗口归一化 `experiments/factor_miner/validator.py:10-12` 为冻结设计；若发生意外语义过度合并会压低 N 并放宽 α=0.10/N 每检验阈值）均为**条件性风险，非已确认缺陷**。
- UNKNOWN（未核证，不推断）：1697 项全仓测试当前是否通过；HEAD 06cb5d2 官方 TRAIN 批进程当前状态；实际尝试污染程度；实际数据漂移。

## 4. 处置

- 原计划：**CHANGES_REQUESTED**（八项缺口见 §1）。
- 修订计划：同路径替换为《以R0为入口的量化研究系统可信度验收计划》，待独立复核；复核通过前 R0 不启动、门禁不变。
- 本轮仅写入本报告与修订计划两份文档；无测试、无正式研究、无门禁变更。

## 附录A 原计划全文（71 行版，sha256 C7EB9ACF7671D39E326A18BDA105AD05BE5C07557C23B0EFA819D6617AE9623B）

```markdown
     1: # R0 正确完成筛选：后续执行与验收计划
     2:
     3: 2026-09-11。依据用户最新要求：Codex 负责计划、派发、独立审核；实际代码构建由 GLM-5.3、思考强度 max 执行。此计划不构成重启授权。
     4:
     5: ## 1. 目标与当前证据
     6:
     7: R0 成功指冻结候选按冻结协议完整筛选，准确报告通过、淘汰及技术异常；全部淘汰也可成功。不要求盈利，不以测试数量代替正确性，不要求先完成整个产品的所有开发。
     8:
     9: - wave3 送审提交已核对：`0fb349b`。仓库存在大量其他未提交修改，禁止清理或混入修复。
    10: - 本次独立复跑原 wave2 合成探针，退出码 0；原六类反例的输出符合修复方向。探针运行成功不是 wave3 独立 PASS。
    11: - wave3 所称 1697 项全仓测试是送审方证据，本次未独立重跑，不能署为主审测试结果。
    12: - `director.run_real_pipeline → evaluate_units → cost_ladder` 在正式 TRAIN 链中，因此账本修复仍需复审；统计筛选和账本输出的具体依赖要分别列出。
    13: - 旧分层计划继续适用；其中 Top15 属历史规格，现产品为 0..N 只及冻结权重规则，但产品规则设计不并入本次 R0 实施。
    14:
    15: ## 2. 顺序与工作包
    16:
    17: ### R0-1：wave3 独立复审（Codex）
    18:
    19: 检查六项修复与修改过的测试预期、协议修订，重点独立验证：
    20:
    21: 1. 长期停牌没有虚构卖出，持仓、数量、市值、资金占用和税费一致。
    22: 2. 开盘边界缺价只回看已知价格，最终收盘估值与陈旧状态正确。
    23: 3. 基准和组合采用同一经济时间区间；不仅单资产，还覆盖多资产、部分受阻、跨月及无信号月。
    24: 4. 未决状态跨月持续、恢复与递延超限不重试契约一致，不通过忽略异常制造收益。
    25: 5. 纯/np 的轴入口拒绝非法数据，同时保留合法稀疏字段；旧测试改判没有删除有效反例。
    26: 6. V3 受控排除代码单独验收，不把它与 R0 门禁混为一项。
    27:
    28: 交付：独立报告，逐项关闭或列出最小反例、影响路径和修复要求。若有缺陷，由 GLM 修复、Codex 复审；不凭送审包宣称闭合。
    29:
    30: ### R0-2：运行与工件验收契约（Codex 定义，GLM 补实际缺口）
    31:
    32: 先盘点已有设施，复用后再补缺口，禁止为了统一入口重写引擎。需形成以下具体工件：
    33:
    34: - 冻结输入账：源候选、拒绝行、去重映射、正式基准单元、描述性窗口单元及实际统计 N 的逐项对应。250 行、246 结构和评估单元数不能互代；以实际冻结输入复核，不预填最终计数。
    35: - 运行身份账：受审代码文件全集、配置、数据内容身份、候选文件、运行环境、实际引擎与回落情况、新输出目录和命令。代码提交号不能替代脏工作树文件身份；候选上的摘要也不能替代 run_meta 的完整身份。
    36: - 工件验收：预期单元集合与实得集合双向一致，拒绝重复、漏项、异常伪装淘汰；核对 registry、candidates、run_meta、去重及拒绝记录之间的映射和计数。
    37: - 异常纪律：完整成功、研究淘汰、技术失败、中断分别表示；异常不算成功覆盖，不事后缩 N。现有正式入口在 evaluate_units 后才创建输出目录，中断证据/恢复行为需专门核验；不预设必须实现续算，允许新身份新目录从头重跑。
    38: - 检查器调用必须带外部预期全集和身份；仅出现 PASS 或进程返回 0 不构成完整验收。
    39:
    40: 验收负例：双方同时漏项、重复单元、None/值翻转、门禁翻转、错误身份、空工件、两路径同异常、被截断的批次。复用已有负例，新增测试针对确认缺口。
    41:
    42: ### R0-3：运行前小样本与资源验收
    43:
    44: 先在临时目录用合成数据跑真实编排路径，检查数据→因子→统计→账本→工件→验收器。不得读取真实 VALIDATION/LOCKBOX 或触碰正式锁箱台账。
    45:
    46: 在允许的 TRAIN 范围内，事前固定风险覆盖样本和候选，验证真实装载与双路径实际参与、确定性、时间截断性质及资源使用。小样本不代替全量完成，历史残批不作完整对照。
    47:
    48: 按实测选择纯/np 正式引擎及资源预算，双引擎大任务顺序执行。未通过的科学定义不得仅靠两路径一致放行。已有有效证据只做变更影响复核，不无条件重跑所有历史测试。
    49:
    50: 交付具体启动包：最终受审身份、候选/数据清单、命令、新目录、资源上限、失败停止办法、旧工件隔离与运行后验收命令。Codex 给出运行前裁决后，由用户决定是否恢复 R0；旧解锁旗标不替代此次决定。
    51:
    52: ### R0-4：正式 TRAIN 与运行后独立验收
    53:
    54: 仅在取得恢复授权后执行。GLM 承担需要的代码实现与运行准备，Codex 审核。正式运行结果进入新目录，保持 VALIDATION/LOCKBOX 原状态。
    55:
    56: 运行后全量核对单元、计数、身份、晋级映射和异常；对事前规定的统计/门禁边界及账本案例独立复算。明确给出「R0 工程完成且工件验收通过」或具体阻断，不以发现幸存者为成功条件。
    57:
    58: ## 3. 后置工作
    59:
    60: - R1 死区簿受控化、完整历史证据图收口：在其被下一轮机器门禁使用前完成。
    61: - 276 项变异测试：不是本次 R0 启动必要门禁；如执行，须隔离独占副本、未变异基线通过，不在多人写入的工作树修改再还原。
    62: - registry 历史优化、成熟库有界比较：按实际依赖和风险排期，不为框架迁移延迟 R0。
    63: - 20 万元、0..N 只、权重和分档交易规则及产品输出：另做组合精测预登记、实现和验收，不能由 R0 PASS 自动解锁。
    64:
    65: ## 4. 派发状态与边界
    66:
    67: 本次尝试向 GLM-5.3 max 派发只读准备任务，并重试一次；两次均被提供方路由拒绝：`unreadable_encrypted_agent_task`（目标提供方无法读取加密任务消息）。代理没有完成盘点，也没有修改代码。未擅自替换模型。
    68:
    69: 待该模型通道可用后，首包为 R0-2 的设施盘点和缺口清单；代码任务以 Codex 确认的缺口及验收条件为输入，不先行大改。Codex 可独立继续 R0-1 审核，模型派发故障不等于审查结论。
    70:
    71: 本次只创建此计划，未修改生产/研究实现，未启动正式研究批次。退出条件是 R0-1..4 的适用证据闭合；工时在 wave3 裁决与 R0-2 缺口确认后估计。
```
