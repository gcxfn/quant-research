# R0 生命周期接线限定审查

2026-09-12。按用户要求直接修复，限定于影响截面/标签的数据生命周期，不扩展为全系统审计。

## 已修复与已核证据

- engine.pool_mask_by_date 从BASIC_PATH取得并校验IPO/outDate，传入Timeline；上市前不计年龄，上市后第252交易会话才可能合格。停牌会话仍计上市年龄，停牌当天资格另行排除，不将规则改为252个成交日。
- 正式带P1掩码的load_stock_rows去掉上市区间外价格，记录过滤数；IPO缺失/非法、outDate非法、先退市后上市、同代码矛盾日期不能静默采用。静态名单对此单列invalid_lifecycle_metadata，不冒充no_raw_daily。
- python -X utf8 -m unittest tests.test_r0_lifecycle_wiring tests.test_factor_miner_price_loader_coverage：18 tests OK（0.268s）。包含251/252边界、停牌计龄、逐日ST/停牌、退市价格/资格边界和异常日期，并复用已有PRICE复权回退测试。
- experiments/r0_lifecycle_probe_20260912.py真实TRAIN抽查输出：artifacts/r0-lifecycle-review-20260912/real-probe.json。8940条basic记录无重复代码；原5552股正式名单中上市/退市日期缺失或非法0条。这只证明当前元数据结构，不证明不存在历史代码变更/重新上市。
- sh.600918上市2020-06-03，首次合格2021-06-16；sh.600956上市2020-06-29，首次合格2021-07-08；两者上市前251会话合格数0。sh.600175退市2020-08-14，退市后合格数0；TRAIN内36个ST日、26个停牌日合格数均0。

## 口径与剩余边界

- ST使用原始逐日isST，非今天名称。最小反例验证未来某日ST不反向修改此前资格；恢复预热沿既有配置，测试以0预热隔离本项行为。
- 停牌行如有数据源提供的收盘价，是估值观测，不代表可成交；缺行不凭空补出可交易价格。R0月末到下月末收盘收益是描述性标签，可以跨停牌期，不能当该日可成交净收益。成交/锁单递延在策略回测前按真实交易规则核对，不借本次修复静默改标签。
- 因子close与月收益标签沿同一个bundle复权价格字段；此前236退市股回退与首因子日前行数0的证据保留。不重复审计整套公司行为；该证据也不证明所有分红数据完整或组合账本已验收。
- 基本表只有一条代码记录，缺少完整证券身份事件表；因此代码变更、吸收合并、重新上市的全历史一致性仍UNKNOWN。若实际出现日期/价格冲突，按受影响证券处理；不因为表无重复代码就宣布无此风险，也不新建身份管理框架。
- 有限证据支持接受IPO/P1与带掩码价格生命周期修复。原all-248-repair-1已中断0单元，新目录重算前须通过入口检查。统计UNKNOWN与留出锁定保持；不宣称全系统生命周期无缺陷。
