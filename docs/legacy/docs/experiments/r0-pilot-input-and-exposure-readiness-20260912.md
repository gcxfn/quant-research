# R0机制小试：真实输入与暴露诊断准备

2026-09-12，主审继续直接执行R0后续研究；优化仍由Leibniz负责。本文不代表九条真实信号已经导出，也不是收益或统计验收。

## 已产生的证据

- `portfolio-real-input-probe-2.json`：固定浦发10日真实输入限定通过，主价格源与baostock七字段相等，P1信号/状态/限价/参与量/税前分红/实际扣费进入账本，独立现金重建一致；没有因子选股。
- `dividend-input-inventory-3.json`：TRAIN728文件与冻结R0证券超集相交，9639实施行。566条送转上市日晚于除权日，4条缺上市日。按固定35个月初执行日相交后，已知日期会影响可卖时点的范围收敛为20个证券×除权日事件；4个缺日期事件另保留。它们只有与候选实际持仓/订单相交后才是本小试具体问题，不能把源记录数直接报为错误交易数。
- 分红多行201组（R0内198）中，6组报告期或支付条款不同；其中仅2组支付条款真正不同。其他同支付条款记录不能仅因ann_date/end_date不同就重复入账，也不能假定其完全没有溯源差异。
- `dividend-payment-conflict-probe-1.json`：两组不同金额均做原始价格交叉核对。601601的2021-06-30前收差为1.3元，源候选0.1/1.3；600188的2022-07-14差为2元，源候选0.4/2。价格支持1.3和2，拒绝任取第一条或直接相加；这是数据源与价格对账证据，未查发行人实施原公告，不冒称最终公司行为实证已经齐备。

566条的计数包含源多行；20事件计数已按证券×除权日去重。R0证券范围现金派息日均与除息日一致，源外179条现金日期差异不阻断本轮。

## 新准备的研究输出

`experiments/r0_pilot_signals_20260912.py`保留全部月末截面分数、有限/缺失/非有限数量、原方向、Top10目标、现金占比和未来TRAIN会话可用性。六个R0代表必须逐月复现repair2 IC及n，三对照不伪装原R0单元。未导出真实信号前不报股票名单。

`experiments/r0_pilot_exposure_diagnosis_20260912.py`只接受完整九条导出及其实际文件哈希；输出36对公式逐月共同截面Spearman与目标权重重叠。确定性同序/反序例分别得到+1/-1、重叠0.475/0。无p值和新通过线，不把高相关当机制已被解释，或把低相关当净Alpha。三项信号选择测试也已通过。

该工具另输出候选代码第一次可能持有以后直至TRAIN末的全部问题事件，保留锁仓可延续的可能性；这是保守待核范围，不是已经实际持有。不能只拿上月目标列表代替真实持仓，或删除有公司行为的证券来获得更干净的收益。

## 运行顺序

1. 优化子代理全池进程释放内存后，以新目录执行一次九条信号导出。主审已批准此信号与暴露诊断范围，无须再次泛化审计。资源紧张不能以股票抽样代替正式全截面CS_RANK语义。
2. 对完整导出运行暴露诊断，筛出实际需接线的事件与候选价格范围。
3. 适用公司行为按经济事件去重、裸价对账与实际可卖日接入固定账本，然后运行已列出的固定费用小试；当前实际收益小试仍未开始。

本轮实查优化子代理python24788仍运行，CPU计数持续增长，系统可用内存约0.6–1GB。主线没有启动另一份全池bundle，不干预其他用户进程。当前等待只限大内存导出；期间已完成事件范围收敛、两组支付差异实证和暴露诊断代码。

## 后续启动与公司行为解析准备

优化新侧全池工件于13:54:19完成、进程释放后，主审于13:55:50启动：

`python -X utf8 -u -m experiments.r0_pilot_signals_20260912 --out artifacts/r0-pilot-signals-20260912-1`

exec session63320；实进程27108，启动桩25424。已输出building TRAIN bundle，manifest为RUNNING。继续等待同一运行，不以无新输出重启。此为已批准的信号诊断导出，不是收益小试或统计晋级。

新增`experiments/r0_pilot_corporate_actions_20260912.py`：按证券、除权/登记/支付/股份上市日和金额归并支付条款，保留源多行及替代金额；仅唯一匹配裸价除息差的事件才返回已对账。不把送转总比例与其构成再相加，缺日期、价格不唯一匹配、停牌边界、实施公告晚于登记等返回DATA_ISSUE。返回`stock_sellable_date`及`ledger_timing_compatible`，不把日期不适配的事件直接送入现有立即可卖账本。

4项测试通过：重复报告期支付不重复、输入顺序不影响冲突匹配、金额相加/两候选均容差内拒绝、延迟送转可卖、缺失/未来日期和非法金额。4个真实固定事件复核通过：600000税前0.6、601601的1.3（排0.1）、600188的2（排0.4）、603078税前0.1加30%送转；最后一例股份2020-05-07上市，除权2020-05-06，返回timing不兼容。证据`pilot-ca-resolver-real-probe-1.json`；这些是输入解析，不是组合现金/送转最终接线通过。

开发期间该新解析模块曾短暂放入factor_miner目录，进入该目录通配身份清单；发现后移到独立experiments脚本并更新测试引用。没有更改运行依赖文件，当前code_identity与本批开始快照完全相同；保留此事的留痕，不据目录新增误判数值污染或重新运行。运行中的配置、信号脚本和实际计算依赖继续冻结。

## 行情输入接续程序

已准备`experiments/r0_pilot_market_inputs_20260912.py`，只接受完成的暴露诊断与其信号manifest身份，按第一次可能入选日期取真实价格/状态至TRAIN末。主价格源用UserDataset裸价，缺该证券时明确标baostock来源；历史ST/停牌仅用baostock，不用当前名称。保留缺价会话、裸价差异、未解决preclose跳变和公司行为替代记录，不将有问题证券删除后回测。

输出逐证券完整裸价bars、限幅、事件解析、问题日期，以及独立TRAIN交易日历。下游不能以候选有价格日的并集压缩交易日历。缺状态行明确status_eligible=False，保留真实估值价；不向前填充为可成交行。价格必须adjustflag=3；上市前/退市后价格不直接进入交易输入，并留异常记录。

3项新检查覆盖历史状态缺失、ST/停牌、IPO/退市边界、重复日期和拒绝复权价；与公司行为/信号选择合计10项通过。此程序尚未消费真实九条输出：真实导出仍在原session63320/PID27108内构建bundle，CPU持续增长、可用内存降至约0.5GB，保持原进程而不重复启动。适配账本对问题持仓/事件的实际处理仍待后续完成，不能把准备程序写成真实账户已通过。

### 三种真实价格/状态输入配对

`pilot-price-status-real-probe-1.json` 保存固定三组输入及全部实际bars/状态：600000在2020-07-20..31主源裸价10行、交易状态允许10；600175在2020-08-07..20因UserDataset无代码走baostock原始价，退市日2020-08-14，6行中08-07正常、08-10..14为tradestatus=0的停牌行，保留价格但阻断成交，退市后不编造行；600956在2020-06-22..07-10主源10行、全在IPO日期2020-06-29后。

三组检查原始adjustflag、生命周期边界及历史状态逐行配对一致。新股的状态允许不代表P1上市满252会话：P1入选仍由已修复的信号导出掩码独立限制，账本bar保留正常状态不放宽信号池。三组固定输入没有因子选股，也没有对候选收益或可交易Alpha下结论。
