# 独立审阅报告：C4（定向修复、最小重放与公共系统裁决）

- 审阅对象 run：`20260922T051244-trust-c4-c4fc42`
- 审阅者身份：主对话会话（ZCode / GLM-5.3），与实施者子代理不同会话；同时担任本轮编排者。
- 审阅时间（本机）：2026-09-22 05:20—05:40
- 绑定身份：代码 HEAD `ae226d9`；工作树改动=既有未提交批次 + C4 追加（`docs/plans/p3-band-contract.md` §11、`docs/evidence/{affected_runs.csv,trial-ledger.md,trial-ledger.json}`、`docs/research/stock_event_research_prereg.md`、两条 `docs/decisions/2026-09-22-*.md`、C1 interface_contract.json 升 1.1）；证据目录 23 项哈希登记。

## 审阅范围与未覆盖范围

**范围**：C4 全部 8 项门 + G4 工程退出条件核对 + 工程批次（C0-C4）总收口。**未覆盖**：未逐字复核 claim_correction_log 六行的每一处原始文档出处（以独立复算关键数字 + 抽查代替）；环境类开放项（editable 安装）留用户。

## 独立动作（实际执行）

| # | 动作 | 结果 |
|---|---|---|
| R1 | 检查器 | `EVIDENCE_FILES_OK`；23 项证据；**junit 804/0/0/0**；0 错误 |
| R2 | 零引擎改动核验 | `git diff --stat src/quant/backtest src/quant/portfolio` = **空**；全部已跟踪文件改动 = 既有批次 8 项 + C4 授权目标 3 项（trial-ledger×2、p3-band-contract），无越权 |
| R3 | 合同修订方式核验 | `git diff docs/plans/p3-band-contract.md`：**纯追加 §11**（估值 mark carry-forward 口径），原文零删改；同步 interface_contract.json 升 schema 1.1 + amendments A-01 |
| R4 | 台账补登核验 | affected_runs.csv 旧行全保留 + 4 个 attrib2 运行追加（含 failed 运行如实标注）；trial-ledger 按 engineering_replay 分账（独立配置 22 / 冒烟 0 / 工程复现 4 / 结果查看分列） |
| R5 | **首年收益独立复算** | 从 `A1/A2_daily_equity.parquet` 亲算：A1 2015 = 570,254.54/500,000−1 = **+14.0509%**（与文档一致）；A2 = 512,998.78/500,000−1 = **+2.5998%**（文档 +2.5999%，差 0.0001pp 属 parquet 两位小数存储 vs 全精度，展示精度内一致，不构成差异） |
| R6 | 裁决与决策记录核验 | system_verdict.md：ACCEPTED_SCOPED，"通过了什么/没证明什么"分列且与 C2/C3 证据一致；两条决策记录（FIFO 错配=策略侧整仓退出+分批止盈为整仓绝对目标→无逐批风险原语→保持 FIFO；席位账同源=attrib2 与主 run 逐位相等属工程复现+cap 命中 0→口径差无观测样本）逻辑闭合 |
| R7 | 测试与开放项 | 804/0/0 归档（C0-MIN-01 关闭）；issue_register 8 项：6 关闭、2 open_needs_user（editable 环境、版本文字同步——均非阻断、非合同）；CRITICAL=0、阻断 MAJOR=0 |

## 逐门裁定

| 门 | 裁定 | 依据 |
|---|---|---|
| C4-01 缺陷有最小反例与独立预期 | **PASS** | issue_register 8 行全带 contract_clause/证据/处置；DEF-01/02 以只读调查+书面裁决关闭（附 46 笔卖出 oversell=0、cap 命中 0 的实证） |
| C4-02 修复未夹带策略/费用调整 | **PASS** | R2：零引擎/策略代码改动；全部修复为合同文字、决策记录、台账、声明 |
| C4-03 受影响 run 最小重放完整 | **PASS** | R4；受影响面正确收缩为"统计复算+登记"（无代码缺陷→无重跑义务，符合主计划 §9 依赖图规则） |
| C4-04 无关路径控制组语义等价 | **PASS** | anchor_comparison 20 项（v4 FULL-bin −2.8959%/−27.294% 等）前后语义等价；引擎目录 diff 为空是最强控制 |
| C4-05 运行起止身份一致 | **PASS** | run_manifest.json（全量测试运行含起止哈希）+ run_records |
| C4-06 旧错误解释修订 | **PASS** | claim_correction_log 六行对应主计划 §9 全部六项；R5 独立复算关键修正值 |
| C4-07 测试日志与命令可复核 | **PASS** | tests.xml 804/0/0 + pytest_full.log（C0-MIN-01 七年之约关闭：713→804 实测归档） |
| C4-08 G4 裁决与局限明确 | **PASS** | R6；局限 8 条如实（交易所逐笔/ETF 腿/板块/2021+/帽绑定/attrib2 订单产物/合成切片/舍入追认） |

## 核心发现

**事实**：C1-C4 工程批次未发现任何引擎代码缺陷；公共系统在主板股票半日合同范围内以三层独立证据（账本 10 臂逐分对账、撮合 9 臂 5,334 单 100% 一致、语义链含 C-05 关闭与 11:30 不变性）达到可复现标准。六项旧错误解释全部落账关闭，受影响运行登记完整，旧版本只读保留。

**推断**：ACCEPTED_SCOPED 成立——剩余局限（模型选择类与环境类）不妨碍 C5 归因复算，按主计划 G4 退出条件，无收益关联的工程扩张到此停止。

**未知/待用户**：费率舍入规则追认（默认裁定在用）；创业板等板块权限证据；editable 安装与版本文字同步（C4-OBS-04/05）。

## 技术意见

**accepted**（工程验收：C4 通过，8/8 门；**G4 达成，工程批次 C0-C4 全部验收通过**）。

- 工程退出：按主计划 §9，更换框架、数据库、全市场分钟回放、界面重写等不再属于本线；仅新暴露的阻断缺陷可重开工程门。
- 下一阶段（C5 归因复算）**未在本轮授权内**：需用户按主计划 §18 另行批准。C5 输入已就绪：B1 事件表（102=39 签发+63 满席）、口径纪律（claim_correction_log）、Dev 沙箱与参考账本/撮合。
- 用户留存决策项：①舍入规则追认；②未提交批次（含 C0-C4 全部新产物）的 git 提交方式——当前一切改动均未提交，建议以分批提交落地（工程批次一组、凌晨修正批次一组），由用户决定；③板块权限与环境项。

此意见绑定上述身份；新提交/数据/配置改变后需复核。
