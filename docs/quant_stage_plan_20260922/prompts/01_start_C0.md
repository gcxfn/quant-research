# 发给实施 agent：仅启动 C0

请以 `01_master_plan.md` 为阶段总计划，先阅读完整目标、硬边界、权限和C0章节；这条指令只授权执行C0现状接管与文档交付，不授权后续阶段。

项目 `gcxfn/quant-research` 的阶段A基线v4和阶段B四候选已经完成。不要再次布置或重复全部A/B，不要用旧README代替新报告。先检查当前HEAD和工作树以及是否已有后续修复，以实际可读版本为准；不要reset/clean/stash来抹掉未提交工作。

本轮目标固定：50万元、个股主收益＋现金、税费后全账户CAGR>10%、每日MDD幅度<20%；原费率、真实半日限价/严格穿透、T+1、资金结算和持仓硬上限不变。当前AGENTS中的ETF主线和其他冲突须列入冲突表；不要顺手改引擎或放宽限制。

只生成C0所需的：
1. 当前版本和证据索引，区分有效/作废/待复验。
2. 有来源的合同快照、冲突表和样本访问记录。
3. AGENTS/主计划的最小修订建议（不是声称已合入）。
4. 下一阶段数据/接口与实际权限缺口。
5. 阶段报告和供独立审阅者检查的证据清单。

可以在新C0证据目录写文档，不得覆盖旧运行、原始数据或哈希基线，不得读新的Val/2025+行情或收益，不得跑新策略、调参数、连接券商、提交/推送或修改main。真实缺文件/权限时写blocked并给出已检查范围，不能虚构内容。

所有“实际运行/已通过”必须有命令、退出码和原始证据。实施者只写completed/pending，不替用户或独立审阅者写accepted。完工后按主计划的一页回报模板提交并停止，不自动进入C1。
