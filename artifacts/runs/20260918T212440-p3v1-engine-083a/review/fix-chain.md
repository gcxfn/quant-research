# v1 引擎修复链记录（2026-09-18 晚）

| 阶段 | 引擎 sha256:16 | 测试 | 事件 |
|---|---|---|---|
| 首版交付 | `dcbdc73cbbcb8b01` | 19/19 | 执行者交付；主对话初验通过（sha/测试/§7.7 符号）；两项疑点移交双签 |
| 首轮双签 | 同上 | — | **REJECT**：C1（桥接披露硬编码 0）+ M1（兜底无视显式股数）+ 8 项 MINOR，见 `reject-report.md` |
| 修复版 | `494310195438cbfb` | 21/21 | 执行者修复：C1→L311 返回真实计数；M1→L1507/L1586 `min(显式股数, 可卖)`；新增 test_20（部分风减兜底）/test_21（m4 送转 added clip 次日可卖）；test_02 重写为真实 T+1 违例扫描；am 决策点冻结（L977/L1050）；docstring 加预算守卫限定（L916）；manifest 路径相对化 |

## 偏差披露

1. **run 目录未按指令新建**：主对话要求新建 `<ts>-p3v1-engine-fix-<rand>/` 且旧目录不动；执行者实际在原目录原地重生成 manifest（旧 manifest 的首版状态——sha `dcbdc73c…`、19 测试、`bridge.post_cutoff=0`（即 C1 的不实值）、inputs 行数 pins——已被覆盖）。首版证据以本文件与本目录 `reject-report.md` 留档补偿；首版引擎字节不再可复原（sha 记录在案）。
2. **修复版 manifest 首次重生成时丢失桥接披露**：`data_checks` 缺失（C1 修复了函数返回值，但结果未进 manifest）。主对话验收发现后，于 2026-09-18 晚用引擎自身 `assert_daily_halfday_bridge` 对真实数据实测并写回：`rows_checked=18,486,939`、`range_violations=3`（样本 sh.600864 2015-06-15 am / sh.600568 2015-11-23 am / sz.000671 2019-03-27 pm）、`pre_cutoff=2,870,381`、**`post_cutoff=1,824`**（0.011 元绝对容差）。
3. **手算样例 3 双口径标注**（MINOR-4）执行者未做（文件哈希与首版逐位相同）；主对话补写（盯市 −1,532.50 / 成本 −2,537.50 并列），manifest outputs 哈希同步更新为 `7c1e86bfdc8c103e…`。
4. 执行者完成汇报中"11,742 行"为口误，实测 1,873 行（`wc -l`）。

主对话未改动引擎代码；上述 2、3 属验收方补齐披露与文档，不改变引擎语义。diff 级复审见 `re-review.md`（复审后写入）。
