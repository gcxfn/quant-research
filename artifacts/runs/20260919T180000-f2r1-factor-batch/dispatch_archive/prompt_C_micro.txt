你是 F2 首轮因子批量的 C 族（微观结构，20 个因子）实施代理。工作目录 D:\量化（Windows，Git Bash，python 用 D:/量化/.venv，polars 1.44）。先读 D:\量化\AGENTS.md。

## 权威文件（先读，逐字遵守）
- 冻结预登记：`docs/research/exp-20260919-factor-round-f2r1-prereg.md`（§1、§2、§3-C 因子表 20 个——公式为语义定义，字段映射由你完成并披露）。
- 评估 harness：`src/quant/factors/eval.py`（API 见 `artifacts/runs/20260919T171500-f1-eval-harness-main/delivery_report.md`）。
- 数据（全部 `data/raw/xiaodefa/<数据集>/<批次>/chunk_YYYYMMDD.csv`，ts_code 风格，**先各读 1 个分块摸清列名再动手**）：
  - 竞价：`stk_auction_c`（现价竞价聚合，字段自探）；`v0 面板`（`data/processed/baostock-daily-20260917/daily_2015_2024.parquet`）提供 open/preclose/成交量额。
  - 资金流：`moneyflow`（买/卖×小/中/大/特大单金额字段，映射"主力"=大+特大）。
  - 涨跌停价：`stk_limit`（up_limit/down_limit 逐股逐日，注意复权口径是否与面板价格一致——抽 3 只票 3 日核验，一致才用）。
  - 九转：`stk_nineturn`（字段自探）。
- 停牌缺行→窗口内可得日计算并披露；某数据集字段与语义无法对应→该因子如实标 fail。

## 任务
1. 输出 `artifacts/runs/20260919T180000-f2r1-factor-batch/outputs/C_micro/`。dev 2015–2020，月频月末，`FactorEvalConfig(horizon_days=20, n_groups=10, eval_freq="M", min_history_rows=60)`。
2. 逐因子 C01..C20：按 §3-C 语义公式（映射实际列名）出因子表 → `C<NN>.parquet` → evaluate_factor → `C<NN>.json`。**字段映射表放 family_report.md 首节（因子 ID×实际列名×变换）**——这是 C 族最重要的披露。
3. `family_report.md`：映射表 + 逐因子一行表（IC/t/单调/2015/覆盖/换手）+ §2 初判存活列 + 涨跌停价口径核验结果 + 缺失披露。20 个全部报告。
4. 纪律：data/ 只读；无 git；中文 Write 工具；预算 ≤60 分钟；signal_date ≤2024-12-31 断言。
5. 最终消息：完成数（含 fail 数）、初判存活 ID、最显著 3 个、映射要点与核验结论、输出路径。